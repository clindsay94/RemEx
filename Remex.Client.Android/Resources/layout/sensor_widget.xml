<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:padding="12dp">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="System Sensors"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:textSize="14sp" />

    <LinearLayout
        android:id="@+id/sensor_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_title"
        android:layout_marginTop="8dp"
        android:orientation="vertical">
        <!-- Sensor rows will be added here dynamically or just have placeholders -->
        <TextView android:id="@+id/sensor_1" android:layout_width="match_parent" android:layout_height="wrap_content" android:textColor="#E0E0E0" android:textSize="12sp" android:text="--"/>
        <TextView android:id="@+id/sensor_2" android:layout_width="match_parent" android:layout_height="wrap_content" android:textColor="#E0E0E0" android:textSize="12sp" android:text="--"/>
        <TextView android:id="@+id/sensor_3" android:layout_width="match_parent" android:layout_height="wrap_content" android:textColor="#E0E0E0" android:textSize="12sp" android:text="--"/>
        <TextView android:id="@+id/sensor_4" android:layout_width="match_parent" android:layout_height="wrap_content" android:textColor="#E0E0E0" android:textSize="12sp" android:text="--"/>
    </LinearLayout>
</RelativeLayout>
