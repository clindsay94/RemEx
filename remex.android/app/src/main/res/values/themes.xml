<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <!-- Use the app icon for the splash screen -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <!-- Switch back to normal theme -->
        <item name="postSplashScreenTheme">@style/Theme.RemEx</item>
    </style>

    <style name="Theme.RemEx" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Quick Settings tile destructive-action confirmation (TileConfirmActivity, RemEx-sxfp).
         Floating/translucent so the confirm dialog reads as an overlay on whatever was already on
         screen when the tile was tapped, not a full-screen app launch for a single yes/no. -->
    <style name="Theme.RemEx.TileConfirm" parent="Theme.Material3.DayNight.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>
</resources>
