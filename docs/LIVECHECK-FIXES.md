# Live-check fixes tracker

Temporary. Deleted when every row is done. Umbrella bead: `RemEx-62k5f` (live-check list: `RemEx-h8zi3`).
Branch: `fix/live-check-findings`. Source: Connor's on-device pass over the 2026-09 perf audit
(`git show f95a92c:docs/PERF-TRACKER.md` has the audit rows; perf commits are `e79d98c`, `f95a92c`
and the P0-P2 commits before them).

Rules: confirm the cause before fixing and say in Note whether it is a **regression** from the perf
work (name the row) or **pre-existing**. Test-first where testable. Release variant only on Android.
Never drive the UI, touch the phone, or relaunch the installed host. Stage files by name; the
dispatcher commits per theme (conventional prefix + `(RemEx-62k5f)`).

Status: `todo` / `staged` / `committed <sha>` / `blocked` (reason in Note) / `needs-connor` (a
product decision; question in Note).

## A: Android widgets

| ID | Finding | Status | Files | Note |
|---|---|---|---|---|
| A1 | Remote Control widget: commands do nothing (they work in-app) | committed 1c53a58 | `app/proguard-rules.pro`, `test/.../WidgetReleaseShapeTest.kt` | **Pre-existing** (not a perf row; keep rules unchanged since b642815). R8 full mode stripped `<init>()` from every `ActionCallback` (release `usage.txt` listed it); Glance instantiates callbacks reflectively, threw, and swallowed it. Added `-keep class * implements ActionCallback { <init>(); }`; post-fix mapping keeps `<init>` on all three. Also fixes the Hardware Info refresh button (same cause). |
| A2 | Remote Control widget: buttons cut off at the bottom at minimum height | committed 1c53a58 | `widget/RemoteControlWidget.kt`, `widget/WidgetGridMath.kt`, `test/.../WidgetGridMathTest.kt` | **Pre-existing**. Button height floor (36dp) + row padding exceeded the 40dp-min content area. Now `LazyVerticalGrid` (scrolls), 4dp frame under 64dp, button height from `WidgetGridMath.buttonHeight` so one whole row always fits. |
| A3 | App Launcher widget: tapping an app does not launch it (works in-app) | committed 1c53a58 | as A1 | **Pre-existing**, same root cause as A1 (`LaunchAppCallback.<init>` stripped). P3-12 change was not involved. |
| A4 | App Launcher widget: too many apps for the size get hidden, no scroll | committed 1c53a58 | `widget/AppLauncherWidget.kt`, `widget/WidgetGridMath.kt` | **Pre-existing** (`take(maxItems)`). Now a scrolling `LazyVerticalGrid` of every selected app; icons bounded to 224px so the larger RemoteViews payload stays under the bitmap ceiling. P3-12 lazy decode kept. |
| A5 | Hardware Info widget: cards don't reflow, get clipped at bottom/sides with many sensors | committed 1c53a58 | `widget/HardwareInfoWidget.kt` | **Pre-existing**. Compact mode was one `Row` running off the sides; card mode fixed rows. Both now a `LazyVerticalGrid` (1-4 card cols / 1-5 compact cols by width), grid takes remaining height via `defaultWeight()`. |
| A6 | Hardware Info widget: sensors beyond the size disappear, no scroll | committed 1c53a58 | as A5 | **Pre-existing** (`take(maxItems)`). Grid scrolls; all selected sensors reachable. |

| A7 | Review: launcher icon cap 224px barely below the host's 256px, and there's no limit on how many apps are selected, so ~35 apps exceed the widget bitmap budget ("Can't load widget") | committed 1c53a58 | `widget/AppLauncherWidget.kt`, `widget/WidgetGridMath.kt`; test `WidgetGridMathTest` | **Pre-existing exposure made reachable by A4** (A4 renders every selected app). Icons now decode to the largest cell (56dp) × `displayMetrics.density` (112px at xhdpi, not 224), one bitmap per app shared by every size `SizeMode.Exact` composes. Hard guard: `iconsWithinBudget` admits icons in display order up to 40% of the launcher ceiling (screen w×h×4×1.5); the rest draw the letter tile, so no selection count can fail the widget. 35 apps on 720x1600 xhdpi: 1.76 MB vs 2.76 MB icon share (was 7.02 MB vs 6.9 MB whole ceiling). |
| A8 | Review: Remote Control / App Launcher taps only toast "not connected" when the app is backgrounded or killed; ReconnectGate only lets a **hardware** widget reconnect in the background | committed 1c53a58 | `RemexClientManager.kt` (`startOneShotConnect`), `widget/WidgetConnect.kt` (new), `widget/RemoteControlWidget.kt`, `widget/AppLauncherWidget.kt`; test `WidgetConnectWaitTest` (new) | **Pre-existing behaviour, deliberately kept by P0-11** (not a regression). Tap with no connection → `initialize` + ONE `connect(null, isAutoConnect = true)` to the saved PC (pinned trust only, never pairs, no `pairingRequired`), then waits ≤5 s for `isAuthenticated` (not `isConnected`, RemEx-0vpw5) inside the callback, then sends as before; failure toasts "not connected". `ReconnectGate` unchanged: no heartbeat for these widgets. An already-open connection is used without waiting, as before. No new strings (reuses `widget_toast_connecting`). |

## B: Personalization

| ID | Finding | Status | Files | Note |
|---|---|---|---|---|
| B1 | Android: live preview doesn't update for vibrancy/contrast sliders, during or after drag | staged | `ui/theme/Theme.kt`, `ui/screens/PersonalizationScreen.kt`; test `CustomPaletteSchemeTest` (new) | **Pre-existing, not P3-11.** MainActivity's single call site passes chroma/contrast and RemExTheme's `remember` keys include both, so the applied theme did update (after the 500 ms save debounce). The preview did not: `MiniCardPreview` took only the seed HEX and rebuilt `colorSchemeFromSeed(seed, isSystemInDarkTheme())` - no chroma, no contrast, no variant, no app theme mode - unchanged since at least b642815 (2026-06, before any perf commit). Now RemExTheme's scheme selection is one function, `rememberPaletteColorScheme` (+ `customPaletteScheme`, `isDarkThemeFor`), and the preview paints from the screen's live slider state through it, so it moves during the drag and always equals the applied theme. RemExTheme behaviour unchanged (same branches, same fallback seed). |
| B2 | Android: Color Studio "Default" is a fixed blue, not the phone's system (dynamic) colors | staged | same as B1 | **Pre-existing; the applied theme is NOT hardcoded.** No blue literal exists in the Android tree (only amber `BrandSeed` and the purple custom-parse fallback). "Default" + Dynamic color on paints `dynamicDark/LightColorScheme(context)` (minSdk 34, so no <31 path exists); "Default" + Dynamic off paints the amber brand scheme. What was missing is a Default preview at all - the card only existed inside the Custom editor. It now shows for both modes from the same function the theme uses. **Device re-check:** if Default + Dynamic on still reads blue, the preview and app are showing what Android reports as the system accent (`system_accent1_*`); on One UI that is the stock blue unless Wallpaper & style > Color palette is applied - not an app bug. |
| B3 | Android: App Language disclaimer should be a popup/snackbar shown on opening the dropdown or picking another language, not a persistent banner | staged | `ui/screens/PersonalizationScreen.kt` | **Pre-existing** (design request; banner present since at least b642815, 2026-06). Banner removed. Picking a DIFFERENT language opens an M3 `AlertDialog` (Language icon, title "App Language", body = the existing `personalization_language_note`, Confirm/Cancel = existing `button_confirm`/`button_cancel`); Confirm applies the locale. Re-picking the current language does nothing. Dialog, not snackbar: applying a locale recreates the activity (MainActivity does not handle `locale`), which would wipe a snackbar before it could be read. No new strings. |
| B4 | PC: Mica lost after dark→light→dark flip (Mica→Acrylic→Mica does repaint). Likely P3-68 | committed b706d07 | remex.desktop/MainWindow.axaml.cs, Services/MicaBackdrop.cs; tests: MicaBackdropTests, MainWindowBackdropTests | **Regression, P3-68.** Pre-P3-68 every apply re-issued the DWM calls; after it a flip issued one `DWMWA_SYSTEMBACKDROP_TYPE=MAINWINDOW` write over an attribute already holding MAINWINDOW. The working Mica→Acrylic→Mica path differs in exactly one DWM respect: it `Clear`s (AUTO) first, a real AUTO→MAINWINDOW transition (reassigning the hint is a no-op in Avalonia 12.1.1 `WindowImpl.SetTransparencyLevelHint` when the level is unchanged, so that is not it). Fix: `MicaBackdrop.Plan` returns `Reapply` on a light/dark flip → Clear then TryApply; unchanged state still makes no DWM call. Not reproduced on device (no UI driving): mechanism inferred from the two paths' DWM calls. |

## C: File transfer

| ID | Finding | Status | Files | Note |
|---|---|---|---|---|
| C1 | Android: pause/cancel then re-download the same file often sticks at pending, even with overwrite | committed 92c3046 | `service/TransferQueueController.kt` (new), `service/FileTransferEngine.kt`; test `TransferQueueControllerTest` (new) | **Pre-existing, not a perf regression**: identical before ce68e66 (P0-8 only swapped the poll for a suspend; P4-6's writer and P1-32 pruning are not involved, and there is no TransferId dedupe: every enqueue is a new UUID). Cancel/pause only flipped the row and sent the control message; the host cancels its sender on CANCEL/PAUSE (`TransferSessionManager.cs:841-859`), so the running `runDownload` waited on `done` for the 6 h `TRANSFER_TIMEOUT_MS` while holding the one-at-a-time drain slot, and the re-download sat Queued behind it. Now each run is a Job registered atomically with its claim; cancel/pause/cancel-all cancel it (partial kept on pause), a run can no longer write over Cancelled/Paused/Queued, a cancel is not treated as a failure (no channel invalidate), and rows a dead process left mid-flight are parked as Paused (the drain never picked them). No host change. **Review fix:** "partial kept on pause" was dead weight - the PC always serves a download from offset 0 (`TransferSessionManager.cs:1564-1566`), and the offer carries only `resumeRequested` (bool), never the receiver's offset, so the host cannot seek. **Download resume needs a protocol change** (a receiver-offset field on `FileTransferOffer`); not invented here. Now `runDownload` deletes its partial however the run ends (inside the run, so a quick resume cannot race it), resume restarts cleanly from 0, the sweep no longer keeps Paused/Failed download partials (push staging still kept), KDocs corrected. Tests `TransferLocalFilesTest`. |
| C2 | Android: entering an access-denied PC folder (e.g. C:\Documents and Settings) still pushes it onto the breadcrumb; every later folder then fails and also stacks; must tap root/one-up to recover | committed 92c3046 | `ui/screens/FileTransferViewModel.kt`, `ui/screens/FileManagerLogic.kt`; test `FileManagerLogicTest` | **Pre-existing** (predates b642815). `browseRemote` committed `_remotePath` when the request was SENT, so a denied folder stayed as the path and the next tap was combined onto it. Now the path is committed only on a successful listing (`FileManagerLogic.committedBrowsePath`); on an error the view stays in the last good folder and shows the browse error. Root/volume switches still reset to "/". |
| C3 | Android: downloading a folder overwhelms the queue; every subfolder stuck on queued | committed 92c3046 | `service/FileTransferEngine.kt`, `service/TransferQueue.kt`, `ui/screens/FileTransferViewModel.kt`; tests `TransferQueueTest`, `TransferQueueControllerTest` | **Pre-existing, not a perf regression.** The host frames only bytes it reads, so an EMPTY file gets no data frame at all, not even a Final one (`StreamSenderAsync`, `TransferSessionManager.cs:1605`), and completes at once; the phone waited for a Final frame for 6 h, so the first empty file in a folder stopped the queue and everything after it stayed Queued. Its `file_transfer_complete` was also dropped (waiter registered too late). Now `TransferResumeLogic.expectsDataFrames` skips the wait and the complete waiter is registered before the offer. Also: the folder is enqueued in ONE queue write (was one full rewrite per file), and each SAF directory is listed once instead of once per `findFile`. RG:403 ack-after-write untouched. **Review fix:** the skip trusts the listing size (stale if the file grew; RG:403 "0 means unknown"), and a missing complete (`expectedSha == null`) counted as verified, committing an empty file as Done. Now `TransferResumeLogic.downloadVerified`: when the frame wait was skipped the host's complete is required and must equal our hash (for an empty file the SHA-256 of empty input, `EMPTY_SHA256_B64`); otherwise the row Fails (retryable). Streamed path unchanged. Test `TransferQueueTest`. |
| C4 | Android: no way to cancel all / multiple pending or queued transfers | committed 92c3046 | `ui/components/FileManagerQueuePanel.kt`, `ui/screens/FileTransferScreen.kt`, `ui/screens/FileTransferViewModel.kt`, `service/FileTransferEngine.kt`, 9 `strings.xml` | **Pre-existing** (missing feature). "Cancel all" in the queue header while any row is unfinished: cancels folder expansions still listing/enqueueing (their not-yet-queued empty targets are deleted), then every unfinished row including the running one; the cancel message goes only to rows the host has seen. Multi-select cancel not done (needs a selection mode in the panel; not cheap). **Review fix:** "host has seen it" was keyed on `state != Queued`, but a paused-then-resumed row is Queued while the PC holds it as Paused, so the PC kept that entry and its upload partial forever. Now a persisted `QueuedTransfer.hostKnows` (set when the drain claims the row; legacy rows default to `state != Queued`) drives `Stopped.hostKnows`. `service/TransferQueue.kt`, `TransferQueueController.kt`; test `TransferQueueControllerTest`. |
| C6 | Android: RemEx uses ~880 MB (data 182 MB+), bulk in internal filesDir | committed 92c3046 | `service/TransferLocalFiles.kt` (new), `service/FileTransferEngine.kt`, `share/ShareToPcViewModel.kt`; test `TransferLocalFilesTest` (new) | **Pre-existing**, shares C1's cause. `transfers/outgoing/<id>.part` (download partials) were deleted only on success/incomplete: never on cancel (and the hung run of C1 never reached its delete), never for a cleared Failed row, never swept. `share_staging/<uuid>/` copies were never deleted after the push; only a 7-day sweep when the share sheet reopens. Now: partial/staged copy deleted on cancel (after the run unwinds), on clear-finished, and after a push lands; startup sweep deletes anything no live row owns (10 min grace for in-flight staging). `transfers/incoming` (phone-as-host) already deletes on PC cancel + 7-day orphan sweep; unchanged. |
| C5 | PC: can't open folders while browsing the phone/remote folder tree (no drilling down) | committed b706d07 | remex.desktop/Views/FileTransferView.axaml.cs; tests: FileTransferViewFolderOpenTests (new) | **Pre-existing, not a perf regression** (view untouched by P1-P4; handler dates from the 2.x rename). Double-click was an `OnPointerPressed` override on the UserControl checking `ClickCount == 2`, which never sees a row press: `ListBoxItem` marks mouse presses Handled when it selects (Avalonia 11 `UpdateSelectionFromPointerEvent`; 12.1.1 `SelectingItemsControl.UpdateSelectionFromEvent` sets Handled unconditionally - checked by decompiling both). Now `DoubleTapped` with `handledEventsToo` on both the details list and the icon grid (Gestures raises it from the press's RouteFinished regardless of Handled). |

## D: Remote desktop

| ID | Finding | Status | Files | Note |
|---|---|---|---|---|
| D1 | Android: two-finger scroll is unreliable; often zooms instead | staged | `ui/screens/TwoFingerGestureClassifier.kt` (new), `ui/screens/RemoteDesktopScreen.kt`; test `TwoFingerGestureClassifierTest` (new) | **Pre-existing, not a perf regression.** P3-15 (e79d98c) only replaced the pressed-pointer `filter` with an index loop, same order/positions; P1-17 only throttles the scroll send. The classifier dates from b642815: it locked the intent on ONE frame's deltas in raw px, pinch checked first (`distDelta > 5f` -> pinch, else `move > 3f` -> scroll). Two fingers dragged together never hold a constant span, and 5 px is under 2 dp at 3x, so fast scrolls locked as zoom. Now `TwoFingerGestureClassifier.classify` compares against the baseline taken when the second finger lands, waits for a 12 dp slop (span change or centre travel), then picks pinch only if span change > 1.25x centre travel (anchored-finger pinch scores 2); locked until lift. Scroll/pan/pinch application, the P1-17 throttle + flush, and the zoom/pan `Modifier.layout` path are untouched. |
| D2 | Display 1: cursor shape doesn't always change (I-beam, inverted, hand); display 2 does | blocked | remex.agent/Handlers/RemoteDesktopHandler.cs, Services/ScreenCapture/WindowsScreenCaptureService.cs, DxgiDesktopCapture.cs (Debug diagnostics only) | **Pre-existing, not a perf regression** (P3-44 scratch does not alias snapshots; P3-34 cache is equivalent to the old lookup). Asymmetry found in code: `DxgiDesktopCapture` is bound to ONE output for the process (never retargeted), so on that display (normally the primary = display 1) the shape comes from DXGI pointer-shape updates polled with `AcquireNextFrame(0)` on hCursor change / 10 Hz, while WGC serves the frames; every other display renders hCursor via GDI. Also: when DXGI is unavailable the stale cached shape is returned and GDI is never tried. Exact failing step unconfirmed - blocked on a repro. Capture: set `Logging__LogLevel__Remex.Agent=Debug` for the host, stream display 1, hover link/text/resize edge; grep the host log for `Cursor handle changed` - the line gives source, frameBackend, activeMonitor, dxgiOutput, dxgiAvailable and dxgiRefresh (timeout / frame-without-shape / shape-decode-failed / unavailable-cached / lost). Likely fix then: prefer GDI shape when DXGI is not the frame source (P3-35's proposal) or fall back to GDI when DXGI returns no new shape. |
| D3 | Adaptive scale (P3-38) not findable: Android never sends `DesktopConfig.AdaptiveScale`, host defaults it off | staged | `ui/screens/RemoteDesktopViewModel.kt`, `ui/screens/RemoteDesktopScreen.kt`, `data/SettingsManager.kt`, 9 `strings.xml`; test `DesktopStreamSettingsJsonTest` (new) | **Pre-existing, not a perf regression**: `buildConfigJson` omitted the field on purpose (comment: each host scale step resizes the stream and rebuilds the phone's decoder surface); P3-38 only made the host controller survive encoder rebuilds. Connor 2026-09-25 approved the toggle. Switch row (label + supporting text) in the stream section of the RD settings sheet, below the Custom sliders, default OFF, persisted as `desktop_adaptive_scale` with the other RD prefs, carried in `RemoteDesktopConfigState.adaptiveScale`. Sent as `"adaptiveScale": <bool>` (always explicit) via `putDesktopStreamSettings`, key = core `DesktopConfig` `[JsonPropertyName("adaptiveScale")]` (`bool?`, in `RemexJsonSerializerContext`); Android builds the JSON itself, so no core/JNI change. A flip goes through `DesktopConfigPushDebouncer.onChange()` (leading edge = sent at once, supersedes a pending slider send). Note: core's doc comment says null = "on for H.264 >= 90 fps" but the host reads `?? false` (RemoteDesktopHandler.cs:1496); moot now that Android always sends it. **Device re-check:** toggle on while streaming H.264 under GPU load; host log shows `adaptiveScale=True` and "Adaptive scale:" steps; watch for the per-step decoder rebuild hitch. |

## Log

- 2026-09-25 A1-A6 staged: widget taps were R8 stripping `ActionCallback.<init>()` (pre-existing, confirmed via release usage.txt/mapping); widgets now use scrolling `LazyVerticalGrid`. `verify.ps1 -Scope android -NoClean` PASS 1003/1004 (1 skipped); `assembleRelease` mapping keeps all three callback constructors.
- 2026-09-25 B4 staged (P3-68 regression: theme flip now Clear+TryApply), C5 staged (pre-existing: DoubleTapped with handledEventsToo), D2 blocked (pre-existing; Debug diagnostics added, needs a display-1 log). `verify.ps1 -Scope dotnet -NoClean` PASS 5475/5476 (1 skipped).
- 2026-09-25 C1-C4 + C6 staged, all pre-existing: a stopped transfer (cancel/pause) and an empty file both held the one-at-a-time drain slot for the 6 h ceiling; cancel/pause now cancel the run (`TransferQueueController`), empty files skip the frame wait; browse path commits only on success; "Cancel all" added; orphaned partials/staged copies deleted + startup sweep. No host change. `verify.ps1 -Scope android -NoClean` PASS 1028/1029 (1 skipped), log `.ralph/fix/verify-C.log`.
- 2026-09-25 A7 + A8 staged: launcher icons sized from cell dp × density with a hard bitmap-bytes guard (letter tile past 40% of the screen-derived ceiling); Remote Control / App Launcher taps make one bounded (5 s) connect to the pinned PC and wait for the reconnect-proof ack before sending, ReconnectGate untouched. `verify.ps1 -Scope android -NoClean` PASS 1038/1039 (1 skipped), log `.ralph/fix/verify-A2.log`.
- 2026-09-25 C1/C3/C4 review fixes staged: download partials no longer kept on pause (host always restarts downloads at 0; download resume needs a protocol change), skipped-frame-wait downloads require the host's matching complete, Cancel all reaches paused-then-resumed rows (`hostKnows`). Android only, no wire change. `verify.ps1 -Scope android -NoClean` PASS 1045/1046 (1 skipped), log `.ralph/fix/verify-C2.log`.
- 2026-09-25 B1-B3 + D1 + D3 staged, all pre-existing: Color Studio preview ignored chroma/contrast (now shares RemExTheme's scheme function and shows for Default too); language note is a confirm dialog on picking a different language; two-finger scroll/pinch decided against the landing baseline past a 12 dp slop; adaptive-scale switch (default off) sends `adaptiveScale`. Android only, no core change. `verify.ps1 -Scope android -NoClean` PASS 1059/1060 (1 skipped), lintRelease passed; 2 new translation warnings are PC (remex.desktop), not these rows. Log `.ralph/fix/verify-BD.log`.
