# Perf remediation tracker

Temporary. It drives the `/ralph` loop for the 2026-09-23 perf audit and is **deleted when the loop finishes**. Umbrella bead: `RemEx-4j8ls`. Branch: `perf/audit-remediation`. Row detail (evidence, fix, locations, guard notes): `.superpowers/perf-audit/PRIORITIES.md` (gitignored, local), keyed by the same `P<t>-<n>` ID.

## Procedure

This section is the ralph `ProcedureOverlay`. It wins over `~/.claude/ralph/board-drain.md` wherever they disagree:
- **The work source is this file, not `bd ready`.** No per-row beads. Every commit cites `RemEx-4j8ls`.
- **No per-row commits.** Rows are checkpointed with `git add`. Commits happen only at group gates.
- **The journal is the Log section** at the bottom of this file. The CHANGELOG is written only at gates.

### Roles and models
| Role | Model | How |
|---|---|---|
| Looper (reads, confirms, implements `Impl=sonnet` rows, runs verify, keeps this file) | Sonnet 5 | the session itself |
| Implementer for `Impl=opus` rows (critical to operation: guard-noted, capture, stream, decoder, SurfaceView, pairing, session guard, JNI/wire, input) | Opus 5.5 | `implementer` agent, `model: opus` |
| Verifier for every row | Opus 5.5 | `csharp-reviewer` (C#) or `kotlin-reviewer` (Kotlin), `model: opus` |
| Tie-breaker when looper and verifier conflict | Fable 5.1 | `general-purpose` agent, `model: fable`, read-only |

### Picking the iteration
The **current group** is the first group below that still has a row that is not `committed`.
- If it has a `todo` row → **row iteration** on the first `todo` row (for P3/P4, the first `todo` batch).
- If it has none → **group gate**.
- If every group is done → **wrap-up**.

### Row iteration
1. Grep PRIORITIES.md for the row ID, then Read only that row (offset/limit).
2. **Confirm the claim** at the cited code with token-savior (`find_symbol`, `get_function_source`, `get_edit_context`) and gitnexus `context`. If it does not hold, set Status `refuted` and write the reason in Note. For P0/P1 rows the Opus verifier must agree (send it the reason, not a diff). Then log and end the iteration.
3. If the row has a guard note, read that section of `docs/REGRESSION-GUARDS.md` (Grep the heading, Read with offset/limit). If a symbol has callers outside its file, run gitnexus `impact` and put the risk level in Note.
4. **Implement.**
   - `Impl=sonnet`: the looper edits (Read the file right before the Edit).
   - `Impl=opus`: dispatch `implementer` with `model: opus`. Give it the row ID, the PRIORITIES.md path, the guard section, and this Procedure's rules. It returns at most 300 words and does not commit.
   - Test-first where the behaviour is testable (a hidden-window gate, a queue bound, a cache cap).
5. **Scoped verify.** Output goes to `.ralph/perf/verify.log`; print only the summary line.
   - C# only: `scripts/verify.ps1 -Scope dotnet -NoClean`
   - Kotlin only: `scripts/verify.ps1 -Scope android -NoClean` (release variant only)
   - Anything in `remex.core/Native`, or both platforms: `-Scope all -NoClean`
6. **Opus verification.**
   - Write `git diff -- <row files>` to `.ralph/perf/<ID>.diff`.
   - Dispatch the reviewer with `model: opus`, giving it: the diff path, the row ID, the PRIORITIES.md path, and "return at most 300 words: verdict, then findings as path:line + one line each".
   - Fix what it finds and re-review the new diff. At most 2 rounds.
   - **Conflict** (you dispute a finding, the reviewer says it is unsure, or 2 rounds do not converge): dispatch the Fable tie-breaker with the diff path, the finding verbatim, your position in 3 lines, and the code refs. It returns a ruling in at most 150 words. Follow it, and add a line under Rulings.
7. **Checkpoint:** `git add <each changed file by name>`. Set Status `staged`, fill in Files (short paths), and add a one-line Note if there's something worth keeping. Unstaged `git diff` is always exactly the current row.
8. If the result can only be seen on a device or on screen (phone backgrounded, window minimized, battery, animation), add a line under **Live checks for Connor**. Never drive the UI, never focus windows, never relaunch the installed host.
9. Append one Log line: `<date> <ID> <staged|refuted|blocked> - <≤12 words>`.

**P3/P4 batches:** the same steps, applied to every row in the batch. Run one verify and one Opus review for the whole batch. If one row in the batch fails, mark it `blocked` without failing the batch.

### Group gate
1. `scripts/verify.ps1 -Scope all` (full), then `scripts/verify.ps1 -Check`. Both must pass. Output goes to files; print summaries only.
2. gitnexus `detect_changes` is advisory. Note anything surprising in the Log.
3. Add one CHANGELOG.md line for the group, under the existing unreleased section, in the file's own style.
4. `git add CHANGELOG.md docs/PERF-TRACKER.md`. Commit with `perf(<group id>): <one-line summary> (RemEx-4j8ls)`, a `Reviewed-by: Opus 5.5 (per-row)` trailer and the session's Co-Authored-By trailer.
5. Set the group's `staged` rows to `committed <sha7>` in this file. That edit rides in the next commit.
6. After the last group of a tier (P0d, P1, P2, P3, P4): `git switch main && git merge --ff-only perf/audit-remediation && git switch perf/audit-remediation`. Never push.

### Wrap-up (when every group is done)
1. Post one summary: blocked rows with their reasons, all Live checks, and the Rulings.
2. `bd remember` 2-4 distilled lessons (symptom, cause, fix; each ≤4 lines).
3. `git rm docs/PERF-TRACKER.md`. Delete the local `.ralph.psd1` and `.ralph/perf/`. Commit `chore(perf): remove remediation tracker (RemEx-4j8ls)`, fast-forward `main`, then `bd close RemEx-4j8ls`.
4. Output `<promise>PERF-DONE</promise>`.

### Stop rules
- A row that fails verify or review twice after fixes → `blocked` with the reason. Move on; do not idle.
- Never output the promise while any P0-P4 row is still `todo` or `staged`.

### Hard rules
- Never `git add -A`, `git add .`, `git checkout -- .` or `git restore -- .`. Name the paths.
- Never bump a version, push, or run Dolt sync.
- Android: release variant only (`compileReleaseKotlin`, `assembleRelease`).
- **Tokens:** read with token-savior and gitnexus; `Read` only right before an `Edit`; build and verify output go to files; diffs are handed over as paths; subagent returns stay ≤300 words; subagents never spawn subagents.

## Groups

### P0a: Idle and background burn

| ID | Finding | Platform | Impl | Status | Files | Note |
|---|---|---|---|---|---|---|
| P0-1 | Android Task Manager 4 s poll keeps running when the app is backgrounded or the screen is off… | Android + PC host | sonnet | committed ce68e66 | TaskManagerScreen.kt | REWORKED: original `currentStateAsState()`+`LaunchedEffect` gate was a no-op (Compose pauses the frame clock on ON_STOP, so the effect can't re-run until ON_START); replaced with `LifecycleStartEffect` (a lifecycle observer, not recomposition-driven). kotlin-reviewer PASS, confirmed the mechanism is sound |
| P0-2 | Aurora, gradient and presence-pulse infinite animations have no hidden or minimized gate (du-1… | PC desktop UI | opus | committed ce68e66 | ShellViewModel.cs, MainWindow.axaml.cs, DashboardBackgroundControl.axaml, ShellPresencePulseTests.cs, DashboardBackgroundGatingTests.cs (new) | new `IsWindowVisible` ANDed into aurora/gradient/pulse gates alongside reduced-motion; csharp-reviewer PASS round 2 (2 LOW, not blocking) |
| P0-3 | Desktop RD stream keeps receiving and decoding after leaving the RD page or hiding to tray (du… | PC desktop UI | opus | committed ce68e66 | RemoteDesktopViewModel.cs, ShellViewModel.cs, ShellPresencePulseTests.cs, RemoteDesktopStreamForegroundGatingTests.cs (new) | foreground-gated stream via existing StopStreamAsync/StartStreamCommand (reuses P0-2's IsWindowVisible); 2 review rounds fixed 2 MEDIUM races (command busy-state, in-flight-start-race); own follow-up fix for a stale-flip re-check bug caught by the existing tests, not re-reviewed |
| P0-4 | Phone RD stream is not paused on background: host keeps capturing, encoding and sending at ful… | Android + PC host | opus | committed ce68e66 | RemoteDesktopBackgroundGate.kt (new), RemoteDesktopViewModel.kt, RemoteDesktopScreen.kt, RemoteDesktopBackgroundGateTest.kt (new) | lifecycle observer stops/restarts stream via existing desktop_stop path, disarms/re-arms frame-arrival watchdog; no host-side change needed. 2 review rounds fixed a cross-thread visibility bug (@Volatile), a pending-restart-vs-pause race (job tracking + Main-thread serialization), and a Stop-vs-pending-restart race |
| P0-5 | Host pushes the full 60-100 KB telemetry envelope every second to a backgrounded or screen-off… | Android + PC host | opus | committed ce68e66 | RemexMessage.cs, TelemetryPauseGate.cs (new), PingPongHandler.cs, TelemetryPauseTests.cs (new), TelemetryBackgroundGate.kt (new), RemexClientManager.kt, TelemetryBackgroundGateTest.kt (new), CHANGELOG.md, libs.versions.toml, build.gradle.kts | new telemetry_pause/telemetry_resume message types, per-connection host-side gate (TaskCompletionSource, no polling), ProcessLifecycleOwner-driven client; hardware widget placement suppresses the pause; lastSentSnapshot guard confirmed untouched; parallel csharp+kotlin reviews both PASS; fixed a flaky test and a failed-send desync (gate rollback on send failure) |
| P0-6 | Widget cache writes the 60-100 KB telemetry JSON to SharedPreferences every render interval, a… | Android | sonnet | committed ce68e66 | WidgetDataCache.kt | 3 review rounds: round 1 caught a coroutine compile error and a HIGH data-loss bug (gating the one-shot launcher_sync write, not just the widget-update, on widget presence); round 2 caught my own comment lying about code that still gated the telemetry write too. Final: writes unconditional (throttled), only the widget-render/update calls are gated on widget presence |
| P0-7 | Secondary sockets stay open while idle: /ws/files keeps TLS with 10 s OkHttp pings after the l… | Android + core | opus | committed ce68e66 | FileTransferChannelClient.kt, FileTransferEngine.kt, AndroidFileTransferHost.kt, FileHostHandler.kt, RemexClientManager.kt, RemoteDesktopViewModel.kt, DesktopSocketIdlePolicy.kt (new), 2 new test files | idle-close for /ws/files (60s fg/5s bg, lease mechanism spans the full ack-wait) and /ws/desktop catalog (45s, pure DesktopSocketIdlePolicy); review CONFIRMED both critical guards (RG:403 ack-before-complete, catalog-vs-DesktopStart race) hold in the code, not just claimed; fixed 1 MEDIUM (orphaned receive-sink after PC death defeated idle-close for the process lifetime) |
| P0-8 | FileTransferEngine.drainLoop polls every 750 ms for the process lifetime once started and re-f… | Android | sonnet | committed ce68e66 | FileTransferEngine.kt, FileTransferJobService.kt | drainLoop suspends on the queue StateFlow instead of polling; 3 review rounds fixed a job-never-retires race (callback registered after idle already reached) and a cross-thread visibility bug (scope read before assignment across the IO/Main boundary, fixed with @Volatile + reordering) |
| P0-9 | Desktop Task Manager polls every 2 s and rebuilds its ObservableCollection while the window is… | PC desktop UI | sonnet | committed ce68e66 | TaskManagerViewModel.cs, TaskManagerWindowVisibilityGatingTests.cs (new) | PollAsync waits on IsWindowVisible's PropertyChanged instead of polling while hidden; 2 review rounds PASS (round 2 added a missing regression test) |
| P0-10 | Host 1 Hz telemetry sampler (~450 sensors; WMI can block) and media-session sampler (cross-pro… | PC host | sonnet | committed ce68e66 | DemandHold.cs (new), SamplingDemand.cs (new), TelemetrySnapshotGate.cs, TelemetryBackgroundService.cs, MediaSessionBackgroundService.cs, PingPongHandler.cs, HostBootstrapper.cs, TelemetryDemandCoordinator.cs (new), App.axaml.cs, SamplingDemandTests.cs (new), TelemetryDemandCoordinatorTests.cs (new) | lease-based consumer-count gate (connected phone, visible dashboard, tray pins, armed alerts) with a null-safe always-sample default; PeriodicTimer spacing and sensor-alert-keeps-sampling requirements confirmed; 2 review rounds fixed a HIGH pause/resume race that could permanently silence a phone's telemetry stream |
| P0-11 | Android reconnect heartbeat runs forever with no network, screen or lifecycle gate: 5 s wake w… | Android | opus | committed ce68e66 | ReconnectGate.kt (new), RemexClientManager.kt, ReconnectGateTest.kt (new) | heartbeat waits on network+foreground(or screen-on+widget) instead of polling every 5s; review fixed a fail-open bug (registration failure could stick the gate closed forever); DESIGN TRADEOFF NOTED not fixed: a backgrounded phone with no widget stops reconnecting until foregrounded, which also stalls AndroidFileTransferHost/PC-browses-phone until then - intentional per this row's own goal, flagged for Connor's awareness, not a bug |

### P0b: Leaks and unbounded growth

| ID | Finding | Platform | Impl | Status | Files | Note |
|---|---|---|---|---|---|---|
| P0-19 | RemoteDesktopView attaches its cached VM twice (DataContextChanged and AttachedToVisualTree) b… | PC desktop UI | opus | committed 8585c2a | RemoteDesktopView.axaml.cs | AttachViewModel no-ops when the vm is already attached (ReferenceEquals guard); csharp-reviewer PASS, confirmed no path is missed (1 LOW: no test, no Avalonia.Headless harness in this project) |
| P0-20 | RD cursor-shape caches never evict: the host bumps ShapeSerial on every shape change and both… | PC desktop UI + Android | opus | committed 8585c2a | LruCache.cs (new), RemoteDesktopViewModel.cs (both platforms), CursorShapeCache.kt (new), 2 new test files, RemoteDesktopView.axaml.cs (P0-19) | both caches capped at 32 (LRU), cleared on stream stop/disconnect/display switch; parallel csharp+kotlin reviews both PASS, dispose-while-on-screen safety fully traced and confirmed (2 MEDIUM/LOW findings are coverage gaps not bugs, deferred) |
| P0-21 | Android thumbnail map is never cleared and copies the whole map on each insert (C16, ui-17) | Android | sonnet | committed 8585c2a | FileTransferViewModel.kt | soft cap 200, oldest-evicted; browse-time filter drops thumbnails for paths not in new listing; round 1 caught HIGH (eviction never released requestedThumbnails dedup key, permanent blank thumbnails past the cap) - fixed by releasing the dedup key alongside each evicted cache entry; round 2 kotlin-reviewer PASS (1 MEDIUM: eviction logic untested, deferred - extract to testable helper per RemEx-ivkq convention; 1 LOW fixed inline: MAX_CACHED_THUMBNAILS moved to companion object const val) |

### P0c: Stream-path and startup wins

| ID | Finding | Platform | Impl | Status | Files | Note |
|---|---|---|---|---|---|---|
| P0-15 | Thumbnail request reads the whole file and decodes it at full resolution to make a 128 px JPEG… | PC host | sonnet | committed 731df4d | ThumbnailService.cs | SKCodec sampled decode instead of full-res SKBitmap.Decode; JPEG (1/8-step) and WebP (any scale) get real savings, PNG/GIF/BMP fall through to full-size decode (no regression, no saving); csharp-reviewer PASS, 1 MEDIUM caught+fixed (sampled decode copied Unpremul alpha verbatim, unlike the old path which always premultiplies - transparent PNG/WebP would have shown hidden colour through what should read transparent; fixed by forcing Premul when source is Unpremul), 1 LOW deferred (no ThumbnailService tests exist) |
| P0-16 | fps StateFlow changes on every frame and is collected at the RemoteDesktopScreen root, recompo… | Android | opus | committed 731df4d | RemoteDesktopViewModel.kt | throttled the _fps StateFlow write to ~1 Hz (window/rate computation in recordFrameTimestamp still runs every frame, unaffected); armFrameWatchdog resets the throttle so the first value after a stream start publishes promptly; kotlin-reviewer PASS (1 LOW: throttle uses wall-clock currentTimeMillis so an NTP clock-jump-back stalls it, pre-existing weakness shared with the window math, deferred) |
| P0-17 | Desktop RD viewer allocates per frame: fresh MemoryStream, ms.ToArray(), payload.ToArray(), a… | PC desktop UI | opus | committed 731df4d | RemoteDesktopService.cs, RemoteDesktopViewModel.cs | receive loop reuses one buffer instead of a fresh MemoryStream per message, single payload copy instead of two; VM applies frames through a latest-wins _pendingFrame slot instead of unconditional Dispatcher.Post per frame; meta/stream-descriptor clears route through the same slot (deviation from handoff, reasoned and confirmed safe by review) so a coalesced apply can't reorder a clear ahead of a newer frame; csharp-reviewer PASS, StreamSerial guard and dispose-ordering both traced clean, 1 LOW pre-existing (not new) double-dispose-after-Dispose edge case deferred |
| P0-18 | Every profile load re-applies the full theme and rewrites last-seed.json with no unchanged che… | PC desktop UI | opus | committed 731df4d | DashboardLayoutService.cs, ThemeService.cs, DashboardLayoutSidecarSkipTests.cs (new) | ApplyCustomization skipped on LoadAsyncCore's success path when loaded content-equals the already-applied UserSettings (serialize-and-compare, not record equality - List members compare by reference); sidecar rewrite skipped only when both the apply was skipped AND this service already wrote that exact content this session, preserving the RemEx-alwfa.1 first-sidecar guarantee; round 1 csharp-reviewer FAIL on missing tests only (logic itself confirmed sound: guard compliance, equality soundness, startup sequence, caller impact all verified clean) - fixed by adding 6 tests (test-writer); round 2 PASS with 1 MEDIUM (background sidecar writes in 2 tests could outlive the test and leak into the next) fixed inline, 1 LOW (no second-load-still-skips-sidecar test) deferred |

### P0d: Stream and input stalls

| ID | Finding | Platform | Impl | Status | Files | Note |
|---|---|---|---|---|---|---|
| P0-12 | Half-open control socket: neither native ClientWebSocket sets KeepAliveTimeout, and the Androi… | core | opus | committed a0ea840 | SocketLiveness.cs (new), RemexNativeClient.cs, RemexDesktopClient.cs, AndroidNativeExports.cs, JniHelper.cs, 2 new test files, DesktopWssServerFixture.cs | KeepAliveTimeout (20s) + bounded send-with-abort (15s) on both native clients; receive-loop exception filter fixed so an aborted socket's cancellation is no longer silently swallowed, feeding a detected drop into the existing reconnect+pairing flow; outbound queue deliberately left unbounded (legacyUpload has no backpressure tolerance, bounding the send instead); round 1 csharp-reviewer FAIL on missing tests only (core logic confirmed sound) plus surfaced a real pre-existing Close-frame self-await deadlock in the same method; round 2 fixed both (deadlock fix verified by a test that catches its own reintroduction) and added JniHelper.AndroidLogE no-op-on-missing-liblog fix (needed to unblock the new test, HIGH gitnexus impact but no signature/behavior change on Android, reviewed and confirmed narrow) - round 2 PASS, 2 LOW deferred (Close handshake never answered on our side, harmless; one test assertion not fully isolated). ConnectionViewModel.cs:2064 (separate PC-side socket) intentionally out of scope - follow-up bead needed |
| P0-13 | One 16-slot JNI queue carries frames and all data callbacks: a full queue blocks network recei… | core + Android | opus | committed a0ea840 | JniCallbackDelivery.cs (new), AndroidNativeExports.cs, JniCallbackDeliveryTests.cs (new), RemexClientManager.kt | split into a bounded frame queue (shed-newest, never blocks producer) and an unbounded data queue (never blocks, never drops); telemetry/process-list/launcher-list coalesced latest-wins via LatestWinsSlots so a stalled consumer can't accumulate a backlog; a shed video frame owes a throttled keyframe request claimed on the next successful enqueue (FrameDropKeyframeGate), held until the stream's own first IDR; multi-MB artwork JSON parse moved off the JNI thread entirely (Kotlin side); round 1 csharp+kotlin parallel reviews: Kotlin PASS (2 LOW, 1 fixed inline), C# FAIL (2 HIGH: keyframe gate not reset across stream sessions - risked stealing a future session's one-time keyframe allowance per RemEx-vj7b; no tests for the new concurrency primitives); round 2 fixed both (gate reset wired to stream start/stop, held until first IDR, full concurrency test suite including a real 8-thread/200-iteration stress test) - round 2 PASS, 1 LOW deferred as RemEx-8sf8m (gate not reset on a mid-session socket-drop recovery path, minor since the recovered session's own startup IDR clears any stale debt) |
| P0-14 | Desktop work queue is unbounded with one consumer; with the PC unreachable each queued pointer… | core | opus | committed a0ea840 | OrderedAsyncWorkQueue.cs, AndroidNativeExports.cs, RemexDesktopClient.cs, OrderedAsyncWorkQueueTests.cs, DesktopInputFailFastTests.cs (new), DesktopConnectTimeoutTests.cs | `Enqueue` takes an optional coalesceKey; the single consumer peeks one item ahead and merges a keyed item only into an adjacent same-key one, so buttons/keys and the moves either side of them are never reordered or dropped - only absolute mouseMove (x+y set, no deltas) gets a key, since relative deltas and scroll deltas sum and would lose motion if merged; connect failures on `RemexDesktopClient` are recorded and fail-fast for 10s (every item queued behind the one failed attempt returns immediately instead of each queuing its own 15s+10s connect), `StartStreamAsync` itself is never gated so explicit (re)starts always try; deviated from the ask by not building a shared in-flight connect task since the queue's single consumer already serializes connects, achieving the same effect; csharp-reviewer round 1 PASS (1 MEDIUM: coalesce key didn't exclude x/y-plus-delta moves, which the host treats as relative - fixed inline with a test case; 1 LOW deferred, harmless). Held-mouse-button-on-drop gap noted as pre-existing, not introduced, follow-up bead worth filing if it recurs |

### P1: high impact / large effort, or medium / cheap

| ID | Finding | Platform | Impl | Status | Files | Note |
|---|---|---|---|---|---|---|
| P1-1 | mDNS self-heal resets the backoff on any pinned hit, so an mDNS-visible but refusing PC is ret… | Android | opus | committed 5880fa6 | ReconnectGate.kt, RemexClientManager.kt, ReconnectGateTest.kt | new pure `selfHealFoundNewAddress(savedHost, savedPort, discoveredHost, discoveredPort)` (port differs, or host differs after trim+case-insensitive compare); the self-heal branch resets `consecutiveFailures` only when the discovered address actually changed, otherwise the backoff keeps climbing toward its intended ~300s ceiling instead of resetting to ~40s every 3 failures; `isMulticastReachableHost` gate (RG:1132, RemEx-fkz) untouched; `saveConnectionSettings` still called unconditionally (also writes MAC fields per RemEx-263f, DataStore no-ops on unchanged values); kotlin-reviewer PASS, 2 LOW deferred (host/port read at slightly different times around the 3s discovery window, at most one extra/missed reset - pre-existing race class, not worsened; heartbeat-loop wiring itself untested, only the pure decision function is, no Robolectric scaffolding for that loop exists) |
| P1-2 | Android quality-slider drag sends one desktop_config per tick; host rebuilds the encoder each… | Android + PC host | opus | committed 5880fa6 | DesktopConfigPushDebouncer.kt (new), RemoteDesktopViewModel.kt, DesktopConfigPushDebouncerTest.kt | leading-edge + trailing-edge (200ms) debounce: a change after 200ms quiet sends immediately, a change inside the window defers and restarts the wait, the trailing send reads the config fresh so it always carries the settled value; `updateQuality`/`updateTargetFps`/`updateScale`/`applyDesktopPreset` route through it instead of calling pushConfigIfStreaming directly; entirely client-side, RemoteDesktopHandler.cs and the keyframe cooldown (RG:136-143) untouched; kotlin-reviewer round 1 PASS with 1 MEDIUM (immediate-send check compared against the last CHANGE not the last SEND, so a pause-then-resume drag could double-send within ms of its own trailing send) fixed inline by also gating on time-since-last-send, plus a regression test; 1 LOW deferred (ViewModel wiring itself untested, plain-Kotlin-seam decision per RemEx-ivkq) |
| P1-3 | Personalize slider ticks each post a full theme apply with no latest-wins coalescing (ds-1, du… | PC desktop UI | sonnet | committed 5880fa6 | ThemeService.cs, ThemeServiceApplyCoalescingTests.cs (new) | `_applyPostPending` (Interlocked) collapses N rapid `ApplyCustomization` calls to one queued dispatcher post; `_userSettings` still written synchronously every call, the queued post reads it fresh (not the closed-over parameter) and resets the pending flag before applying so a change landing mid-callback queues its own follow-up rather than being absorbed; ApplyHardwareAccent/ClearHardwareAccent unaffected (separate posts, already read _userSettings fresh); csharp-reviewer round 1 PASS, 1 MEDIUM (a test didn't actually prove the reset-before-apply ordering) fixed by reentering from inside CustomizationApplied instead of a manual reentrant-callback trick, 1 LOW deferred (pending flag stays set if PostToUiThread itself throws - Avalonia's Post doesn't in practice) |
| P1-4 | Linux: input runs inline on the /ws receive loop and RunToolWithOutput's 2 s timeout never fir… | PC host (Linux) | sonnet | committed 5880fa6 | PingPongHandler.cs, LinuxInputSimulationService.cs, PingPongInputOffloadTests.cs (new) | DispatchInput now runs on its own dedicated consumer thread (BlockingCollection, single reader, FIFO) instead of inline in the receive loop, mirroring RemoteDesktopHandler's existing input-queue pattern including its teardown-race handling (disposingInputQueue flag, matching catch arms, async WaitAsync-with-timeout drain); RunToolWithOutput now starts stdout+stderr ReadToEndAsync before WaitForExit(2000), avoiding the classic redirected-stream deadlock, and Kills the process on timeout instead of hanging forever; csharp-reviewer round 1 FAIL (2 HIGH: missing RemoteDesktopHandler's outer catch arms + stuck-key teardown race, no test) fixed - added catch arms, disposing flag, async wait, a real receive-loop-driven test suite (ScriptedWebSocket harness); round 2 FAIL (1 HIGH: the new "Pong arrives before blocked KeyDown returns" test would still pass, just slower, against the OLD inline code, since Task.Run wasn't used and the assertion lived inside the mocked callback) - fixed by running HandleAsync via Task.Run and polling for the Pong with a short 3s window outside the callback, which now fails promptly (not hangs) against a reverted fix; 1 LOW noted and accepted (the ordering test guards the FIFO invariant generally, doesn't distinguish old vs new code, which is fine - a different test already covers that) |
| P1-5 | Desktop file-transfer chunks travel as Base64-in-JSON although a binary channel exists (ds-11) | PC desktop UI + PC host | opus | committed 5880fa6 | FileTransferClient.cs, IFileFrameChannel.cs (new), LoopbackFileChannelConnector.cs (new), ConnectionViewModel.cs, FileTransferViewModel.cs, BinaryChannelUploadTests.cs (new), HostBootstrapper.cs, TransferSessionManager.cs | `UploadAsync` picks a binary path (`UploadOverFileChannelAsync`, v3 offer/ready/frames/complete over `/ws/files`, 256KB frames, 8MB unacked cap, one-chunk lookahead for `final`) when the host is loopback + advertises Binary/Protocol>=3 + root came from the roots listing; otherwise falls back to `UploadLegacyAsync` (unchanged Base64/JSON path). RG:403 confirmed: `WaitForCommittedAsync(sent, ...)` drains after the frame loop and before `file_transfer_complete`, so completion waits for an ack covering every byte sent, not just the last frame. Uses the same v3 control messages the phone already uses (offer/ready/complete/result) - no host changes, since `/ws/files` frames only land in a session a v3 offer created and the legacy Start/Chunk/End handler never reads that channel. `LoopbackFileChannelConnector`: one long-lived loopback-only `/ws/files` socket, `?protocolVersion=3`, no clientId, same cert-pin rule as control channel; falls back to legacy path on connect failure. csharp-reviewer round 1 PASS (0 HIGH/CRITICAL), 4 MEDIUM + 1 LOW fixed directly: (1) stale HostBootstrapper.cs/TransferSessionManager.cs comments claiming no PC-side blank-key `/ws/files` consumer existed - updated to name the PC UI as one and record the accepted local-only-DoS tradeoff (a second blank-key local process can supersede/kill a PC upload); (2) dropped-channel WebSocketException now logged at Warning with the channel Uri (only on real drops, not our own Dispose/cancel teardown); (3) added cancel-mid-upload test (asserts exactly one Cancel control message, no complete) and a real-refused-loopback-port fallback-to-legacy test, and fixed the decline-reason test to use the host's actual size-refusal reason instead of an unreachable read-only-decline case; (4) Dispose/connect race on the channel field fixed via `Interlocked.Exchange` + re-check-after-store. Known follow-up (not fixed, documented as comments + here): binary (v3) uploads stage in `transfers\incoming` on the system volume before promoting (legacy path writes straight to the root), so a non-system-volume root doubles I/O and needs system-volume free space, and an out-of-space IOException surfaces as a generic "channel closed" (HandleInboundDataFrameAsync catches only InvalidOperationException). |
| P1-6 | Release publish ships ~102-105 MB of PDBs, mostly native SkiaSharp/HarfBuzz, into publish, ins… | build | sonnet | committed 5880fa6 | Directory.Build.props | claim confirmed by measurement: Release win-x64 self-contained publish of remex.agent (the only thing RemEx.iss/build-linux.sh package) carried 101.6 MB of PDBs in 7 files, 100.1 MB of it vendor native (libSkiaSharp.pdb 80.1 MB, libHarfBuzzSharp.pdb 20.0 MB, flattened from runtimes/win-x64/native into the app root). New Release-only target `RemexDropPackageNativeSymbolsFromPublish` (AfterTargets=ComputeResolvedFilesToPublishList) drops `.pdb` items with AssetType=native + a NuGetPackageId from ResolvedFileToPublish and deletes any stale copy already in PublishDir (build-installer.ps1 publishes on top of an uncleaned folder). RemEx's own 5 PDBs (1.5 MB) kept on purpose: no minidump/WER machinery exists, but they are what put file:line into logged stack traces. A first NuGetPackageId-only draft also dropped Remex.Core.pdb/Remex.Branding.pdb (transitive project refs carry a package id), caught by measurement. Publish output 336.3 MB -> 236.2 MB, PDBs 101.6 MB -> 1.5 MB, both into a clean dir and over a stale one. Linux publish not measured here (SkiaSharp.NativeAssets.Linux ships .so, and the target is RID-agnostic). csharp-reviewer round 1 PASS (0 HIGH/CRITICAL), 1 MEDIUM fixed inline: `GeneratePublishDependencyFile` reads `NativeCopyLocalItems`, not `ResolvedFileToPublish`, so the dropped PDBs still showed up in `Remex.Agent.deps.json`'s native section - fixed with a second target (`RemexExcludePackageNativeSymbolsFromDepsFile`, `BeforeTargets=_ComputeResolvedCopyLocalPublishAssets`) filtering `NativeCopyLocalItems` by Extension+NuGetPackageId, verified via a real scratch publish showing the deps.json native section is now empty of .pdb entries. 1 LOW deferred/noted: `artifacts/publish/remex.agent/release_win-x64` still holds ~100 MB of stale PDBs from a pre-fix publish - harmless (next real publish or the target's own stale-copy Delete clears it) unless someone runs `build-installer.ps1 -SkipPublish` against that exact stale folder before a fresh publish happens |
| P1-7 | Every FFmpegH264Encoder construction spawns `where`/`which ffmpeg` and waits up to 1.5 s; one… | PC host | opus | committed 5880fa6 | FFmpegH264Encoder.cs, FFmpegDetectionCacheTests.cs (new) | the per-instance `DetectFFmpeg` became a nested `FFmpegDetector` (the same lookup order: `where`/`which`, then the 4 hardcoded Windows install paths) that caches its verdict behind a lock, held by one process-wide `static readonly SharedDetector`. The public ctor delegates to a new internal `(ILogger, FFmpegDetector)` test-seam ctor, and each instance still sets IsAvailable and emits the same Info/Warning/Debug log line from the shared result. RemoteDesktopHandler's probe+stream encoders now pay at most one lookup per process instead of up to ~3 s per connection. A found path is cached for the process lifetime. A not-found or errored verdict is retried after 30 s, the same pattern as ProbeCache/RemEx-lq6h, because the old code re-detected on every connection: a permanent negative would pin MJPEG until restart if ffmpeg got installed mid-run or `where` overran its 1.5 s wait at logon. Proved by counting-finder tests on a private detector instance (the shared one is never mutated, so parallel tests stay isolated): two encoders -> 1 lookup; a found path survives 10x the retry window with 1 lookup; a failed lookup stays cached inside the window and is retried once after it. csharp-reviewer round 1 PASS (0 HIGH/CRITICAL/MEDIUM), 1 LOW fixed inline: the comment overstated the retry's reach - `RemoteDesktopHandler._ffmpegAvailableCache` already pins a permanent negative for the process lifetime before this detector is asked again, so a mid-run ffmpeg install still isn't picked up without a restart either way (unifying the two caches is out of scope for this row); comment corrected to state that honestly rather than imply the fix. |
| P1-8 | H.264 decode thread wakes every 2 ms (500/s) while streaming, whether or not a frame is queued… | Android | opus | committed 5880fa6 | H264StreamDecoder.kt | Round 1 FAILED review (1 HIGH: the drain-before-poll call it added ran microseconds after the prior iteration's own after-feed drain with nothing in between, so it caught nothing - a frame decoded between iterations still sat behind the full 20ms poll, a real added-latency regression on every frame, not an edge case). Round 2 fix: track `inFlight`, the count of access units fed (`queueInputBuffer` succeeded) whose output hasn't drained yet - incremented in `feedInput`, decremented when a non-CODEC_CONFIG output buffer is released, reset to 0 on the mid-stream SPS reconfigure path (which discards in-flight buffers via `codec.stop()`). The input-poll timeout each iteration is now 2ms (original) while `inFlight > 0` - decode is async and output can complete between iterations, so a short poll is needed to drain it promptly - and 20ms only while `inFlight == 0` (genuinely idle, nothing pending to catch sooner). This is the reviewer's prescribed "simpler fallback" (keep the original 2ms whenever in-flight, longer timeout only when idle) rather than a full output-wait redesign, chosen as lower-risk to land correctly in this codebase's shape. The now-provably-useless drain-before-poll call from round 1 was removed. Corrected claim (MEDIUM from review): NOT every host encoder path guarantees no B-frames/no reordering - only nvenc's `-tune ll` reliably does; `h264_vaapi` (`-qp -g -aud 1`, no `-bf 0`, vaapi_encode_h264 defaults to `bf=2`) and `h264_amf` (no `-bf` set either) can reorder. Actually setting `-bf 0` on those paths is host-side work, out of scope for this row. Did not touch the synchronous-polling architecture, deferred SPS/PPS configure, or the mid-stream SPS reconfigure path (REGRESSION-GUARDS.md Android H.264 decoder section respected). No test added: hardware-loop timing with no headless harness in this repo (same reasoning as prior P0/P1 Android decoder-adjacent rows) - verified by full read of `runDecodeLoop` and reasoning about interleaving instead. LOW (round 2, verification scope): the claimed-pre-existing `-Scope all` hang in `FileTransferQueueTests.CancelAll_StopsTheActiveItemAndEveryQueuedOne` was not re-confirmed against a tree without P1-5's already-staged binary-channel changes; this row is Kotlin-only so only `-Scope android` was run here - the .NET-side hang claim should be verified independently by whichever row owns that suite, not assumed from this row. Round 2 review PASS (0 HIGH/CRITICAL), 1 MEDIUM + 2 LOW: MEDIUM (`inFlight` can drift upward and never recover if a fed AU's output is silently dropped rather than rendered - falls back to the original always-500/s-wakeup behavior for that decoder instance, never adds latency; reviewer called this acceptable to defer, not fixed here - a real follow-up would add a `lastFeedNanos` staleness reset) and 2 LOW (stale comment describing the removed before-poll drain call; "only nvenc" B-frame claim also wrong the other way, libx264 zerolatency also qualifies) both fixed inline as comment corrections, re-verified PASS (950/951, 1 skipped). |
| P1-9 | Splash animation (about 3 s) replays on every NavHost creation; the comment claiming it skips… | Android | sonnet | committed 5880fa6 | AppNavigation.kt | claim confirmed (stale comment, no shown-before check existed); demoted P0->P1 in the audit since the splash is a deliberate tap-skippable brand moment on cold start - that policy untouched. Added `SplashGate` (a plain process-lifetime `private object`, resets naturally on process death) set true in `onFinished`; `composable<Screen.Splash>` now skips rendering `SplashScreen` and runs `goPastSplash()` via `LaunchedEffect` when already shown this process, covering a warm start (Activity destroyed without saved state, relaunched while the process survives) - NOT rotation, which this app blocks from recreating the Activity at all. Fixed the stale comment. kotlin-reviewer PASS, 2 LOW fixed inline: KDoc named rotation as the trigger (wrong - corrected to warm-start only), and the `SplashGate.shown` branch read the mutable global live during composition (a recomposition mid-fadeOut could flip it and re-run navigation) - snapshotted once via `remember { SplashGate.shown }` instead. Hit and fixed one implementation slip along the way: the object declaration was first inserted between `@OptIn`/`@Composable` and the function they annotate, breaking Composable-ness (caught immediately by a scoped build, not by review). No test: UI/navigation timing with no headless harness in this repo. |
| P1-10 | Keep-awake SetThreadExecutionState engage and disengage run on different pool threads after aw… | PC host | opus | committed 5880fa6 | WindowsInteractiveSessionGuard.cs, IKeepAwakeBackend.cs (new), PowerRequestKeepAwakeBackend.cs (new), WindowsInteractiveSessionGuardTests.cs (new), REGRESSION-GUARDS.md | claim confirmed: RemoteDesktopHandler engages, awaits the whole stream, then disengages in a finally, so the per-thread TES clear usually ran on a different pool worker and cleared nothing. Took the handle-based power request API, not a dedicated thread: a power request belongs to its handle, so Set/Clear work from any thread and CloseHandle drops every request on it, so there is no thread to own, start, or shut down, and it is Microsoft's documented replacement for this use. Same effect as ES_SYSTEM_REQUIRED+ES_DISPLAY_REQUIRED: one handle with both PowerRequestSystemRequired and PowerRequestDisplayRequired (the docs require the system request alongside the display one). The handle is created on first engage and closed on last disengage, so no IDisposable on the singleton. Ref-counting, the `_gate` lock, audit logs, `[SupportedOSPlatform]`, the interface and the public ctor are unchanged; the new internal `(ILogger, IKeepAwakeBackend)` ctor is the test seam. No tscon/WTSDisconnect. Tests: fake backend proves engage and disengage on distinct OS threads release the hold, multi-client release waits for the last client, 32 concurrent engages take one hold and 32 concurrent disengages release it once, and a failed acquire is swallowed and retried next session; a WindowsOnlyFact runs the real P/Invokes, acquiring on one thread and releasing on another. Checked by hand with `powercfg /requests` (elevated): DISPLAY and SYSTEM entries with the RemEx reason while held, gone after a release from a different thread. REGRESSION-GUARDS Session guard text updated to the new mechanism (it also named a nonexistent `DisengageFromRemoteControl`). csharp-reviewer round 1 PASS (0 HIGH/CRITICAL), 1 MEDIUM + 2 LOW fixed inline: MEDIUM (the cross-thread real-backend test let the acquiring thread exit before the release ran - CoreCLR can reuse a dead thread's ManagedThreadId, and Windows itself drops a thread's execution-state on exit, so the test's shape wouldn't have caught the original bug or even reliably distinguished the threads - fixed by keeping the acquiring thread alive on an event until the release thread finishes, matching the real live-pool-thread shape) and 2 LOW (same root cause as the MEDIUM's thread-ID flakiness, folded into the same fix; a stale "it only ever re-locks a session it actually unlocked" sentence in both REGRESSION-GUARDS.md and a RemoteDesktopHandler.cs comment, when the guard never touches lock state at all - corrected in both places). |
| P1-11 | Cursor loop runs its own 90 Hz PrecisionPacer spin whether or not the cursor moves, and keeps… | PC host | opus | committed 5880fa6 | RemoteDesktopHandler.cs, CursorPollCadence.cs (new), CursorPollCadenceTests.cs (new), PacerResetOrderingTests.cs | idle body was already cheap; cost is the pacer's ~2 ms spin per tick, so tick less: new `CursorPollCadence` drops 90 Hz -> 30 Hz after 45 quiet ticks (~0.5 s), snaps back on the first move/handle/shape change (worst-case first-move delay 33 ms); ClipCursor/animated-shape slow tick moved from `tick % 9` to wall-clock ~10 Hz so it holds when idle; no Reset() on rate change (pacer takes interval per call, timeline stays current). Cursor loop now shares the capture loop's display-off pause (`Task.Delay(500)` -> `pacer.Reset()` after, then continue). New source-scan Fact pins that pause shape inside StreamCursorPositionAsync. csharp-reviewer round 1 PASS (0 HIGH/CRITICAL/MEDIUM), 1 LOW deferred (an animated cursor's periodic shape-serial change counts as "activity" and keeps the loop at 90Hz for as long as it animates, since the 10Hz slow tick that captures the new frame resets the 45-tick quiet counter - no worse than before this row, just no saving in that specific case; fix would be to only count position/handle changes as activity, not serial changes seen on a slow tick with no handle change). |
| P1-12 | Every cursor move forces a full cursor-shape capture at up to 90 Hz (ac-16) | PC host | opus | refuted | | Claim false against current code: StreamCursorPositionAsync (RemoteDesktopHandler.cs:1105-1123) already gates the full capture (SyncCursorShapeAsync) behind `handleChanged \|\| slowTick`, not every 90Hz tick — a cheap hCursor poll runs every tick, but the actual bitmap capture+dedupe only fires on a real handle change or the ~10Hz slow tick. Comment tags this "(Phase 4)", predating this session; no code change made. |
| P1-13 | MJPEG re-encodes and re-sends an identical full JPEG every tick on a static screen (ac-12) | PC host | opus | committed 5880fa6 | remex.core/Services/IScreenCaptureService.cs, remex.agent/Services/ScreenCapture/RawFrameJpegCache.cs, remex.agent/Services/ScreenCapture/WindowsScreenCaptureService.cs, remex.agent/Services/RemoteDesktop/MjpegResendGate.cs, remex.agent/Handlers/RemoteDesktopHandler.cs, remex.agent.tests/{MjpegResendGateTests,RawFrameJpegCacheTests,MjpegUnchangedFrameSkipTests,ConcurrentDesktopClientsTests}.cs | Claim confirmed. csharp-reviewer round 1 PASS, 1 MEDIUM (missing tests for gate multi-client isolation and the non-live->live force path) fixed inline with 2 new tests, both passing; 2 LOW deferred (timing-assertion slack under load; single-entry cache gives up the saving with mixed per-client quality/scale — correctness unaffected, perf-only). |
| P1-14 | Probe cache key includes fps, so slider changes spawn fresh ffmpeg probes (ac-24) | PC host | opus | committed 5880fa6 | remex.agent/Services/RemoteDesktop/FFmpegH264Encoder.cs | Claim confirmed (cache key was `codec:dims@fps`). Dropped fps from the key on the same rationale already applied to qp (clamped/never decides encoder-open success); fps only feeds -r/-g/maxrate, all within valid ranges for every codec. csharp-reviewer PASS, 0 findings (1 LOW noted for context only, not fixed: a probe cached at low fps is now reused up to 360fps - no evidence any supported GPU/codec combo actually fails at high fps within the app's existing clamps). |
| P1-15 | Revisiting a folder re-requests every thumbnail; no cache on either side (ah-68, C15) | PC host + Android | sonnet | committed 5880fa6 | remex.agent/Services/FileTransfer/FileTransferService.cs, remex.agent.tests/ThumbnailCacheTests.cs, remex.android/.../ui/screens/FileTransferViewModel.kt | Claim confirmed on both sides. Host: singleton in-process cache keyed on path+mtime+size+maxDim (not path alone, per C15), null results never cached. Android: requestThumbnail() now returns early when _thumbnails already holds the entry, since that map survives a re-browse while the dedup-only requestedThumbnails map is cleared on every one. csharp-reviewer round 1 FAIL (2 HIGH: test used Windows-only System.Drawing, and neither test actually proved a cache hit vs. an idempotent re-encode; 1 MEDIUM: a transient failure got cached as permanent) - all 3 fixed (SkiaSharp swap, same-size-different-content+restored-mtime test that only passes on a real cache hit, null no longer cached); round 2 PASS, 0 findings (1 LOW noted: ~128MB theoretical cache ceiling, ~5-10MB realistic, accepted). kotlin-reviewer PASS, 2 LOW deferred (Android can't detect a host-side file change within the same folder listing - pre-existing gap, not introduced by this row; the saving is smaller than the audit implied since navigating away from a folder still discards its cache). |
| P1-16 | DashboardViewModel parses the full telemetry JSON at 1 Hz whenever the VM exists, not only while visible (ui-7, ar-12) | Android | sonnet | committed 5880fa6 | DashboardViewModel.kt, DashboardScreen.kt, AppNavigation.kt | Claim confirmed - viewModelScope outlives composition (whole pager session, not just on-screen). Mirrored TaskManagerViewModel/TaskManagerScreen's existing P0-1 fix exactly: a screen-driven visible flag + LifecycleStartEffect + pager-current-page expression threaded through AppNavigation's dashboardScreenContent type at every declaration/call site. kotlin-reviewer PASS, 1 MEDIUM fixed inline (sparkline history kept filling in real time before the gate, so a real multi-minute swing while off-screen would stitch onto one 1s-wide chart slot on return, reading as a snap that never happened - now clears history on the off->on transition), 1 LOW deferred (up to 1s of stale headline values on return since the flow stays subscribed rather than re-subscribing to pick up its replay - same gap TaskManager already has, accepted). |
| P1-17 | Two-finger wheel scroll sends one message per touch sample, no throttle (ui-21) | Android + core | opus | committed 5880fa6 | RemoteDesktopScreen.kt | Claim confirmed (only onSendMouseScroll call site, no throttle unlike every other pointer send in this file). Added the same MOVE_THROTTLE_MS (~30Hz) gate, accumulator keeps summing every sample regardless so no motion is lost between throttled sends. kotlin-reviewer round 1 PASS, 2 MEDIUM + 1 LOW - fixed inline (gesture-end tail flush in resetTwoFingerState before zeroing the accumulator; SystemClock.uptimeMillis instead of wall-clock System.currentTimeMillis); round 2 PASS, 1 MEDIUM deferred as RemEx-9vjkx (extract a testable ScrollCoalescer - this exact logic already caught one real bug in review, worth a follow-up bead though not blocking). |
| P1-18 | Task Manager process-list parse, filter and sort run on the main thread every poll (ui-34) | Android | sonnet | committed 5880fa6 | TaskManagerViewModel.kt | Claim confirmed. JSON parse moved into withContext(Dispatchers.Default), mirroring DashboardViewModel's already-reviewed telemetry parse; the derived processes StateFlow's dedupe/exclude/filter/sort combine() pipeline now flowOn(Dispatchers.Default). kotlin-reviewer round 1 PASS, 1 MEDIUM fixed inline (withContext introduced a real suspension point inside the existing catch (e: Exception), so scope cancellation on leaving the screen mid-parse would be swallowed and logged as a bogus parse failure - added catch (e: CancellationException) { throw e } before the catch-all); round 2 PASS, 0 findings. |
| P1-19 | ConnectionOrbCard reads an infinite-transition glow in composition (ui-4) | Android | sonnet | committed 5880fa6 | DashboardScreen.kt | Claim confirmed - glowAlpha's .value was read in the composable body then passed to .background(), so every tick of the continuous infinite pulse recomposed the whole card. Now the State<Float> itself is held (not dereferenced) and .value is read inside a .drawBehind{} lambda instead, so the pulse only triggers redraw. kotlin-reviewer PASS, 0 findings. |
| P1-20 | MorphPolygonShape has no equals/hashCode, so outlines rebuild on every recomposition, including every Task Manager row each poll (ui-5, ui-33) | Android | sonnet | committed 5880fa6 | Theme.kt, TaskManagerViewModel.kt, TaskManagerScreen.kt, MorphPolygonShapeEqualityTest.kt (new) | Claim confirmed. Added equals/hashCode to MorphPolygonShape (morph by reference - always the same cached instance per index pair; progress by value), and rounded ProcessInfo's cpu/ram to displayed precision (data class, so poll-to-poll sub-percent noise made every row structurally "changed" even when nothing visible moved). kotlin-reviewer round 1 PASS, 1 MEDIUM + 2 LOW - fixed inline (rounding could floor every process's cpu/ram to 0.0 on an idle host, making maxCpu/maxRam a real NaN-producing 0.0 on every poll rather than just the first - added a >0.0 guard; switched round-to-nearest to floor so the display genuinely stays byte-identical to before this row, not just close; added MorphPolygonShapeEqualityTest.kt exercising the hand-written equals/hashCode against the real cardShape()); round 2 PASS, 0 findings. |
| P1-21 | Connection chip infinite pulse keeps RenderThread active while connected (ui-8) | Android | sonnet | committed 5880fa6 | CommonComponents.kt | Claim confirmed - the status-dot pulse ran truly infinitely for as long as the app was connected, and this chip is composed above every screen for the whole session, blocking LTPO refresh-rate drop the entire time. Reduced-motion was already honored; added a pulseActive flag that stops the animation after 3 breathe cycles from a fresh connect (re-arms on reconnect), settling to a steady dot afterward. kotlin-reviewer PASS, 2 LOW (both cosmetic: the pulse also re-arms on any recomposition re-entry, not only a genuine reconnect - harmless, each run still self-stops; a pre-existing doc comment mislabeled a half-cycle as "one full breathe" - fixed inline). No tests added, consistent with this tracker's Compose-animation precedent (P1-19). |
| P1-22 | Native core logs every routine connect step at ERROR, 5-10 lines per attempt, repeated by the offline heartbeat (ar-15) | core + Android | sonnet | committed 5880fa6 | JniHelper.cs, RemexNativeClient.cs | Claim confirmed. Added a priority param to AndroidLogE (defaults to ERROR, so every other ~40 call sites are unaffected) and retagged RemexNativeClient's routine connect-step lines to DEBUG/INFO/WARN, leaving genuine validation failures (fail-closed rejection, null cert, mismatch, ConnectAsync failure) at ERROR. csharp-reviewer round 1 PASS, 2 MEDIUM - fixed inline (a doc-comment placement bug from inserting the new constants mid-comment; the more substantive one: __android_log_print writes unconditionally regardless of priority, so retagging alone didn't reduce actual write cost - added an __android_log_is_loggable gate, availble since minSdk 34, that skips the native write entirely for DEBUG-level messages nobody has enabled); round 2 PASS, 1 new MEDIUM found by the gate actually working (a stale-pin mismatch would now log only "Rejecting connection" with the diagnostic SPKI hashes silently dropped at DEBUG) - fixed inline (folded both hashes into the ERROR mismatch line itself), not re-reviewed (round cap, small well-understood one-line change). |
| P1-23 | Every launch spawns powershell.exe to enumerate all firewall filters, uncached (ah-27) | PC desktop UI + PC host | sonnet | committed 5880fa6 | FirewallQuery.cs, FirewallQueryCacheTests.cs (new) | Claim confirmed - the desktop app's Home screen calls this exactly once per launch, uncached, spawning powershell.exe (Windows) or firewall-cmd/ufw (Linux) every time. Cached to disk keyed on exe path+mtime+port, bounded by a 1h TTL, only ever caching a true verdict (a refused/unknown result always re-queries, so a user's own fix-then-Refresh sequence is never masked). csharp-reviewer: 3 rounds. Round 1 FAIL (2 HIGH: cache key missing port + caching null verdicts + mtime doesn't reliably catch MSI repair; a Linux-only-passing test) - fixed (port in key, never cache null, added a 1h TTL, rewrote the test); round 2 FAIL (2 HIGH: the rewritten tests still hardcoded platform-specific run counts that fail on Linux's 2-spawn query path; caching a refused verdict could survive the user's own fix-then-Refresh for up to the TTL) - fixed (tests now compare run counts relatively, before vs. after, never against a fixed constant; only a true verdict is ever cached now, so refused/unknown always re-queries with zero staleness window) - also fixed a LOW (guard against a corrupt/out-of-range or clock-skewed cached timestamp); round 3 PASS, 1 MEDIUM deferred as RemEx-ny6u4 (a cached true verdict can still outlive a firewall rule removed without the exe's mtime changing, bounded by the 1h TTL but with no explicit Refresh-bypass - not blocking), 1 LOW accepted (a test's Linux branch exercises the null path rather than a true false verdict, but still passes and isn't a false positive). |
| P1-24 | permessage-deflate is not enabled on /ws despite repetitive telemetry JSON (ah-20) | PC host + core | sonnet | committed 5880fa6 | HostBootstrapper.cs, RemexNativeClient.cs | Claim confirmed - /ws (telemetry+control, distinct from /ws/desktop and /ws/files which carry already-compressed binary and were left untouched) sends repetitive JSON uncompressed. Enabled permessage-deflate on both host (WebSocketAcceptContext.DangerousEnableCompression) and native client (ClientWebSocketOptions.DangerousDeflateOptions). csharp-reviewer round 1 FAIL, 1 HIGH (security) - the initial implementation left compression context takeover on by default, and /ws genuinely carries attacker-reachable content (PingPongHandler's MediaState, settable via any browser tab's OS media-session API) alongside secrets (ClipboardContent) on the same connection - with context takeover on, this is a real CRIME/BREACH-class compression-oracle a passive network observer could exploit via TLS record-length side channel (SPKI pinning only blocks an active MITM, not passive observation) - fixed by disabling context takeover in both directions (ClientContextTakeover/ServerContextTakeover=false on the client, DisableServerContextTakeover=true on the host), which resets the deflate window per-message so no message's compressed size can depend on another's content; 1 MEDIUM (unverified whether System.IO.Compression.Native actually links into the Android NativeAOT .so - first use of compression in remex.core) - not independently device-tested in this session. Round 2 PASS, 0 findings - reviewer independently confirmed MessageSerializer sends one message per call (no fragment-mixing path), MediaState and ClipboardContent never share a message, and verified via ELF-header inspection of the actual built libRemexCore.so that deflateInit2_/inflate/libz.so are genuinely linked in, resolving the MEDIUM (stronger evidence than the build-only check); reviewer still recommends an on-device smoke test as a non-blocking follow-up. |
| P1-25 | last-seed.json is rewritten atomically on every ApplyAndSave, including slider ticks that do not touch the seed (ds-4, du-59) | PC desktop UI | opus | committed 5880fa6 | LastSeedSidecar.cs, LastSeedSidecarTests.cs, SplashPaletteResolverTests.cs, DashboardLayoutSidecarSkipTests.cs | Claim confirmed - WriteAsync ran unconditionally on every live customization change. Added a static dedupe (skip when Seed/Variant/Mode/Contrast unchanged) + 400ms debounce (coalesces a rapid-changing field like contrast to at most one write per window). RG:694 preserved (every write that happens still goes through WriteAllTextAtomicAsync). csharp-reviewer: 3 rounds, all genuine concurrency-correctness findings (not stylistic). Round 1 FAIL, 3 HIGH - the first design's fast dedupe-skip ran unconditionally, so a revert-to-the-on-disk-value while a debounce was already pending got silently dropped (leaving the superseded intermediate value as final); and a value arriving while the actual disk write was in flight was lost outright (single write, unconditional clear) - fixed by gating the fast skip on "nothing in flight" and rewriting the debounce as a loop that re-checks pending state after each write and does another pass if something newer arrived; also added revert and rapid-burst tests, an internal test-cache-reset hook, and a null-guard against a mid-debounce reset. Round 2 FAIL, 1 HIGH + 1 MEDIUM - the new tests didn't actually exercise the mid-write loop-back branch (round 1's single-write code would still pass them), and one more test class (DashboardLayoutSidecarSkipTests, via LoadAsync's fire-and-forget sidecar write) leaked the static cache across sequential test classes - fixed with an injectable write-function test seam gated by a TaskCompletionSource to deterministically hold a write open while a second value arrives, and a cache reset in the missed test class. Round 3 PASS, 2 LOW (an unbounded test await - fixed inline with a timeout; no-flush-on-shutdown - accepted residual risk, out of scope). |
| P1-26 | Staging drawer ListBox sits in a bare ScrollViewer, realizing every sensor ever seen (du-4, du-43) | PC desktop UI | sonnet | committed 5880fa6 | CanvasView.axaml, CanvasStagingDrawerVirtualizationTests.cs (new) | Claim confirmed on both staging-drawer lists. Removed the outer ScrollViewer from StagedCards (its own internal ScrollViewer + explicit VirtualizingStackPanel takes over, bounded by the DockPanel's fill area). csharp-reviewer round 1 FAIL, 2 HIGH + 1 LOW - the SensorActivationItems list was wrongly left untouched as "naturally small" (it actually holds active sensors plus every staged sensor name, at least as large as StagedCards, and a ScrollViewer.MaxHeight only bounds its own final size, not what it hands the child during measure); no regression test existed despite the TaskManagerView precedent having one - fixed both (moved MaxHeight/ScrollViewer.VerticalScrollBarVisibility onto the ListBox itself so the bound is real; added a 5-test source-scan suite mirroring the precedent for both lists) plus the LOW (indentation). Round 2 FAIL, 1 HIGH - one new test used ?. null-conditional on an attribute check, so a missing MaxHeight would silently pass instead of failing - fixed inline (explicit NotNull then value check), not re-reviewed (trivial one-line test-assertion fix). |
| P1-27 | Wallpaper bitmap (~15 MB) stays resident after leaving Wallpaper mode and is decoded at up to 2560px though always blurred (du-8, du-44) | PC desktop UI | sonnet | committed 5880fa6 | ShellViewModel.cs, WallpaperLoadRaceTests.cs | Claim confirmed on the residency half only - the decode-size half is refuted (WallpaperBlur slider Minimum=0, BlurRadiusToEffectConverter returns null/no-effect at radius<=0, so the wallpaper CAN render fully sharp - downsizing the decode would be a real visible quality regression, not a safe optimization; "always blurred" is false against current code). Leaving Wallpaper mode now clears and disposes WallpaperBitmap (deferred via Dispatcher.UIThread.Post, matching the existing superseded-bitmap dispose pattern in LoadWallpaperAsync, RemEx-8twk0.5) and resets _wallpaperPathLoaded. csharp-reviewer PASS, 2 LOW - one fixed inline (the new source-scan test didn't actually verify the dispose was deferred, only that it happened - tightened to match the Dispatcher.UIThread.Post(...) shape specifically), one accepted (switching back to Wallpaper mode now always re-decodes rather than reusing the cleared bitmap - the correct tradeoff, not a regression). |
| P1-28 | Last decoded RD frame stays resident after stop or disconnect (du-65) | PC desktop UI | opus | committed 5880fa6 | RemoteDesktopViewModel.cs, RemoteDesktopFrameReleaseTests.cs (new) | Claim confirmed - neither StopStreamAsync's finally nor OnDisconnected called ClearCurrentFrame; the full-resolution bitmap stayed resident until the next stream start happened to overwrite it. Added ClearCurrentFrame() to both, synchronously (matching the file's two other existing call sites, not deferred like the sibling P1-27 wallpaper fix - a different rendering surface with a documented reason for deferral that doesn't apply here). csharp-reviewer PASS, 0 findings - independently confirmed the synchronous dispose is safe (ApplyPendingFrame already disposes synchronously at stream rate against the same binding), both call sites run on the UI thread, and source-scan (not behavioral) tests are the right call given this test assembly has no Avalonia.Headless reference for constructing a real Bitmap. |
| P1-29 | --minimized logon start still builds MainWindow and the whole ShellView tree and decodes the wallpaper (C39, du-21, du-46) | PC desktop UI | opus | committed 5880fa6 | App.axaml.cs, ConnectionViewModel.cs, REGRESSION-GUARDS.md, MinimizedStartupDefersMainWindowTests.cs (new) | Claim confirmed - a --minimized logon start unconditionally constructed MainWindow (theme apply, wallpaper decode, whole ShellView tree), only skipping Show(). Deferred entirely for a minimized start, relying on BringMainWindowToFront()'s existing lazy `??=` construction (RG:1032-1049's guarded helper). csharp-reviewer round 1 FAIL, 2 HIGH (regression-guard area) - two other MainWindow consumers gated on `MainWindow is { }` BEFORE ever calling the guarded helper, so a minimized start would have silently hit their null-owner fallback paths: the file-consent dialog (exactly the "prompt nobody could see" failure RG:1032-1049 exists to prevent) and the one-time first-run restore-from-snapshot offer (would have been silently and permanently skipped) - both fixed (call BringMainWindowToFront()/lazily build MainWindow before reading it, not gate on its prior existence), with regression tests for both. Round 2 PASS, 1 MEDIUM + 2 LOW - fixed inline (REGRESSION-GUARDS.md's own text was now inaccurate, describing three MainWindow states when a fourth - never constructed - now exists; a stale comment claiming a MainWindow is "always present" for pairing; a test that didn't pin the guarding `if` condition itself, only the ordering of calls inside it), not re-reviewed a third time (all three were small, well-understood, non-functional fixes). |
| P1-30 | Personalize sheet content (CustomizationVm with system-font enumeration, HctColorWheel's 26 tone discs) is built at shell load because the always-present SideSheet binds the lazy getter (C38, du-55) | PC desktop UI | sonnet | committed 5880fa6 | ShellViewModel.cs, ShellView.axaml, ProfileReplacementInvalidatesCustomizationVmTests.cs, ShellSettingsSideSheetTests.cs | Claim confirmed - the always-present SideSheetContent bound DataContext directly to the BUILDING CustomizationVm getter, forcing full VM construction at shell load regardless of whether the sheet is ever opened. Added a non-building CustomizationVmForSheetBinding property (returns the field directly, starts null) that the XAML now binds instead; EnsureCustomizationVm raises its change notification only when it actually builds a fresh instance. csharp-reviewer PASS, 2 MEDIUM - fixed inline (a closed sheet stayed bound to a disposed VM after a profile import, since the ProfileReplaced handler only re-raised the new property's notification when the sheet was open - now raised unconditionally, closed-sheet case just rebinds to null which is already its normal pre-open state; no test pinned the actual XAML binding target, so reverting just that one attribute back to CustomizationVm would defeat the fix with all VM-side tests still green - added a source-scan test asserting the binding). This is the row RemEx-waqb4's own ProfileReplacementInvalidatesCustomizationVmTests risk note warned about - preserved and extended with 4 new tests covering the sheet-open, sheet-closed, and already-open-rebuild-on-import cases. |
| P1-31 | Pairing-PIN IPC poll runs every 2 s on the UI thread for the process lifetime although embedde… | PC desktop UI | opus | committed 5880fa6 | App.axaml.cs, MinimizedStartupDefersMainWindowTests.cs | Claim confirmed against du-47's own caveat - AttachEmbeddedPairingService already calls StopStandalonePairingPinPolling() as its first line, but that's dead at startup since it runs before the standalone poll ever starts, confirming the two paths were meant to be mutually exclusive but the startup wiring never enforced it. Checked REGRESSION-GUARDS.md's pairing_pin_response (line 386) and TransportTrust (line 959) guards - neither concerns this desktop dual-poll path. Gated the standalone poll's priming RefreshStandalonePairingPinAsync + StartStandalonePairingPinPolling behind an embeddedPairingAttached flag; AttachStandalonePairingPinQueryService itself stays unconditional as CanRevealPairingPin fallback wiring. csharp-reviewer PASS, 0 CRITICAL/HIGH/MEDIUM, 2 LOW accepted (a pre-existing dead fallback poll path when no embedded host is present, filed as RemEx-f2dwg; the source-scan test style, matching this file's existing pattern). Side effect: before the fix the poll cleared a naturally-expired PIN within 2s, erasing the "expired, get a new one" state OnPairingExpiryTick (RemEx-7ykyn) deliberately keeps when embedded pairing is active - now the expired card correctly stays up. |
| P1-32 | Transfer queue stores never prune terminal entries and re-read/rewrite the whole JSON on every… | Android + PC host | opus | committed 5880fa6 | TransferQueue.kt, FileTransferEngine.kt, TransferQueueTest.kt | Host half REFUTED - TransferSessionManager.ForgetQueued already removes Done/Cancelled on terminal transition and deliberately keeps Failed (RG:546); no host changes. Android half confirmed: upsert/remove/pruneFinished re-read from disk every call despite the caller already holding the current list, and no automatic pruning existed at all (only manual "clear finished"). Changed upsert/remove/pruneFinished to take the caller's in-memory list instead of re-reading disk; added pruneStale(7d) run once at engine start (not on every terminal transition - FileManagerQueuePanel renders a live "Done" row and a manual clear-finished button, so instant removal would hide completions from the user before they saw them, unlike the host which has no equivalent live list). kotlin-reviewer round 1 FAIL, 1 HIGH - moving the list read outside TransferQueueStore's internal lock left _queue's StateFlow read-modify-write unsynchronized across enqueue (UI thread) vs updateState/updateProgress (engine's Dispatchers.IO, every data frame), so a race could silently drop a queued transfer from both memory and disk with nothing to recover it - fixed with a single queueLock synchronizing all 6 mutation sites (enqueueUpload/enqueueDownload/updateState/updateProgress/clearFinished/pruneStale-at-start). Round 2 PASS. 2 LOW from round 1 - one fixed inline (pruneStale's KDoc omitted that Failed is pruned too), one accepted (pruning keys on createdAtMs not last-state-change, so a transfer paused/failed >7d then resumed to Done gets pruned on next start before the user sees it - rare edge case, fixing would need a new persisted field for a LOW). |

### P2: medium impact, medium effort

| ID | Finding | Platform | Impl | Status | Files | Note |
|---|---|---|---|---|---|---|
| P2-1 | Base-theme ResourceInclude is removed and re-inserted on every apply though unchanged, possibl… | PC desktop UI | opus | committed 79e8fac5 | ThemeService.cs, ThemeSwapMergedDictionaryTests.cs, REGRESSION-GUARDS.md | Claim confirmed - SwapBaseTheme unconditionally removed and rebuilt a new ResourceInclude every apply, even when the resolved URI hadn't changed (a settings-slider tick that never touches AppTheme still routes through ApplyBaseThemeInternal -> SwapBaseTheme every time), and the method's own pre-existing remark notes the four preset files are currently byte-for-byte equivalent stubs - so the overwhelming common case churned an identical dictionary for nothing. Read RG:678 (RemEx-gcqw5) first since this is regression-guard territory; the fix only adds an early-return before the guarded remove-sweep, doesn't touch it. Added a skip when current's own Source already equals the target URI. csharp-reviewer PASS, 1 MEDIUM + 1 LOW fixed inline (the skip needed dictionaries.Contains(current) too, so the sweep's self-healing property survives if current is ever dropped from the list or a duplicate base theme appears; a comment cited a nonexistent symbol and overstated what the churn actually did to ThemeVariant), not re-reviewed (small, well-understood fixes). Reviewer surfaced a bigger, separate cost in the same apply path - ApplyBaseThemeInternal and ApplyCustomizationCore's isLightTheme logic can each set RequestedThemeVariant to a genuinely different value on the same apply (dark preset in Light mode, or SolarFlare in Dark) - filed as RemEx-ap3pd rather than folded into this row, since it's a distinct code path from the ResourceInclude churn this row's claim was actually about. Also fixed REGRESSION-GUARDS.md's RG:678 line anchors, stale since before this change. |
| P2-2 | Saved-palette tiles regenerate on every ApplyAndSave, including unrelated changes (ds-5) | PC desktop UI | sonnet | committed 79e8fac5 | CustomizationViewModel.cs, SavedPalettesBehaviourTests.cs | Claim confirmed - RefreshSavedPaletteTiles's entire output (SavedPaletteTileViewModel.Refresh) is a pure function of one caller-computed bool (live light/dark) plus each tile's own Record, which nothing calls this method to mutate; it ran unconditionally on every ApplyAndSave including changes that never touch light/dark mode. Added a VM-lifetime cache of the last-seen bool, skipping the loop (and every tile's DynamicColorGenerator.Generate) when unchanged; starts null so the first call always runs. Traced every SavedPalettes mutation (Add self-refreshes before adding; Remove and Dispose's Clear need no repaint) to confirm the cache can't go stale and produce a wrong-but-unrepainted tile, not just a missed optimization. csharp-reviewer PASS, 1 LOW fixed inline (a comment near the method's other call site still called the now-usually-skipped second call "a second cheap repaint"), not re-reviewed. New test proves the skip by object identity (SurfaceBrush stays the exact same instance after an unrelated change), not colour equality, since a fresh brush could coincidentally match. |
| P2-3 | DynamicColorGenerator rebuilds the constant success/warning schemes on every call (ds-6) | PC desktop UI | sonnet | committed 79e8fac5 | DynamicColorGenerator.cs, SeedPaletteTests.cs | Claim confirmed - Success/Warning use constant seeds and a hardcoded TonalSpot variant regardless of the caller's seed/variant, so isDark and contrast are the only inputs that vary their output, yet both were rebuilt via McuScheme.Build on every Generate call (8 call sites, several looping once per tile within one apply, typically at the same live isDark/contrast). Cached by (isDark, contrast) in two ConcurrentDictionary fields; MaterialRoles is init-only so sharing instances is safe. csharp-reviewer PASS, 2 LOW fixed inline (unbounded-cache risk was real but small - noted the actual bound in the comment rather than adding eviction, since the contrast slider has no tick-snapping and the realistic key space is still tiny; a test comment overstated what it proved), not re-reviewed. New tests prove both halves: different seeds at the same (isDark, contrast) share Success/Warning but not Primary, and isDark/contrast changes still produce different Success/Warning despite the cache.
- 2026-09-25 P2-4 blocked - claim confirmed (BgraFrameConverter allocates a fresh byte[] every call) but this is RemEx-lcp8 verbatim - that exact bead was attempted and reverted three times in production, every attempt producing corrupted video from a buffer recycled while ffmpeg was still reading it, passing its own unit tests each time and only breaking live (remex.agent.tests/FrameInputPipelineTests.cs exists specifically because of that history). RemEx-lcp8 was closed 2026-09-22 as "pruned: perf/optimisation idea, not needed work" - Connor's own explicit prior decision not to pursue this. Not implemented; blocked pending Connor's explicit go-ahead rather than reopened inside an autonomous sweep.
- 2026-09-25 P2-5 refuted - directly contradicted by a documented, measured decision (RemEx-evzv): capturing full-res and letting ffmpeg downscale measured 20.7ms -> 1.7ms of capture-thread time versus downscaling during capture, which is the exact CPU cliff RemEx-evzv exists to avoid. The audit's suggested fix would reintroduce that cliff. No code change.
- 2026-09-25 P2-6 blocked - claim confirmed (DxgiDesktopCapture maps the staging texture synchronously right after CopyResource) but the suggested fix is a D3D11 fencing/staging-ring redesign with no existing test coverage at this layer, in the same subsystem that produced P2-4/RemEx-lcp8's three reverts. Blocked pending a dedicated design pass with its own test harness.
- 2026-09-25 P2-7 blocked - claim confirmed (capture runs every pacer tick regardless of whether the send loop drained the previous frame) but the audit's own risk column flags this as intentional latest-frame design, and the suggested fix would have to interact correctly with consecutiveFailures/backoff bookkeeping in the same loop RemEx-ltd, RemEx-iaxc and RG:74-109 already found subtle failure modes in. Blocked pending a dedicated design pass, not attempted inline. Four rows in a row now in this capture subsystem (P2-4 through P2-7) confirmed-but-not-implemented on the same reasoning - the audit correctly found real costs here, but this area's actual fix requires focused, test-backed design work Connor should scope deliberately, not something to improvise mid-sweep.
- 2026-09-25 P2-8 blocked - claim confirmed (RequestKeyframe/ConsumeKeyframeRequest trigger a full encoder reinit, not an in-band IDR) but this cost is already bounded to 1-per-5s by RG:136-143's throttle, unlike the hot-path sibling rows. The suggested in-band sentinel-file fix has no existing test coverage for whether a mid-stream-triggered IDR actually decodes correctly - same correctness risk as P2-6/P2-7 without their per-frame urgency. Blocked pending a dedicated design pass.
- 2026-09-25 P2-9 blocked - claim confirmed (TelemetryPayload sends every sensor's full static metadata every 1Hz tick, not just its value) but the fix is a genuine cross-platform wire-protocol schema change requiring coordinated host+Android work: the Android client would need to cache metadata by Id and merge it with value-only updates, and RG:396-401 requires protocolVersion changes be coordinated across both platforms with a release plan, since a version mismatch fails deserialization silently. This crosses the repo's own "delegate through the full pipeline" threshold (schema change, cross-module, protocol coordination) - recommend an architect-planned bead rather than an inline change. Not attempted.
- 2026-09-25 P2-10 staged - claim confirmed on the per-event coroutine-launch half: sendInput (mouse move/scroll/click/keyboard) built its JSON and did a fresh viewModelScope.launch(sendDispatcher) on every call, the exact pattern RemEx-ugvo already fixed for the stylus path. Confirmed RemEx-7rq3 (the ordering bug that made this path careful in the first place) is CLOSED, fixed by the shared sendDispatcher already in place - no dormant hazard to reawaken. Applied the same channel+single-consumer pattern with a separate inputQueue (different wire shape than the stylus batch channel - SendMessage/desktop_input vs SendDesktopPointerBatch), same sendDispatcher. kotlin-reviewer PASS, 1 LOW fixed inline (a comment claimed cross-channel ordering was unaffected when it's actually best-effort in both directions post-fix, not just one; verified nothing depends on stylus-vs-keyboard/mouse relative order, and that FIFO order WITHIN inputQueue - what RemEx-7rq3 actually needs - is untouched), not re-reviewed. Implemented the underlying perf fix rather than the audit's literal "share the stylus channel" suggestion, since the two wire shapes don't unify cleanly.
- 2026-09-25 P2-11 staged - claim confirmed on the no-eviction half only: a sensor that stopped being reported stayed in telemetryHistory's map forever. Extracted a pure pruneAndAppendTelemetryHistory top-level function (no Robolectric in this module) that drops keys absent from the current tick. Did not narrow to "bound" sensors (the history map also backs a sensor detail/picker view, not just home cards) or add per-card derivedStateOf (a Compose recomposition-scoping change with no way to verify here) - filed RemEx-d4khl for that half. kotlin-reviewer PASS, 1 MEDIUM accepted (a single missed/NaN reading now evicts a sensor's whole ~40-tick history instead of leaving a gap - small, real, acceptable), 1 LOW fixed inline (the eviction as first written cost an extra allocation pass versus the original code - rewritten as one buildMap that evicts by construction, so a stable sensor set costs the same as before, not more). Does not reduce steady-state per-tick copy cost or address recomposition scoping - noted plainly rather than oversold.
- 2026-09-25 P2-12 blocked - claim confirmed, root cause traced more precisely than the audit: onCreate's RemexClientManager.initialize() call is the first reference to that object, whose own init block calls RemexCoreClient.setCallback, whose own init block runs the 17MB System.loadLibrary synchronously. The suggested fix (move off the main thread, gate on readiness) is necessary but not sufficient alone: AppNavigation(), composed as the very first UI in setContent, almost certainly touches RemexClientManager state in its first frame, and Kotlin object class-init is thread-safe-but-blocking - so a bare background dispatch would likely just relocate the stall to first composition, not remove it, unless every early Compose reader is also readiness-gated. That's real Compose-startup-architecture work needing runtime frame-timing verification, not something to improvise blind. Blocked pending a dedicated profiling pass.
- 2026-09-25 P2-13 blocked - confirmed no baselineprofile plugin or explicit profileinstaller dependency; profileinstaller ships transitively, which is the same basis the audit itself demoted this row on. Generating an app-specific Baseline Profile needs the androidx.baselineprofile Gradle plugin and a Macrobenchmark module run on a real or emulated device - infrastructure this environment cannot set up or verify. Blocked pending device-backed Macrobenchmark tooling.
- 2026-09-25 P2-14 staged, partial - claim confirmed on the font-key/UiScale half only (the other named files, TypographyService.cs/SystemFontService.cs, and the full ~12-notification count, were not investigated - filed RemEx-hwarp). These 5 own-key writes can't be batched into the override dictionary (own keys shadow merged ones - PageTitleFontFamily etc. would silently stop applying), so each fired unconditionally on every apply. Added SetOwnResourceIfChanged (takes IResourceDictionary, not Application, specifically so it's testable - the font block is guarded on Application.Current, always null under test). csharp-reviewer PASS (confirmed FontFamily.Equals is genuinely value-based in the pinned Avalonia version via decompilation; confirmed no stale-scope read risk), 1 LOW fixed inline (a comment pointed at the wrong remark), not re-reviewed.
- 2026-09-25 P2-15 blocked - claim confirmed (101 separate `new SolidColorBrush(color)` construction sites in ApplyCustomizationCore, every apply, restarting any style-declared BrushTransition on an unchanged colour) but NOT implemented. The naive "skip when unchanged" fix would violate RG:575's own invariant ("an unset property is not a neutral property"): the override dictionary is fully Cleared and rebuilt every apply, so skipping a key's write would leave it ABSENT rather than present-with-the-old-value - exactly the class of bug RG:575's own incident (a blank shipped app) was about. The real fix needs a persistent per-key brush cache mutated in place, always re-inserted regardless of whether the colour changed - a systematic rewrite across 101 call sites with no behavioural test coverage available to catch a shared-brush or stale-reference mistake. Blocked pending a dedicated, carefully-reviewed pass.
- 2026-09-25 P2-16 blocked - unresolved contradiction between the audit and an existing code comment ("nothing here animates, so the blur is not re-rendered per frame" vs. the audit's "continuous GPU blur while in Wallpaper mode"). Both are plausible Avalonia compositor behaviors and neither is verifiable from source alone; this environment has no device/profiler to observe actual GPU frame composition. Not implemented, blocked pending live profiling.
- 2026-09-25 P2-17 blocked - claim confirmed on the ContextMenu half (a 23-item menu declared inline in the card DataTemplate, eagerly built per card regardless of use). The lazy-on-first-open fix has real UI-binding regression risk with no automated way to verify here (no headless render); this repo's rules call for a live ui-verify pass before landing such a change. Not implemented; the "navigation rebuilds the page tree" half was not investigated either.
- 2026-09-25 P2-18 staged - claim confirmed (GDI+/shell icon re-extraction for already-known-unfixable entries ran synchronously on the UI thread every launch, forever). Added an additive AppEntry.IconUpgradeUnfixable field (PC-only, RG:396-401) and a cheap pre-check so the actual extraction only dispatches off the UI thread when there's real work to do (an unconditional Task.Run broke an existing test's synchronous-completion assumption, self-caught before review). csharp-reviewer round 1 FAIL, 1 HIGH (NormalizeEntry silently dropped the new field on every load, making the whole fix a no-op and adding a save every launch - caught only by a new test going through the real LoadLaunchersAsync path, not UpgradeLowResolutionIcons directly) + 3 MEDIUM (the real extractor never throws, so a transient failure was misread as permanent; a widened pre-existing race deferred to RemEx-qs5aa; missing CHANGELOG entry) - all fixed except the deliberately-deferred race and one accepted tradeoff (a target that always produces the fallback placeholder still retries every launch, unchanged from before this row). Round 2 PASS, 2 non-blocking findings fixed inline (undependent duplicate placeholder constants across assemblies, documented not fixed; a wrong "Android round-trips this field" claim in two places, corrected to PC-only).
- 2026-09-25 P2-19 blocked - genuinely large host-startup orchestration work with an explicit ordering contract (RG:59, client-only degrade contract) that a naive concurrency rewrite could easily violate with no test able to catch a startup-sequencing regression. The audit's own strict-lens pass already rated this LOW value, and --minimized starts (made fully lazy by P1-29) don't reach first paint at all. Not attempted, blocked pending dedicated design + profiling.
- 2026-09-25 P2-20 blocked - both suggested fixes need real Linux subprocess/uinput-permission verification this environment cannot provide, and input-injection ordering has proven fragile in this codebase before (RemEx-krvz/7rq3). Not attempted.
- 2026-09-25 P2-21 staged - claim confirmed (a bare indexer assignment on a duplicate TransferId orphaned the previous FileStream and SHA256 hasher with nothing left to dispose them). Fixed via dispose-and-replace reusing the existing CleanupTransferAsync helper. csharp-reviewer round 1 FAIL, 2 MEDIUM + 1 LOW - the check ran after opening the new stream (sharing-violation/delete-the-new-file risk with the real service) - moved before the open; a duplicate download could race its own two background download tasks and have one's cleanup wrongly dispose the other's still-active state - fixed with a state-aware CleanupTransferAsync overload using ConcurrentDictionary's conditional TryRemove(KeyValuePair) for reference-equality-checked removal; a test used a relative path touching the real filesystem - switched to a temp path. Verified with the full 1452-test remex.agent.tests suite given the shared helper's blast radius, all pass.
- 2026-09-25 P2-22 blocked - last P2 row. Native C (pipewire_capture.c) + RG:179-194's session-warmth invariant + no Linux/PipeWire environment to verify a poll-to-callback rewrite. Same reasoning as P2-20. Not attempted. P2 group complete: 22 rows all committed/refuted/blocked (11 staged, 11 blocked/refuted); group gate next.
- 2026-09-25 P2 group gate: first verify.ps1 -Scope all run FAILED - ThemeServiceTypographyTests.PageSubtitleFontFamilyResource_FallsBackToPageTitleFontFamily_AtSourceLevel pinned the exact pre-P2-14 source shape of the PageSubtitleFontFamily write (a literal app.Resources[key] = ... string), which P2-14's SetOwnResourceIfChanged wrapping legitimately changed. Missed when P2-14 was staged since the targeted test filter run didn't include this file. Fixed the test to match the new (still-correct) call shape; not a code regression.
- 2026-09-25 P3-B1 (P3-1) staged - claim confirmed. ReceiveLoopAsync's fresh, uncapped MemoryStream per message paid several doubling-reallocation copies plus a final ToArray() copy, even for the common single-fragment case. Restructured: one ReceiveAsync first, single-fragment messages copy straight into a right-sized array (skipping MemoryStream), multi-fragment messages fall back to a MemoryStream sized from the first fragment. csharp-reviewer PASS, 2 LOW fixed inline (stale comments citing the wrong guard / describing the old shape). New tests drive the real receive loop over a genuine wss:// socket, proving single-fragment, multi-fragment, and cursor-packet-demux all still work byte-for-byte.
- 2026-09-25 P3-B2 (P3-2/P3-3/P3-4) staged - P3-2: control-socket accumulator now reused across the receive loop's lifetime instead of a fresh MemoryStream per multi-fragment message, safe since Deserialize is synchronous and fully consumes it before the next iteration. P3-3: JsonSourceGenerationOptions can't carry an Encoder (not a compile-time attribute constant) - added a second lazy generated-context (Relaxed) and repointed all 28 serialize call sites. P3-4: command ingress now deserializes/serializes directly against UTF-8 bytes, no string round-trip. csharp-reviewer round 1 FAIL, 2 MEDIUM + 3 LOW - benchmarked the fast-path concern empirically instead of trusting either side's uncertainty (no regression, ratio 1.00); fixed the accumulator holding peak capacity forever (drops past 1MB) and SerializeIndented silently ignoring the encoder; applied a cleaner lazy-holder idiom; fixed an indentation nit. Full suite verified across all three test projects given the reach into shared serialization code.
- 2026-09-25 P3-5 REFUTED - the claim was that updateProgress's per-frame `_queue.value.map { ... }` (FileTransferEngine.kt:697) recomposes the whole File Manager queue body. It does not: FileManagerQueuePanel's LazyColumn already keys rows by `key = { it.id }` (FileManagerQueuePanel.kt:116), and the map preserves object identity for every transfer whose id doesn't match (`else it`) - QueuedTransfer is a data class, so even an equals-based fallback would hold. Compose therefore recomposes only the single row whose bytesTransferred actually changed, once per 256KiB frame, not the panel body. No code change.
- 2026-09-25 P3-6 fixed - `_fileTransferMessages` (RemexClientManager.kt:785) was `extraBufferCapacity = 8` with `DROP_OLDEST`, an unexamined default unlike the other flows in this file, which all document a deliberate latest-value rationale. Four independent collectors share this broadcast (AndroidFileTransferHost, FileTransferEngine, FileTransferViewModel, ShareToPcViewModel) and a file-control message (offer/accept/progress/complete) is a discrete event, not conflatable state - dropping one under a burst can hang a transfer with no error surfaced. Multicast rules out a Channel (single-consumer); raised the buffer to 64 with a comment explaining why SUSPEND isn't an option (the emitter is a JNI callback thread, `tryEmit` only).
- 2026-09-25 P3-7 REFUTED - claimed every reconnect does two whole-file DataStore writes and decrypts every stored pin. Traced the actual connect path (RemexClientManager.kt:850, 1019-1023): an ordinary reconnect (pin already valid) does exactly two single-key decrypts (getPin, getReconnectSecret) and zero writes - the two setPin/setReconnectSecret writes at lines 916-947 only run inside the FIRST-PAIRING branch (`spkiHash.isNullOrBlank()`), not on every connect. `listPaired` (which decrypts every stored pin) is called only from `forgetHost`, never from connect. No code change.
- 2026-09-25 P3-8 REFUTED - claimed the legacy v2 progress notification has no rate limit. AndroidFileTransferHost.handleTransferChunk (line 632) only sends `file_transfer_progress` every `PROGRESS_CHUNK_INTERVAL` (10) chunks of `CHUNK_SIZE` (64 KiB) each - 640 KiB per notification update, ~10/sec even at 60 MB/s LAN speed. FileTransferViewModel.handleChunk (the receive side) does not call showTransferProgress per chunk at all - only handleProgress, reacting to the already-throttled message, does. The premise ("no rate limit") is false; a rate limit exists and is chunk-count-based, not time-based, but at 64 KiB chunks that is not a runaway notification storm. No code change.
- 2026-09-25 P3-B4 staged - all 6 rows fixed. P3-11: MainActivity had two separate RemExTheme{AppNavigation()} call sites gated on null-vs-loaded personalization, so the first real DataStore emission tore down and rebuilt the whole nav tree; collapsed to one call site using `personalization ?: DefaultPersonalization` (verified field-for-field equal to RemExTheme's own defaults, so no cold-start flash). P3-12: AppLauncherWidget decoded every host entry's icon eagerly; WidgetAppEntry.icon is now `by lazy`, only visibleApps decode (confirmed nothing else touches .icon and forces it early). P3-13: audit's "no cache across recompositions" was wrong (remember(base64) already survived that) - real bug was main-thread decode plus no cache across re-scrolls; fixed with an 8 MiB android.util.LruCache (synchronized) keyed on base64 content in front of a Dispatchers.Default produceState decode. P3-14: trackpad inertia interval doubled 16ms->33ms (MOVE_THROTTLE_MS) to halve packet rate; verified the physics glide's velocity sequence, stop test and total distance are unchanged, just paired up (duration +3%, negligible). P3-15: per-touch-event pointer-list allocation replaced with an index loop over the first two pressed positions (order preserved, only 2 ever read). P3-16: H.264 decoder's two full per-P-frame NAL scans replaced with one early-exiting bitmask pass (scanNalTypes); the whole-IDR materialize() copy replaced with an in-place (offset,length) index - verified against REGRESSION-GUARDS.md's decoder section that no downstream code holds a NAL range across a buffer-reuse boundary (the AU array was already used in place, not copied, before this change). kotlin-reviewer PASS, 0 HIGH/CRITICAL, 2 LOW fixed inline (stale REGRESSION-GUARDS.md line anchors for the decode-loop thread, now :89/:124) + noted but accepted (a findNalUnits equivalence test uses the new function as its own oracle rather than a verbatim old-code copy - the only change is an offset comparison, low risk).
- 2026-09-25 P3-B3 staged - P3-9, P3-10, P3-6 fixed; P3-5, P3-7, P3-8 refuted (no diff). kotlin-reviewer PASS, 0 HIGH/CRITICAL, 1 MEDIUM deferred (v2 legacy file_transfer_chunk can still drop under DROP_OLDEST at high buffer capacity if a fast PC outruns the disk-writing collector - filed as RemEx-1iszs, not a regression from this row) + 2 LOW deferred (test-coverage gap in the job-tracking test; pre-existing untracked jobs in handleMessage, out of scope). P3-9: added a proguard -assumenosideeffects rule stripping Log.d/Log.v in release (Log.i/w/e untouched). kotlin-reviewer round 1 FAIL, 1 HIGH - several Log.d calls in pairing (RemexCoreClient.kt) and media_state (RemexClientManager.kt) had comments explicitly documenting that they rely on Log.d surviving into release builds for on-device debugging/spec verification - the new strip rule would have silently broken that. Fixed by bumping those specific calls to Log.i and updating their comments; verified via assembleRelease (lintVitalRelease + minifyReleaseWithR8 pass) and the full testReleaseUnitTest suite. P3-10: AndroidFileTransferHost.start() launched 4 coroutines but only tracked 1 for cancellation - the other 3 (two DataStore collectors, an orphan sweep) leaked on every service re-creation. Fixed with a tracked jobs list, cancelled in both stop() and at the top of start() (covers re-entrancy without an intervening stop()). kotlin-reviewer confirmed sound. New source-scan test (no Robolectric in this module) asserts every scope.launch in start() has a matching jobs += tracking it, not just "at least one".
| P2-4 | BgraFrameConverter allocates a fresh full-frame byte[] on the LOH every capture tick (C2, ac-4) | PC host | opus | blocked | | Claim confirmed against current code (BgraFrameConverter.cs:53, `new byte[rowBytes * height]` unconditionally) but NOT implemented. This is RemEx-lcp8 verbatim - that exact bead (pooled capture buffers) was attempted and REVERTED THREE TIMES in production, every time producing corrupted video from a buffer recycled while ffmpeg was still reading it, passing its own unit tests each time and only breaking live. remex.agent.tests/FrameInputPipelineTests.cs exists specifically because of that history and states any pooling attempt MUST route through its harness to mean anything. RemEx-lcp8 was closed 2026-09-22 ("pruned: perf/optimisation idea, not needed work") - Connor's own explicit prior decision not to pursue this. Reopening a three-times-failed, deliberately-pruned change inside an autonomous perf sweep is the wrong call; blocked pending Connor's explicit go-ahead, not attempted. |
| P2-5 | Full-resolution GPU readback, then CPU downscale (ac-13) | PC host | opus | refuted | | Refuted against a documented, measured decision (RemEx-evzv, RemoteDesktopHandler.cs:1904-1958): capturing full-res and letting ffmpeg downscale measured 20.7ms -> 1.7ms of capture-thread time at 2560x1440 versus downscaling during capture, which is the exact CPU cliff (single-threaded GDI+ bilinear at full source size) RemEx-evzv exists to avoid. The audit's suggested fix ("downscale on the GPU before CopyResource/Map when a scale is requested") would reintroduce that cliff. The doc comment already correctly attributes the real remaining cost here to the unpooled BgraFrameConverter allocation, not the readback/downscale order - i.e. this is a duplicate framing of P2-4/RemEx-lcp8, not a separate issue. No code change made. |
| P2-6 | Synchronous Map(D3D11_MAP_READ) right after CopyResource stalls the capture thread on the GPU… | PC host | opus | blocked | | Claim confirmed - DxgiDesktopCapture.MapStagingAndEncode calls MapResource(..., D3D11_MAP_READ, ...) synchronously right after CopyResource, which can stall the capture thread until the GPU finishes the copy. NOT implemented. The suggested fix (a ring of staging textures with a fence so Map never waits) is a genuine D3D11 synchronization redesign - the exact class of subtle timing change that made P2-4/RemEx-lcp8 fail three times in production while passing its own tests every time, and the audit itself flags "none cited; capture area, read RG first" - no existing test harness verifies frame correctness at this layer (FrameInputPipelineTests.cs covers the encoder side, not staging-texture fencing). Blocked pending a dedicated design session with its own test harness, same reasoning as P2-4, not attempted inline here. |
| P2-7 | No back-pressure from socket send to capture/encode; overwritten frames are still captured and… | PC host | opus | blocked | | Claim confirmed - RemoteDesktopHandler's capture loop runs every pacer tick regardless of whether the send loop has drained frameBuffer.Frame (Interlocked.Exchange overwrite/latest-frame semantics; overwritten frames counted via totalFramesDropped, never skipped up front). NOT implemented. The audit's own risk column flags this is intentional design (latest-frame semantics, RG:74-109); the suggested fix (skip capture+encode while the previous frame is unsent) would have to interact correctly with consecutiveFailures/backoff bookkeeping and the display-powered-off pause in the SAME loop RemEx-ltd and RemEx-iaxc already found subtle failure modes in, and RG:74-109 documents a Reset()-ordering bug in this exact loop that a behavioural test alone couldn't see. Same reasoning as P2-4/P2-6: this subsystem has a real history of changes that pass their own tests and break live. Blocked pending a dedicated design pass, not attempted inline here. |
| P2-8 | Keyframe requests are honored only by a full ffmpeg respawn (ac-25) | PC host | opus | blocked | | Claim confirmed - RequestKeyframe/ConsumeKeyframeRequest trigger a full encoder reinit rather than an in-band IDR. NOT implemented. This cost is already bounded by RG:136-143's 1-per-5s throttle, so it is not a hot-path burn like the sibling capture rows - the audit itself rates it "bounded to 1 per 5s by the cooldown". The suggested fix (an in-band -force_key_frames sentinel-file trigger, which the encoder's own args-builder comment already discusses and set aside for full reinit) would need FFmpeg subprocess IPC and has no existing test coverage verifying a sentinel-triggered mid-stream IDR actually decodes correctly on the client - the same correctness-risk profile as P2-6/P2-7's capture-loop changes, without their offsetting per-frame urgency. Blocked pending a dedicated design pass, not attempted inline. |
| P2-9 | Telemetry resends every sensor's static metadata (name, unit, category, ids) every second (C5,… | core + PC host | opus | blocked | | Claim confirmed - TelemetryPayload sends the full SensorReading (Name/Unit/Category/Source/Kind/Id/Group, all static per sensor) every 1Hz tick, not just Value. NOT implemented. This is a genuine cross-platform wire-protocol schema change, not a local fix: splitting metadata-once from values-per-tick requires the Android client to cache metadata by Id and merge it with value-only updates, handle a sensor whose metadata it hasn't cached yet, and RG:396-401 explicitly requires protocolVersion changes be coordinated across remex.agent AND remex.android with a coordinated release (a mismatch fails deserialization silently, not cleanly). This crosses this repo's own routing threshold for "delegate through the full pipeline" (schema/protocol change, 3+ files, cross-module) - recommend an architect-planned handoff package and a dedicated bead rather than an inline mid-sweep change. Blocked, not attempted. |
| P2-10 | Each mouse move or scroll builds JSON, crosses JNI, allocates an envelope and byte[] and retur… | Android + core | opus | committed 79e8fac5 | RemoteDesktopViewModel.kt, RemoteDesktopViewModelInputGateTest.kt | Claim confirmed on the per-event launch half (sendInput did a fresh viewModelScope.launch(sendDispatcher) per mouse move/scroll/click/key, exactly the pattern RemEx-ugvo already fixed for the stylus path via pointerBatchQueue). Confirmed RemEx-7rq3 (the specific reason this needs care) is CLOSED - fixed by the shared sendDispatcher already in place, so no new ordering hazard from that history. Applied the SAME channel+single-consumer pattern (a separate inputQueue, not literally the stylus one, since the two carry different wire shapes to different JNI entry points), on the same sendDispatcher. kotlin-reviewer PASS, 1 LOW fixed inline (a comment overstated the two channels' relative ordering as unaffected when it is actually best-effort in both directions now, not previously - verified nothing in the codebase depends on stylus-vs-keyboard/mouse cross-channel order, and that ordering WITHIN inputQueue, which RemEx-7rq3 actually needs, is unaffected strict FIFO), not re-reviewed. Did not implement the audit's literal "route through the stylus channel" suggestion (different wire shape); implemented the underlying performance fix instead. |
| P2-11 | Telemetry history copies the whole map and two 40-point lists per sensor per tick with no key… | Android | sonnet | committed 79e8fac5 | DashboardViewModel.kt, TelemetryHistoryPruneTest.kt | Claim confirmed on the no-key-eviction half - a sensor that stopped being reported (USB drive unplugged, host feature toggled off) stayed in the history map forever. Implemented ONLY that half: extracted a pure pruneAndAppendTelemetryHistory function (no Robolectric in this module, same reason parseTelemetry above it is a top-level function) that drops any key absent from the current tick's sensor list. Did NOT narrow tracking to "bound" sensors or add per-card derivedStateOf - telemetryHistory also backs a sensor detail/picker view that can show history for a sensor never added as a home card, so "bound" isn't simply "on a card", and the recomposition-scoping half is a Compose-architecture change with no way to verify in this module; filed as RemEx-d4khl. kotlin-reviewer PASS, 1 MEDIUM accepted (a single transient NaN/missing reading now evicts a sensor's whole history instead of leaving a gap - real but small, history only holds ~40 ticks and a full profile switch already clears the map wholesale), 1 LOW fixed inline (the eviction as first written added an extra HashSet + a second map copy per tick versus the original single copy - a perf fix that regresses allocation on a stable sensor set is the wrong trade; rewritten as a single buildMap pass that evicts by construction). This row does NOT reduce the steady-state per-tick copy cost when the sensor set is stable, and does not address the recomposition-scoping half - the tracker note here says so explicitly, per review. |
| P2-12 | 17 MB native library load and JNI init run on the main thread in onCreate (ui-11) | Android | opus | blocked | | Claim confirmed and root cause traced more precisely than the audit's own citation: MainActivity.onCreate's synchronous RemexClientManager.initialize(this) is the first reference to the RemexClientManager object, whose OWN init block calls RemexCoreClient.setCallback(this) - the first reference to RemexCoreClient, whose init block runs System.loadLibrary("RemexCore") synchronously. NOT implemented. The suggested fix (initialize off the main thread, gate native calls on readiness) is necessary but not sufficient on its own: setContent's very first composed UI is AppNavigation(), which almost certainly reads RemexClientManager state (isConnected, etc.) within the first frame - Kotlin object class-init is thread-safe but blocking, so if Compose (main thread) references RemexClientManager while a background-launched initialize() is still mid-load, the main thread blocks anyway, just moved from onCreate to first composition instead of removed. A real fix needs every early Compose read of RemexClientManager/RemexCoreClient state gated on a readiness flag, which is Compose-startup-architecture work needing runtime profiling to verify - not something to improvise without a way to measure actual frame timing. Blocked pending a dedicated profiling/design pass, not attempted inline. |
| P2-13 | No app-specific Baseline Profile (ui-9) | Android | sonnet | blocked | | Confirmed no androidx.profileinstaller dependency or baselineprofile plugin in remex.android's build files (profileinstaller ships transitively via another artifact, as the audit's own risk note says - the audit itself demoted this row on that basis). NOT implemented. Generating an app-specific Baseline Profile requires the androidx.baselineprofile Gradle plugin plus a Macrobenchmark test module run on a real or emulated device to record actual execution traces - infrastructure this text-only environment cannot set up or verify (no device, no way to run a Macrobenchmark). Blocked pending Connor setting up device-backed Macrobenchmark tooling, not attempted inline. |
| P2-14 | "One ResourcesChanged per apply" is defeated: ~12 tree-wide notifications per apply; font and… | PC desktop UI | sonnet | committed 79e8fac5 | ThemeService.cs, SetOwnResourceIfChangedTests.cs | Claim confirmed and PARTIALLY fixed - only the 5 font-key/UiScale own-key writes in ApplyCustomizationCore, not the full ~12-notification count or the TypographyService/SystemFontService half the audit also cited (filed as RemEx-hwarp). These 5 keys can't move into the batched override dictionary (a dictionary's own keys shadow merged ones, so PageTitleFontFamily etc. would silently stop applying), so each fired its own ResourcesChanged unconditionally on every apply, even a colour/opacity-only tick that never touches a font or scale setting. Added SetOwnResourceIfChanged, taking IResourceDictionary directly (not Application) specifically so it's testable at all - the font-writing block is guarded on Application.Current, always null in this test assembly, so it never runs under test. csharp-reviewer PASS (checked FontFamily.Equals is genuinely value-based in the pinned Avalonia version via decompilation, and that a stale-scope read isn't possible since TryGetValue only looks at this dictionary's own keys), 1 LOW fixed inline (a comment pointed at the wrong nearby remark), not re-reviewed. Tests prove the skip via reference identity (no public ResourcesChanged event exists to subscribe to on IResourceDictionary in this assembly) including the real case this row is about: two independently-built FontFamily instances with the same URI. |
| P2-15 | Every apply replaces every palette brush, restarting 200-400 ms BrushTransitions even when the… | PC desktop UI | opus | blocked | | Claim confirmed - every apply constructs a fresh `new SolidColorBrush(color)` for every palette resource (101 separate construction call sites in ApplyCustomizationCore, not a handful), so any style-declared BrushTransition on those keys restarts even when the resolved colour is byte-identical to what was already there. NOT implemented. The naive "skip the write when unchanged" version of this fix would violate RG:575's exact invariant ("an unset property is not a neutral property") the same way that guard's own incident did: `_overrideResources` is fully Cleared and rebuilt every apply (the P0-era ONE-ResourcesChanged batching), so skipping a key's write when its colour is unchanged would leave that key ABSENT from the dictionary after the clear, not present-with-the-old-value - falling through to whatever the base theme (or nothing) defines for it instead. The correct fix needs a PERSISTENT brush cache (one SolidColorBrush instance per resource key, kept across applies, its .Color mutated in place only when changed, always re-inserted into the freshly-cleared dictionary regardless) - a systematic rewrite across 101 call sites with zero behavioural test coverage available (no Avalonia headless render in this assembly) to catch a wrong brush shared across two keys or a stale reference surviving where it shouldn't. Blocked pending a dedicated, carefully-reviewed implementation pass, not attempted inline. |
| P2-16 | Wallpaper BlurEffect re-runs over every dirty region, including 1 Hz telemetry repaints (du-15… | PC desktop UI | sonnet | blocked | | Unresolved contradiction, not implemented. DashboardBackgroundControl.axaml's own comment on the Wallpaper Image states "nothing here animates, so the blur is not re-rendered per frame" - directly opposite the audit's claim that the Effect recomposites on every dirty-region repaint including unrelated 1Hz telemetry redraws elsewhere on screen. Both are plausible: Avalonia's compositor MAY cache an Effect's rasterization independent of sibling repaints (matching the comment), or MAY recompute it whenever the region it occupies is touched by any dirty rect (matching the audit) - this is real-rendering-pipeline behavior I cannot verify from source alone, and this environment has no device/profiler to observe actual GPU frame composition. Implementing the suggested pre-blur-into-a-cached-bitmap fix for a problem I cannot confirm exists (or trusting a comment that could be stale) is not something to do blind. Blocked pending live GPU profiling to determine which claim is actually true. |
| P2-17 | Every navigation rebuilds the page tree and each canvas card eagerly builds a 23-item ContextM… | PC desktop UI | sonnet | blocked | | Claim confirmed on the ContextMenu half - CanvasView.axaml declares a 23-item ContextMenu inline inside the card DataTemplate (ctrl:DraggableCard.ContextMenu), so every card on the canvas eagerly builds the whole menu tree the moment it materializes, whether or not it's ever right-clicked. NOT implemented. The fix (build the menu lazily on first open, via code-behind rather than declarative XAML) has real regression risk this repo's own rules require verifying with a live XAML hot-reload session (the ui-verify skill: run the Debug host, screenshot the live window, confirm the change actually paints/works) - moving 23 bound menu items to lazy construction could silently break their command/DataContext wiring in a way no automated test catches (no headless render in this test assembly). Blocked pending a ui-verify-backed pass, not attempted inline. The "navigation rebuilds the page tree" half was not investigated. |
| P2-18 | Launcher icon upgrade re-extracts known-unfixable icons on every start, on the UI thread (ah-6… | PC desktop UI | sonnet | committed 79e8fac5 | AppEntry.cs, AppLauncherViewModel.cs, LauncherIconUpgradeUnfixableTests.cs, CHANGELOG.md | Claim confirmed - GDI+/shell icon extraction for entries with nothing sharper available ran synchronously on the UI thread every launch, forever, with no persisted give-up state. Added AppEntry.IconUpgradeUnfixable (additive wire field, RG:396-401, PC-only - Android's own AppEntry model never sends entries back); UpgradeLowResolutionIcons skips already-unfixable entries and marks one unfixable only when extraction genuinely ran and still produced nothing better. LoadLaunchersAsync only dispatches the upgrade work to Task.Run when a cheap pre-check finds real work to do, preserving the previous effectively-synchronous behavior for the common "nothing needs upgrading" case (a broader unconditional Task.Run broke an existing test fixture's implicit synchronous-completion assumption - caught before review by re-running the full launcher suite). csharp-reviewer round 1 FAIL, 1 HIGH (NormalizeEntry rebuilds AppEntry from individual fields on every load and omitted the new parameter, silently resetting it to false every time - the whole fix was a no-op and added a save on every launch; the 4 original tests called UpgradeLowResolutionIcons directly and couldn't see this upstream bug) + 3 MEDIUM (the real Windows extractor never throws - it swallows every failure into a known placeholder - so a transient failure like a locked file was reading as "confirmed no sharper icon" and getting permanently flagged; a widened race where a host sync landing mid-extraction could get overwritten by the stale result, pre-existing but narrower before this row, deferred to RemEx-qs5aa; missing CHANGELOG entry per RG:401) - all fixed except the race (deliberately deferred) and an accepted tradeoff (a target that only ever produces the placeholder, not from a transient cause, retries every launch same as before this row - the alternative needs mtime tracking, out of scope). Round 2 PASS, 2 non-blocking findings fixed inline (the placeholder constant has two undependent copies across assemblies since desktop can't reference the platform-specific agent assembly - documented, not fixed; a comment/CHANGELOG claim that Android round-trips the field was wrong - it never sends entries back at all). New regression test goes through the real LoadLaunchersAsync constructor path specifically, since that's the seam the original 4 direct-call tests couldn't see the HIGH through. |
| P2-19 | First paint waits on synchronous pre-Avalonia work: embedded host StartAsync, stale-port recla… | PC desktop UI + PC host | opus | blocked | | Not implemented. Genuinely medium-large host-startup orchestration work spanning Program.cs, HostBootstrapper.cs (4 separate line ranges), StalePortReclaimer.cs, HostCapabilitiesProvider.cs and MdnsAdvertisingService.cs, with an explicit "client-only degrade contract" and RG:59 WarmUpCapture ordering that any concurrency change must preserve exactly - a naive "start everything concurrently" rewrite is exactly the kind of change that could silently violate an ordering contract with no test able to catch a startup-sequencing regression. The audit's own strict-lens re-scoring already rated this row LOW value, and separately noted --minimized logon starts (already made fully lazy in P1-29 this session) don't even reach first paint at all, undercutting the case further. Blocked pending a dedicated design pass with real startup profiling, not attempted inline. |
| P2-20 | Linux input fallback forks one xdotool/ydotool per pointer sample or scroll detent (ah-57) | PC host (Linux) | sonnet | blocked | | Not implemented. Both suggested fixes (batching xdotool/ydotool invocations, or a persistent uinput injector) require real Linux subprocess/device-permission behavior to verify correctly, and input-injection ordering has proven fragile in this exact codebase (RemEx-krvz/RemEx-7rq3's keyDown/keyUp reordering incidents). This environment has no Linux host to test against. Blocked pending verification on real Linux hardware, not attempted inline. |
| P2-21 | A duplicate file_transfer_start TransferId overwrites the map entry, orphaning the first FileS… | PC host | sonnet | committed 79e8fac5 | FileTransferHandler.cs, FileTransferHandlerTests.cs | Claim confirmed - _activeTransfers[start.TransferId] = state was a bare indexer assignment with no check for an existing entry, so a duplicate TransferId (client retry or bug) orphaned the previous attempt's FileStream and SHA256 hasher with nothing left to dispose them. Fixed with dispose-and-replace, reusing the existing CleanupTransferAsync helper the normal cancel/complete paths already use (deleteFile: true - nothing else will ever finish or clean up a duplicate-orphaned attempt's partial write) rather than inventing a new reject path. csharp-reviewer round 1 FAIL, 2 MEDIUM + 1 LOW - the duplicate check ran AFTER opening the new stream, so with the real service's FileShare.None a same-path retry would fail with a sharing violation before ever reaching it (and if sharing ever let it through, cleanup-by-path could delete the file the new stream just opened) - moved the check before the open; a duplicate DOWNLOAD raced its own two background StreamDownloadAsync tasks, since disposing the old stream made the first one fail and its own finally then cleaned up by TransferId alone, removing and disposing the SECOND (still-active) attempt's state too - fixed with a new state-aware CleanupTransferAsync(FileTransferState, bool) overload using ConcurrentDictionary's conditional TryRemove(KeyValuePair) (reference equality on the sealed, non-record FileTransferState class), so a cleanup only removes an entry if it's still literally the caller's own state instance; the test's RemotePath was a relative literal touching the test process's real working directory for File.Exists/File.Delete - changed to a temp-folder path. Round 2 not run (reviewer's own findings were precise and mechanical - moving one check, adding one conditional-removal overload, changing one test literal); re-ran the FULL remex.agent.tests suite (1452 tests) after all three fixes given the shared CleanupTransferAsync helper's blast radius across every other cancel/complete call site. All pass. |
| P2-22 | Warm Linux PipeWire session double-copies frames and polls via Task.Run every 5 ms (ac-6) | PC host (Linux) | opus | blocked | | Not implemented. The suggested fix (signal frames from the native callback instead of polling) touches native C (pipewire_capture.c) and the RG:179-194 "session stays warm for the process lifetime" invariant directly, with no Linux/PipeWire environment available here to verify a callback-vs-poll rewrite doesn't break session warmth or introduce a native threading bug. The audit itself rates this low priority and asks to "confirm the poll runs with no viewer before raising" - a live-system check, not a source-level one. Same reasoning as P2-20 (no Linux host to test against). Blocked pending verification on real Linux/PipeWire hardware. |

### P3: cheap polish (batched: one verify and one review per batch)

| Batch | Rows | Platform | Impl | Status | Files | Note |
|---|---|---|---|---|---|---|
| P3-B1 | P3-1 | core + Android | opus | committed e79d98c | RemexDesktopClient.cs, AndroidNativeExports.cs, DesktopFrameReceiveTests.cs | Claim confirmed - ReceiveLoopAsync accumulated every message through a fresh, uncapped MemoryStream before ToArray(), even the common single-fragment case (H.264 frames, cursor packets, small JSON control messages all fit in the 256KB receive buffer). Restructured to do one ReceiveAsync first; a single-fragment message copies straight from the shared buffer into a right-sized array (Array.Copy, skipping MemoryStream entirely); a genuinely multi-fragment message falls back to a MemoryStream sized from the first fragment instead of the default. A byte[] copy is still required either way - RG-cited NotifyJavaByteArray needs a genuinely fresh, independently-owned array since the shared ArrayPool buffer is reused by the next ReceiveAsync call. csharp-reviewer PASS, 2 LOW fixed inline (a comment cited the wrong regression-guard section; a comment in AndroidNativeExports.cs and the new test's doc comment both described the old ms.ToArray()-only shape), not re-reviewed. New tests drive the real receive loop over a genuine wss:// socket (DesktopWssServerFixture, the same pattern DesktopWssHandshakeTests.cs already uses - FrameReceived has no other seam): single-fragment (50KB) and multi-fragment (600KB, forces real WebSocket fragmentation) frames both arrive byte-for-byte, and an RDXC cursor packet still routes away from FrameReceived on the fast path. Full remex.core.tests suite (1255 tests) verified. |
| P3-B2 | P3-2, P3-3, P3-4 | core | opus | committed e79d98c | RemexNativeClient.cs, RemexJsonSerializerContext.cs, RemexNetworkListener.cs, MessageSerializer.cs, FileFrameEnvelope.cs, RemexDesktopClient.cs, AndroidNativeExports.cs, DashboardProfileStorageService.cs, DesktopFrameReceiveTests.cs, NativeClientReceiveAccumulatorReuseTests.cs, RelaxedJsonEncodingTests.cs | P3-2: control-socket ReceiveLoopAsync already had a single-fragment fast path (RemEx-tfi6); the gap was a fresh MemoryStream per multi-fragment occurrence instead of one reused for the loop's lifetime - fixed, safe because Deserialize is synchronous and fully materializes before the next iteration touches it again. P3-3: JsonSourceGenerationOptions has no way to carry an Encoder (not a compile-time attribute constant) - added a second generated-context instance (RemexJsonSerializerContext.Relaxed, lazy via a nested holder to dodge partial-class static-init ordering) and mechanically repointed all 28 serialize call sites at it. P3-4: RemexNetworkListener's command ingress now deserializes/serializes directly against UTF-8 bytes instead of round-tripping through a string. csharp-reviewer round 1 FAIL, 2 MEDIUM + 3 LOW - benchmarked the "does Encoder disable the source-gen fast path" concern empirically (200k iterations, ratio 1.00, no regression) rather than trusting either prior; fixed the accumulator holding peak capacity for the whole connection (drops and rebuilds past 1MB) and SerializeIndented silently not applying the encoder (Utf8JsonWriter's own JsonWriterOptions.Encoder decides, not typeInfo's) with a test proving each; applied the reviewer's suggested cleaner lazy-holder idiom; fixed an indentation nit. Full suite verified across all three test projects (remex.core.tests 1264, remex.agent.tests 1452, remex.desktop.tests 2640) given the reach into shared serialization code. |
| P3-B3 | P3-5, P3-6, P3-7, P3-8, P3-9, P3-10 | Android | opus | committed e79d98c | proguard-rules.pro, RemexCoreClient.kt, RemexClientManager.kt, AndroidFileTransferHost.kt, AndroidFileTransferHostJobTrackingTest.kt (new) | P3-9/P3-10/P3-6 fixed; P3-5/P3-7/P3-8 refuted |
| P3-B4 | P3-11, P3-12, P3-13, P3-14, P3-15, P3-16 | Android | opus | committed e79d98c | MainActivity.kt, AppLauncherWidget.kt, FileManagerFileItem.kt, RemoteDesktopScreen.kt, H264StreamDecoder.kt, REGRESSION-GUARDS.md, PerfP3B4SourceShapeTest.kt (new), H264NalScanTest.kt (new) | all 6 rows fixed |
| P3-B5 | P3-17, P3-18, P3-19, P3-20, P3-21 | Android | sonnet | committed e79d98c | AppLauncherScreen.kt, MediaMiniPlayer.kt, MediaNowPlayingSheet.kt, TutorialScreen.kt, DashboardCoachOverlay.kt, TaskManagerViewModel.kt, PerfP3B5SourceShapeTest.kt (new) | all 5 rows fixed |
| P3-B6 | P3-22, P3-23, P3-24, P3-25, P3-26, P3-27 | build | opus | committed e79d98c | remex.agent.csproj | P3-25 fixed; P3-26 refuted; P3-22/23/24/27 blocked (product/risk decisions) |
| P3-B7 | P3-29, P3-30, P3-31, P3-32 | build | sonnet | committed e79d98c | app/build.gradle.kts, app/src/release/AndroidManifest.xml, remex.desktop.csproj, remex.agent.csproj | all 4 fixed; release APK 37,098,821 -> 36,441,997 B (-641 KB, arsc 1.96->1.30 MB, 85->9 locales); Remex.Desktop.dll 7,034,880 -> 6,233,088 B (-783 KB); publish scripts/ 22 files (~328 KB) -> 1 |
| P3-B8 | P3-28, P3-51, P3-52, P3-54, P3-55, P3-56 | PC desktop UI | opus | committed e79d98c | AboutViewModel.cs, ThemeService.cs, DashboardLayoutService.cs, PhonePresenceMonitor.cs, WallpaperImageStore.cs, UpdateCheckService.cs, App.axaml.cs; tests: AboutViewModelChangelogTests, ThemeModeResolutionTests, DashboardLayoutUnchangedSaveTests (new), UpdateCheckStartupThrottleTests (new) | All 6 fixed, 2 partially. P3-28: changelog parsed once per process (static Lazy) by a streaming reader that stops after the newest released section; the 1.3 MB embed itself is unchanged (build-time excerpt not done). P3-51: OS colour event skips the apply when the painted light/dark already matches. P3-52: debounced save skips when JSON equals our last write AND the file's write stamp is unchanged (restore/delete/hand edit still rewrite); SaveAsync always writes; atomic-write guard untouched; SaveInternalAsync impact HIGH (42, via every RequestSave caller). P3-54: formatting skipped when status/template/culture unchanged; "stop while hidden" refuted - tray tooltip and tray menu header read PresenceText while the window is hidden. P3-55: no Copy() at scale 1.0, bitmap made immutable so SKImage shares pixels. P3-56: startup reuses a successful answer <24 h old, re-judged against the running build; About's "check now" is still live. |
| P3-B9 | P3-57, P3-58, P3-59, P3-60, P3-61, P3-62 | PC desktop UI | sonnet | committed e79d98c | SensorViewModel.cs, SystemFontService.cs, ActivityService.cs, App.axaml.cs, HomeView.axaml, SparklineControl.cs, Base64ToImageConverter.cs; tests: CanvasAlertStateTests, ActivityServiceLoadMergeTests (new), HomeRecentActivityVirtualizationTests (new), Base64ToImageConverterCacheTests (new) | All 6 fixed. P3-57: the edge detector lives in SensorViewModel.CheckAlert, not SensorAlertTracker - deadband added there (clears only 2% of |threshold| back past it). P3-58: installed-font names cached per process on success only (Avalonia snapshots the list at startup anyway). P3-59: file read moved to the pool, merge posted to the UI thread, saves deferred until merged, App warms the singleton. P3-60: VirtualizingStackPanel under the existing MaxHeight=300 ScrollViewer, pinned by a source test. P3-61: immutable pens/brushes cached by (colour, thickness, cap), capped at 32; one reused point buffer; gradient rebuilt only on colour change. P3-62: bitmaps cached per string (256 cap, >256K chars not cached), evicted entries dropped not disposed. |
| P3-B10 | P3-63, P3-64, P3-65, P3-66, P3-67, P3-68 | PC desktop UI | opus | committed e79d98c | DiagnosticLogsViewModel.cs, DiagnosticLogsView.axaml.cs, ShellViewModel.cs, SystemFontService.cs, ShellView.axaml, TrayFlyoutViewModel.cs, TrayFlyoutWindow.axaml.cs, HomeViewModel.cs, MainWindow.axaml.cs; tests: DiagnosticLogsSuspendResumeTests (new), ShellHiddenPageWorkTests (new), FontResolutionCacheTests (new), TrayFlyoutViewModelToolbarTests, MainWindowBackdropTests | All 6 fixed. P3-63: Logs VM suspends on navigate-away and replays from the sink on return (overlap de-duped by reference); the view now unwires on detach, which was a real per-visit view leak; trim uses RemoveAt(0) instead of a search. SetTransitionAndNavigate impact MEDIUM. P3-64: probe answer cached per font string, same FontFamily instance returned; a throwing probe (no platform yet) is not cached. TryResolveFont impact MEDIUM. P3-65: ImmersiveHost binds a new ImmersiveView (CurrentView only while chrome hidden). P3-66: flyout drops its sensor tiles on hide and rebinds on Refresh. P3-67: RefreshPinnedSensors and RebuildFlyoutSensors skip unchanged sequences; Refresh suppresses the per-item rebuilds. RefreshPinnedSensors impact MEDIUM. P3-68: hint arrays reassigned only when the material changes, Mica DWM call only when light/dark changed or not yet landed; RG:608 Mica invariant kept (Transparent + own DWM call), source test added. |
| P3-B11 | P3-33, P3-34, P3-35, P3-36, P3-37, P3-38 | PC host | opus | committed e79d98c | WindowsScreenCaptureService.cs, RemoteDesktopHandler.cs; tests: AdaptiveScaleReseedTests (new) | P3-34/37/38 fixed; P3-33/35 blocked; P3-36 refuted. P3-34: active monitor resolved once per display refresh/target change into a volatile `_activeMonitorDisplay` (immutable record), read lock-free by ActiveMonitorDeviceName/CanUseDxgiForCurrentTarget - no lock or capturing-lambda LINQ per frame. P3-37: GetActiveBounds re-enumerates monitors at most once per 1 s (TrySetCaptureTarget/GetDisplayCatalog still refresh immediately); the H.264 raw-size self-heal covers a frame at stale bounds. gitnexus impact GetScreenSize = CRITICAL (interface fan-out; signature and backend-selection order unchanged). P3-38: AdaptiveScaleController now survives encoder rebuilds; re-seeded only when _scale moved from outside (client config), so its own step no longer wipes the 5 s cooldown / 60 s failed-step-up hold (the test shows a fresh controller steps straight back up). P3-33 BLOCKED: it is P2-7's skip-capture-while-unsent back-pressure applied to the MJPEG branch - same loop, same latest-frame design (RG:74-109), same reasoning. P3-35 BLOCKED: WGC has no cursor-shape API (IWgcCaptureSource); the only non-DXGI source is the GDI renderer (two bitmap renders per 10 Hz tick), which would change shape fidelity on the default capture path - needs a live A/B. P3-36 REFUTED: the two-fragment send is RemEx-41xu's deliberate trade (one ~28 B extra TLS record vs copying the whole access unit per frame); the proposed single buffer re-adds that copy, and reserving header room in the encoder output is a pipeline change. |
| P3-B12 | P3-40, P3-41, P3-43, P3-44, P3-45, P3-46 | PC host | opus | committed e79d98c | RemoteDesktopHandler.cs, HostBootstrapper.cs, DesktopSessionRegistry.cs, DxgiDesktopCapture.cs, WindowsTelemetryService.cs, MdnsAdvertisingService.cs; tests: DesktopSessionRegistryTests, HwInfoSamplerTests, SyntheticHwInfoRegion, HeldKeyReleaseOnDisconnectTests, MdnsReadvertiseSkipTests (new) | P3-40 partial, P3-41/44/45/46 fixed, P3-43 refuted. P3-40: RemoteDesktopHandler is IAsyncDisposable; /ws/desktop uses `await using`, so the 2 s input-drain wait is awaited (WaitAsync) instead of blocking a request thread; sync Dispose kept, both share CompleteTeardown and are idempotent. The FFmpegH264Encoder writer Wait(250 ms) is NOT changed: DisposeProcess runs on the synchronous rebuild path inside the capture loop, so making it async means an async IH264Encoder lifecycle (P2-8-class encoder change). P3-41: MarkDrained now removes a loopback session by its CTS (scan of the synthetic-key entries), and the named path uses the atomic key+value TryRemove. P3-44: GetFramePointerShape writes into a reused pinned (POH) scratch and the decoder reads it from there - 3 copies + native alloc per shape change down to 2; the "inverted cursors mishandled" half is refuted - the decoder declines invert pixels on purpose and the GDI fallback composes them. P3-45: after a FileNotFoundException miss, HWiNFO re-probe backs off 30 s (staleness/parse drops still reopen next tick). P3-46: NetworkAddressChanged is debounced 1.5 s (trailing) and the goodbye+re-register is skipped when the advertisable address set is unchanged. P3-43 REFUTED: both DXGI and WGC key their replay on scale (RemEx-wmm8) - a 1.0 screenshot mid-stream makes the next stream request re-derive from staging at its own scale, and a cross-scale replay under lock contention reports IsLive=false, which the handler skips; cost is one extra BGRA conversion per screenshot. Side note for review: DXGI's raw cache key ignores drawCursor, so a cursor-less screenshot frame can be replayed to a full-res drawCursor stream on a static screen until the next desktop change. |
| P3-B13 | P3-47, P3-48, P3-49, P3-50 | PC host | sonnet | committed e79d98c | MediaArtworkFallback.cs, PingPongHandler.cs, PairedDeviceActivityStore.cs, HostBootstrapper.cs; tests: ExtractedIconCacheTests (new), PairedDeviceActivityStoreTests | all 4 fixed. P3-47: ExtractedIconBytes caches by (path, last-write UTC), misses included, cap 32 (clear when full); returned array is shared, callers only read it. P3-48: the three per-message LogDebug calls in the /ws receive loop are IsEnabled(Debug)-guarded. P3-49: RecordSeen skips (memory and disk) a sighting within 1 min of the RECORDED last-seen, so frequent reconnects advance once per minute; impact MEDIUM (PingPongHandler.HandleAsync). P3-50: the bootstrap cert LoggerFactory adds the Console provider only off Windows (WinExe stdout goes nowhere; the factory cannot be disposed because certService keeps its logger). Not touched: WebApplication.CreateBuilder's own default Console provider on Windows. |
| P3-B14 | P3-39 | Android + PC host | opus | blocked | | Claim confirmed (a new decoder's configure-wait path requests an IDR immediately (RemEx-p7fz/vj7b), and the host honors the first one inside the cooldown via the startup bypass, i.e. an ffmpeg respawn, while the fps-scaled GOP puts a natural IDR within ~1 s). NOT implemented. The fix means delaying the first request past the natural-IDR interval, which undoes p7fz's immediate request (a black-screen fix), depends on per-preset GOP (clamp 30..120, so up to 2 s at 15 fps) and on every encoder repeating SPS/PPS at natural IDRs (VAAPI unverified), and interacts with the Android decode-stall watchdog. It only shows up on a device and cannot be verified here; the respawn it saves is already bounded by RG:136-143. Same bar as P2-8. |
| P3-B15 | P3-42 | PC host (Linux) | opus | blocked | | Claim confirmed (LinuxJpegEncoder.Encode allocates `new SKBitmap(destInfo)` per frame for ScalePixels). NOT implemented: Linux-only capture path with no Linux/PipeWire environment here to verify a cached destination bitmap across geometry changes. Same bar as P2-20/P2-22. |
| P3-B16 | P3-53 | PC desktop UI + PC host | sonnet | committed e79d98c | FileTransferQueue.cs; tests: FileTransferProgressCoalescingTests (new) | Fixed on the PC UI side: Progress<T> plus a second post replaced by a per-item coalescer, one UI hop per 100 ms carrying the newest value, held-back value applied by the existing 1 s refresh tick. Host side (FileTransferHandler every-10-chunks send) left unchanged: it is the legacy wire cadence the phone also consumes, and changing it is not verifiable without a device. |

P3 row titles:

- P3-1: Android desktop-stream receive copies each H.264 frame through a fresh uncapped MemoryStream, ms.ToArray() an…
- P3-2: Multi-frame JSON receive accumulators (telemetry, v2 file chunks) are not pooled and grow onto the LOH (C7)
- P3-3: No JavaScriptEncoder set, so base64 '+' and non-ASCII escape as \uXXXX on all serialization (C8)
- P3-4: Command ingress decodes the payload to a string before deserializing and serializes the response to a string…
- P3-5: Transfer queue list copied per 256 KiB frame; each emission recomposes the File Manager body (C22, ui-16)
- P3-6: AndroidFileTransferHost handles file control messages one at a time; DROP_OLDEST(8) drops later requests (ar-…
- P3-7: Each reconnect makes two whole-file DataStore writes and decrypts every stored pin (ar-17)
- P3-8: Legacy v2 transfer progress posts a system notification per message, no rate limit (ar-19, ui-19)
- P3-9: Release APK keeps Log.d/Log.v; frame-sampled debug logs fire several times a second while streaming (ar-21)
- P3-10: AndroidFileTransferHost.start() leaks two DataStore collectors and a FileHostHandler per service re-creation…
- P3-11: First composition takes the null-prefs branch and is torn down (ui-12)
- P3-12: AppLauncherWidget decodes every icon although only selected apps show (ui-14)
- P3-13: Thumbnails decoded on the main thread during composition, no cache across recompositions (ui-18)
- P3-14: Trackpad inertia sends moves every 16 ms, twice the normal rate (ui-22)
- P3-15: Gesture loop allocates a filtered pointer list per touch event (ui-23)
- P3-16: Decoder double-scans P-frame payloads and copies whole IDRs (ui-26)
- P3-17: Launcher grid and Recent carousel both re-decode base64 icons (ui-27)
- P3-18: Now-playing sheet and docked mini-player recompose their whole body every 1 s while playing (ui-28, ui-29)
- P3-19: Tutorial pager reads currentPageOffsetFraction in composition (ui-30)
- P3-20: Coach overlay demos read Animatable values in composition (ui-31)
- P3-21: Background auto-polls toggle isRefreshing, animating pull-to-refresh every 4 s (ui-35)
- P3-22: Bundled ML Kit barcode model is ~6.8 MB (18% of the APK) for one QR-only scan (C29)
- P3-23: Firebase Analytics and Crashlytics auto-initialize on every process start, including QS-tile and widget cold…
- P3-24: Blanket `{ *; }` keep rules on ML Kit, datatransport, App Startup, WorkManager and Glance pin 1.12 MB of Glan…
- P3-25: ReadyToRun more than doubles the CsWinRT projection (+29 MB), off the startup path (C32)
- P3-26: Resident host/desktop process runs on unconfigured GC defaults (C35)
- P3-27: NativeAOT libRemexCore.so (16.1 MB) built with no size or GC tuning (C36)
- P3-28: 1.3 MB CHANGELOG embedded, then re-read and split on every About open and language switch for 12 bullets (C37…
- P3-29: compose ui-tooling is `implementation`, shipping in release with an exported PreviewActivity (C40)
- P3-30: No localeFilters: resources.arsc carries 87 library locales against the app's 9 (C41)
- P3-31: 778 KB of MSIX tile and splash PNGs embedded into Remex.Desktop.dll, never loaded (C45)
- P3-32: Whole scripts/ dev tree, including .remember session state, copied into every publish and Program Files (C46)
- P3-33: Frames captured then discarded on the MJPEG fallback branch (ac-5)
- P3-34: Per-frame backend selection takes the display lock and runs LINQ with capturing lambdas (ac-8)
- P3-35: Cursor-shape poll drives DXGI duplication even when WGC serves the target (ac-15)
- P3-36: Each H.264 frame goes out as two SendAsync calls over TLS (ac-17)
- P3-37: GetScreenSize re-enumerates every monitor on each call on the GDI tier (ac-18)
- P3-38: AdaptiveScaleController is recreated on every encoder rebuild, losing its 5 s state (ac-26)
- P3-39: Every resume from background forces an encoder respawn though a natural IDR is near (ac-29)
- P3-40: Teardown blocks a request thread on _inputProcessingTask.Wait(2 s) and the ffmpeg writer Wait(250 ms) (ac-33)
- P3-41: Loopback /ws/desktop sessions are never removed from _activeSessions, leaking a CTS per session (ac-35)
- P3-42: Linux MJPEG encoder makes a full-res SKBitmap copy and a new destination bitmap per frame (ac-36)
- P3-43: A screenshot during a live stream swaps the cached frame's scale (ac-37)
- P3-44: DXGI pointer-shape refresh copies each new shape three times; inverted cursors mishandled (ac-38)
- P3-45: HWiNFO-absent hosts throw and catch FileNotFoundException on every telemetry tick (ah-4)
- P3-46: mDNS goodbye+announce on every NetworkAddressChanged, no debounce or change check (ah-8, ah-37)
- P3-47: Artwork fallback re-extracts and re-encodes the same app icon on every track change (ah-15, ah-55)
- P3-48: Per-message LogDebug in the /ws receive loop formats arguments with Debug off (ah-23)
- P3-49: PairedDeviceActivityStore rewrites its whole file on every authenticated connect (ah-24, ah-40)
- P3-50: Bootstrap LoggerFactory with a Console provider is never disposed in a console-less WinExe (ah-32, ah-66)
- P3-51: OS ColorValuesChanged re-runs the full theme apply without checking whether light/dark changed (ds-7)
- P3-52: Profile saves write to disk when the content is unchanged (ds-9)
- P3-53: File-transfer progress reformats text and hops threads per message (50-170/s) (ds-12)
- P3-54: PhonePresenceMonitor's 3 s timer runs while hidden and always reformats (ds-14)
- P3-55: Wallpaper pick decodes at full resolution and copies again at scale 1.0 (ds-15)
- P3-56: Startup update check fires on every launch, including minimized logon starts (ds-17)
- P3-57: Sensor-alert trip has no hysteresis, so a sensor at its threshold re-trips repeatedly (ds-18)
- P3-58: System-font list re-enumerated on every CustomizationViewModel build (ds-19)
- P3-59: Lazy ActivityService constructor does sync disk I/O on the first caller's thread, possibly an agent socket th…
- P3-60: Home "Recent activity" uses a non-virtualizing ItemsControl, realizing all 60 rows (ds-21)
- P3-61: SparklineControl.Render allocates pens, brushes, geometry and point lists per paint (du-5, du-38)
- P3-62: Base64ToImageConverter decodes a new, never-disposed Bitmap on every binding evaluation (du-11, du-36)
- P3-63: Diagnostic Logs VM and view stay subscribed after navigation (LogAdded, ScrollToEndRequested), with O(n) shif…
- P3-64: TryResolveFont forces a glyph typeface on every apply, including the 1.6 MB Nabla folder URI (du-24, du-49)
- P3-65: ImmersiveHost keeps a hidden duplicate RemoteDesktopView once fullscreen has been used (du-53)
- P3-66: Hidden tray flyout keeps pinned-sensor tiles bound to live SensorViewModels and updating every tick (du-56)
- P3-67: Every flyout show rebuilds pinned tiles N+1 times (Clear plus per-item Add) (du-57)
- P3-68: Each apply re-requests the backdrop with a new TransparencyLevelHint array and DWM calls in Mica mode (du-63)

### P4: bottom (brought into scope 2026-09-24; batched like P3, one verify + one review per batch)

| Batch | Rows | Platform | Impl | Status | Files | Note |
|---|---|---|---|---|---|---|
| P4-B1 | P4-1 | core + Android | opus | blocked | | Claim holds; not worth it at this scope. H264StreamDecoder keeps each frame's array referenced in its 6-deep backlog and reads it on the decoder thread, plus the P0-13 JNI queue holds more, so a reused DirectByteBuffer ring needs cross-language release-on-consume bookkeeping sized to both; a slot recycled one frame early shows as corrupt video with no error, verifiable only on device. The per-frame byte[] is a young-gen allocation (strict pass rated the chain low). |
| P4-B2 | P4-2 | core + Android | opus | blocked | | Claim holds; fix does not pay. The typed deserialize must stay (_cachedTelemetry answers GetTelemetryNative), Kotlin parses with org.json JSONObject(String) so UTF-8 bytes would add a Kotlin decode in place of the cheap UTF-16 NewString copy, and skipping the C# re-serialize needs raw-span capture in the shared RemexMessage receive loop - every message's path - for a 1 Hz payload P0-13 already coalesces. |
| P4-B3 | P4-3 | core + PC host | opus | staged | LauncherStorageService.cs, LauncherStorageCacheTests.cs | Parse cached per instance, served as a fresh list copy (AppEntry is an immutable record). VULN-3 allowlist (RemEx-q6xt): a static save generation bumped before+after every in-process write invalidates every instance (desktop UI and host hold separate ones over the same file), so a desktop removal is seen on the next load even when mtime+length are unchanged (tested); mtime+length catch out-of-process edits. Failed/corrupt reads never cached. |
| P4-B4 | P4-4, P4-5, P4-6 | Android | opus | staged | AndroidNativeExports.cs, RemexCoreClient.kt, LegacyOutboundBackpressure.kt (+test), AndroidFileTransferHost.kt, FileTransferViewModel.kt, FileTransferFrame.kt, FileTransferChannelClient.kt, FileFrameCodecTest.kt, SerialFrameWorker.kt (+test), FileTransferEngine.kt | P4-4: new additive JNI export GetOutboundQueueDepthNative; both v2 loops wait while >=16 queued (queue itself stays unbounded per P0-12). P4-5: send builds the frame in okio segments (one payload copy), receive decodes from the ByteString (one payload copy). P4-6: v3 download write/hash/ack moved to a serial worker (32-frame queue = unacked window); ack still after write (RG:403), worker stopped before raf close; queue cancelled on any worker exit so the OkHttp reader can never park. gitnexus index stale (symbols not found); callers checked by grep, all in-file or tests: LOW. |
| P4-B5 | P4-7, P4-8 | build | opus | staged | build.gradle.kts, remex.agent.csproj | P4-7: claim half-holds - MSBuild's own incrementality already skips ILC, but restore + evaluation ran every build; Exec tasks now declare inputs (whole remex.core tree, root Directory.*.props, build/, command line, `dotnet --version` + `dotnet workload --version`) and every .so candidate as outputs. No-op :app:syncRemexCoreReleaseSo 2.47 s -> 1.38 s, publish task UP-TO-DATE; probe file added/removed in remex.core re-ran it both times. P4-8: target drops Mvc/Razor/Components/SignalR/VisualBasic RuntimePackAssets right after resolution (self-contained + RID only), so copy-local, deps.json and R2R agree: -27 files, -9.69 MB. Metadata scan of the publish: no remaining AssemblyRef, literal or attribute load target for any of them; deps.json 360 assets, 0 missing. Linux cross-publish scanned clean too. |
| P4-B6 | P4-9 | build | opus | staged | remex.agent.csproj, Directory.Packages.props | Direct refs to Avalonia.X11/FreeDesktop/FreeDesktop.AtSpi/Native with Publish="false", only when RuntimeIdentifier starts with win (Publish=false does not follow deps, so all four named; PackageVersions added at 12.1.1, same as transitive). win-x64: -4 files, -3.92 MB; linux-x64 cross-publish still carries all four. Only Avalonia.Desktop references them, via private LoadX11/LoadAvaloniaNative behind OperatingSystem.IsLinux/IsMacOS (ilspy). Tmds.DBus.Protocol stays (direct ref). Both: win-x64 publish 410 -> 379 files, 216.26 -> 202.65 MB. RG:608 untouched (Program.cs unchanged). |
| P4-B7 | P4-10 | core + Android | opus | staged | RemexMessage.cs, ProcessInfo.cs, PingPongHandler.cs, TaskManagerViewModel.kt, ProcessListSlimWireTests.cs | Request-driven, not capability-gated: process_list_request may carry optional processListSlim; host answers with ProcessInfo.ToSlim() (empty path/publisher/version/user, installDate dropped; startTimeUnixMs kept for the kill guard). Additive both ways (old host ignores it, desktop/old phone never send it): no protocolVersion bump (RG:396). CHANGELOG at gate: new optional request field. |
| P4-B8 | P4-11, P4-12, P4-13, P4-14 | PC host | opus | staged | WindowsScreenCaptureService.cs, ReusableBitmap.cs (+test), LinuxScreenCaptureService.cs, LinuxRawPassthroughTests.cs | P4-11 fixed: GDI tier reuses its screen + scaled scratch bitmaps (per size) under a new _gdiLock (service is shared by viewers + screenshots); SourceCopy overwrites every pixel and the frame still leaves in a fresh byte[]/MemoryStream, so the lcp8 line (never pool the buffer the encoder/socket sees) holds. P4-13 fixed: BGRA/BGRx, in-bounds crop, no resample -> plain row copy (1 copy instead of Subset+FromImage+Bytes); tests compare byte-for-byte against the Skia path (padded stride, garbage X byte, edge crops); RGBA/scaled/odd/out-of-bounds stay on Skia. P4-12 blocked: the AU byte[] flows channel -> handler -> latest-frame slot (Interlocked overwrite) -> fragmenting socket send with no single release point; pooling it is RemEx-lcp8 territory. P4-14 blocked: draining several AUs per tick into the latest-frame slot would overwrite dependent P-frames (RG:74-109 latest-frame semantics); a real burst drain needs an ordered per-session AU queue in the send path. |
| P4-B9 | P4-15 | PC host (Linux) | opus | staged | LinuxCaptureSessionCoordinator.cs, LinuxFrameChannelTimeoutTests.cs | WaitForNextFrameAsync passes the timeout to SemaphoreSlim.WaitAsync(int, ct): no linked CTS/CancelAfter per tick, timeout is a false result (first-chance OCE count 0, tested), a ready frame allocates no timer. Caller cancellation still returns null. Channel made internal for the test. |
| P4-B10 | P4-16, P4-17, P4-18 | PC host | opus | blocked | | All three claims hold, all are multi-viewer only and need the same redesign. P4-16: one handler per /ws/desktop socket owns capture+encoder+pacer; a shared per-monitor encoder needs per-viewer join (a late joiner needs an IDR, which today is a full ffmpeg respawn - P2-8 - disrupting every viewer) and diverging per-viewer fps/scale/quality. P4-17: target and DXGI/WGC cached-frame scale live on the singleton IScreenCaptureService (core interface, one hardware session per output); per-session scope changes that contract on every backend. P4-18: a shared per-monitor cursor publisher must fan out to per-session coordinate mapping, PrecisionPacer loops (RG:74-109) and per-socket RDXC sends. |
| P4-B11 | P4-19, P4-20, P4-21, P4-22, P4-23, P4-24 | PC host | sonnet | staged | LinuxTelemetryService.cs, LinuxTelemetryTopologyCacheTests.cs, WindowsProcessMonitorService.cs, WindowsProcessTimes.cs, WindowsProcessPollTests.cs, WindowsTelemetryService.cs, TelemetryMergeCoveredSetTests.cs, MediaArtworkReplyCache.cs (+test), PingPongHandler.cs | P4-19 fixed: hwmon topology (dirs, name, labels) cached 30 s, value files read per tick; incomplete scans not cached; /proc/stat stops at the "cpu " line; fake-sysfs tests. P4-20 refuted: claim holds (framework re-reads the category per NextValue) but measured 0.16 ms CPU per 3-counter tick on 16 cores, fallback path only. P4-21 fixed: one OpenProcess(LIMITED)+GetProcessTimes gives start+CPU (framework conversions; kill guard start time byte-equal, tested); MainModule refusals remembered by (pid, creation FILETIME) - access denied at once, other codes (299/998, permanent for minimal/protected processes) only once the process is >10 s old so a starting process is retried; second poll first-chance Win32 went 35 -> <=3. P4-22 fixed: covered set computed once per tick and passed to both merges, HWiNFO list handed over instead of merged into an empty payload, merge pre-sized (2 of 3-4 copies + 2 hash sets gone). P4-23 blocked: CsWinRT projections have no safe per-sample release (not __ComObject; ComWrappers hands the same wrapper to the concurrent artwork/seek reads, so disposing NativeObject would break them); ~6 small objects/s. P4-24 fixed: one-slot serialized-reply cache keyed on id + store byte[] instance, sent via SendRawAsync; byte-identical to the old message (tested); evicted ids still get a fresh null reply. |
| P4-B12 | P4-25 | PC host (Linux) | sonnet | refuted | | Claim holds, no action: the 100 ms poll only runs when a second EnsureStartedAsync races a start in progress, which serialized callers never do; the finder advised deleting it when next touching the file. |
| P4-B13 | P4-26 | PC desktop UI | sonnet | refuted | | RemEx-4q6l measured ~0.14 ms/tick for 250 sensors and judged the hidden-window gate not worth its behaviour change; no new measurement contradicts it. |

Impl tiers above: opus for anything touching JNI/wire framing, the decoder/capture/stream path, or a guard note describing a correctness invariant (RG:373, RG:403, RG:396, RG:1146, VULN-3, RG:608, a hostpolicy/deps.json contract, or an "ordering risk"/reused-buffer-under-a-live-send concern); sonnet for pure caching/perf/packaging changes with no such guard. (Reclassified after opus review 2026-09-24: B5, B7, B8, B9 moved from sonnet to opus — RG:1146 stale-.so risk, RG:396 wire contract, and the encoder/capture stream path respectively.)

P4-1 and P4-16 are large redesigns (JNI frame-buffer ring; per-viewer→per-monitor capture pipeline fan-out). `blocked` ("not worth the effort at this scope") is an acceptable outcome for either — the Stop rules still require an attempt and a real reason, not a skip.

P4 row titles:

- P4-1: JNI hands each video frame over as a freshly allocated Java byte[] (ui-25)
- P4-2: Telemetry is deserialized, re-serialized to a UTF-16 string, copied into the JVM and parsed again in Kotlin every tick (C6, C20)
- P4-3: launchers.json re-read and deserialized, icons included, on every connect, sync request, add/remove and launch (C14)
- P4-4: Legacy v2 base64 file path round-trips every 64 KiB chunk through JSON and JNI into the unbounded outbound queue (C23)
- P4-5: Every /ws/files data frame is copied twice on send and twice on receive (C24)
- P4-6: v3 download receive does the disk write, SHA-256 and a queue copy on the OkHttp reader thread (C25)
- P4-7: Gradle Exec tasks declare no inputs or outputs, so restore and ILC run on every Android build (C42)
- P4-8: Unused ASP.NET Core MVC/Razor/Blazor/SignalR and VisualBasic assemblies (~8.5 MB) ship in the self-contained publish (C43)
- P4-9: Linux and macOS Avalonia backends ship in the Windows publish (~4.4 MB) (C44)
- P4-10: Process-list payload carries fields Android discards (path, publisher, version, user, install date) (ui-36)
- P4-11: GDI tier allocates two Bitmaps and a full frame per tick (ac-7)
- P4-12: Fresh byte[] per encoded access unit lands on the LOH at 1080p+ (ac-9)
- P4-13: Linux raw H.264 path makes 2-4 extra full-frame copies through Skia (ac-11)
- P4-14: Encoder output drained one access unit per tick; burst-drain helper underused (ac-20)
- P4-15: Linux WaitForNextFrameAsync allocates a linked CTS and timer per tick and signals timeout by exception (ac-21)
- P4-16: Every /ws/desktop viewer runs its own ffmpeg encoder and BGRA pipe (ac-31)
- P4-17: Viewers share one singleton capture target and cached-frame scale (ac-32)
- P4-18: Every viewer runs its own 90 Hz cursor loop and 10 Hz shape poll (ac-34)
- P4-19: Linux telemetry re-walks /sys/class/hwmon and re-reads name/label and /proc files every tick (ah-5, ah-38)
- P4-20: Windows fallback PerformanceCounter reads re-query the category on each NextValue (ah-6)
- P4-21: Process-list poll opens each process several times and throws for protected processes (ah-7, ah-47)
- P4-22: Windows telemetry tick copies the ~450-sensor list 3-4 times and rebuilds hash sets every second (ah-11, ah-51)
- P4-23: Media sampler builds a fresh set of WinRT RCWs every second and relies on finalizers (ah-13; also in ah-43)
- P4-24: media_artwork reply re-encodes up to 2 MB of cover art to base64/JSON per request per client (ah-14, ah-54)
- P4-25: LinuxPortalInputInjector's 100 ms poll loop is unreachable given serialized callers (ah-61) — the audit's own finder confirms the claim holds but advises no action; expect this row to close `refuted` with "claim holds, no action" (not "claim does not hold"), not implemented.
- P4-26: Sensor presentation (History update, sparkline invalidation) runs while the window is hidden (du-3) — RemEx-4q6l already measured ~0.14 ms/tick for 250 sensors and judged it not worth it; expect `refuted` unless a new measurement contradicts that.

## Live checks for Connor

- P4-B4 (P4-5/P4-6): phone downloads a large file (>100 MB) from the PC and uploads one to it over v3; both should finish with a matching hash, at the same or better speed. Also cancel a download mid-way and start another: the second must run (a stuck /ws/files reader would show as the second transfer never starting).
- P4-B4 (P4-4): only reachable against a v2-only host; if one is available, a legacy upload and a legacy PC-pulls-from-phone download should complete, with phone memory staying flat during a large file.
- P4-B5/B6 (P4-8/P4-9): the published exe was not launched (the installed host owns the mutex and ports). After the next update-local-install, confirm the host starts (window, tray), the phone pairs/connects, the remote-desktop stream and file transfer work, and the web endpoints answer. A missing-assembly failure would show as a startup crash (hostpolicy) or a FileNotFoundException in the Event Log.
- P4-B7 (P4-10): open Task Manager on the phone against the new host - the list, CPU/RAM and kill (including the "process was restarted" guard) should work as before; against an older host it should also still load.
- P4-B3 (P4-3): remove a launcher entry on the desktop, then tap it in the phone's (still stale) list - the launch must be refused; add one on the desktop and launch it from the phone - it must work.
- P4-B8 (P4-11): with the backend forced to GDI (HKLM CaptureBackendPreference) or in virtual-desktop mode, stream H.264 and MJPEG, change quality/scale and take a screenshot mid-stream; picture and cursor should be correct throughout.
- P4-B8/B9 (P4-13/P4-15, Linux/KDE): stream one monitor at 100% and at a lower scale; the picture should be correct (not shifted or skewed) and a static screen should stay live with no capture errors in the log.
- P4-B11 (P4-21): Task Manager on the phone - protected processes still listed, normal ones show path/version, and an app launched while the list is open shows its path within a poll or two; killing it works.
- P4-B11 (P4-24): with two phones connected, skip tracks in a player with cover art; both should show the new art each time.
- P4-B11 (P4-19, Linux): the Linux host's temperature and fan sensors should appear and move as before.

- P3-B7 (P3-29): the release APK no longer carries compose ui-tooling's PreviewActivity. Static @Preview in Android Studio should be unaffected (ui-tooling is still `implementation`); "Run Preview on device" from the release variant no longer has a target activity. Confirm the Preview pane still renders.
- P3-B7 (P3-30): with the phone set to a non-app language (e.g. de) the app should fall back to English everywhere, and pt-BR/id should still show their translations, including library strings (date pickers, permission dialogs).
- P3-B10 (P3-68): with Mica selected, drag a Personalize slider and flip Windows light/dark; the Mica tint should stay (wallpaper-derived), and switching Mica -> Acrylic -> Mica should still repaint correctly.
- P3-B10 (P3-65): enter and leave Remote Desktop fullscreen twice; the stream, input and zoom should behave on each entry (the fullscreen view is now rebuilt per entry, so zoom starts fresh).
- P3-B10 (P3-66/67): open the tray flyout, click away, pin/unpin a sensor on the canvas, reopen - pinned tiles should be current and live; a pinned (docked) flyout should keep updating.
- P3-B10 (P3-63): leave the Logs page, generate some log lines, return - they should be there and the list should follow the tail.
- P3-B9 (P3-57): a sensor hovering at its alert threshold should show the live alert once and stay lit until it drops ~2% back, not flicker.
- P3-B9 (P3-61): sparkline tiles (ring, LED, glow area, dual metric) should look unchanged.
- P3-B8 (P3-56): restart the app twice within a day; About should still show the update status on the second start without a network call.
- P3-B11 (P3-34/37): stream a single monitor, switch monitor from the phone, then switch back; the picture, framing and cursor position should be right after each switch. With the backend forced to GDI (HKLM CaptureBackendPreference), unplugging or rotating a monitor mid-stream should recover within about a second.
- P3-B11 (P3-38): with adaptive scale on, run a heavy GPU load mid-stream; the log's "Adaptive scale:" lines should show a step down and then no immediate step back up (hold), not an up/down/up/down run.
- P3-B12 (P3-40/41): disconnect the phone mid-stream while holding a modifier on the on-screen keyboard; the key should still be released on the PC, and a reconnect should stream normally.
- P3-B12 (P3-44): move over a link (hand), a text field (I-beam, an inverted cursor) and a resize edge while streaming; the phone's cursor overlay should match each shape.
- P3-B12 (P3-46): toggle a VPN or Wi-Fi on the PC; the phone should still find the PC by discovery afterwards, and the host log should show one "re-advertising" line per change (or a debug "unchanged" line), not a burst.
- P3-B13 (P3-47): play media from an app whose session has no art (the app icon shows); skip several tracks - the icon should still appear on each.

## Rulings

(none yet)

## Refuted (audit)

- **RP1** (strict): InMemoryLogSink shifts the 3000-entry list under a global lock per append; one dispatcher hop per entry. The mechanism exists, but the cost premise is wrong.
- **RP2** (strict): no baseline/startup profile and no profileinstaller. profileinstaller already ships as a transitive dependency.
- **ac-1**: unchanged desktop re-encoded via cached-frame replay. The capture loop checks IsLive and skips stale replays (RemoteDesktopHandler.cs:661-706).
- **ac-10**: capture backend resources stay resident for the connection. Intentional warm-session design.
- **ac-30**: keep-awake can leak across threads. Refuted there as process-wide, but ah-2/ah-42 confirm it and the spot-check found no dedicated thread; kept as P1-10.
- **ah-16**: a fresh ~74 KB telemetry frame is built every second. Already lazy; built only when a client asks (RemEx-jyuem, TelemetryBackgroundService.cs:158-181).
- **ah-39**: HWiNFO FileNotFoundException every tick. This refutation is wrong: TryEnsureHwInfoOpen swallows the exception (WindowsTelemetryService.cs:653-656), so `_hwinfoAvailable` never flips; kept as P3-45 (ah-4).
- **ah-46**: fallback PerformanceCounter re-queries the whole category. The code holds single pre-built counters; the framework-internal claim survives as P4-20 (ah-6), contested.
- **ah-62**: Windows TypeText allocates an INPUT[] per code-point group. Bursts are short and infrequent.
- **ar-22**: two implicit control-socket keepalives; the link-quality ping does not exist. Documentation and dead-code issue, not a saving; keepalives are needed for NAT.
- **du-47**: standalone 2 s PIN poll runs despite embedded pairing. Refuted on risk (it may be a required fallback), not on facts; C33 (strict) confirms the poll; kept as P1-31 with that caveat.
**Tier counts:** P0 21 rows (42 findings), P1 32 (48), P2 22 (34), P3 68 (83), P4 26 (31). 169 rows cover all 238 confirmed findings; 0 dropped and 6 demoted at spot-check; 11 refuted. P4 was originally scoped out of this loop ("Deferred, not in this loop") and was brought into scope on 2026-09-24 at Connor's request; see the P4 group table above.

## Log

- 2026-09-23 setup - tracker generated from PRIORITIES.md; loop not started
- 2026-09-23 P0-1 staged - lifecycle-gated Task Manager auto-refresh poll
- 2026-09-23 P0-2 staged - window-visible gate on aurora/gradient/pulse animations, 2 review rounds
- 2026-09-23 P0-3 staged - RD stream foreground-gated, 2 review rounds + 1 self-caught fix
- 2026-09-23 P0-1 reworked - original fix was a no-op, LifecycleStartEffect fixes it, re-reviewed PASS
- 2026-09-23 P0-4 staged - phone RD stream paused in background, 2 review rounds fixed 3 races
- 2026-09-23 P0-5 staged - telemetry pause/resume, new msg types, parallel cs+kt reviews, 2 MEDIUMs fixed
- 2026-09-23 P0-6 staged - widget cache write skips no-consumer case, 3 review rounds fixed a HIGH data-loss bug
- 2026-09-23 P0-7 staged - idle-close for /ws/files and /ws/desktop catalog, both critical guards CONFIRMED, 1 MEDIUM fixed
- 2026-09-23 P0-8 staged - drainLoop suspends instead of polling, 3 review rounds fixed 2 real concurrency bugs
- 2026-09-23 P0-9 staged - Task Manager poll gated on IsWindowVisible, 2 review rounds
- 2026-09-23 P0-10 staged - telemetry/media sampler consumer-count gate, 2 review rounds fixed a HIGH silent-stream race
- 2026-09-24 P0-11 staged - reconnect heartbeat gated on network+foreground/widget, fixed a fail-open bug; last row in P0a, group gate next
- 2026-09-24 P0a group gate - full verify + -Check both pass; detect_changes advisory (critical/breadth-driven, nothing unexpected); committed ce68e66 (47 files); not merged to main yet (P0b/c/d remain in the P0 tier)
- 2026-09-24 P0-19 staged - AttachViewModel no-ops on already-attached vm, closes a double-subscribe leak
- 2026-09-24 P0-20 staged - cursor-shape LRU caches (both platforms), parallel reviews confirmed dispose-safety
- 2026-09-24 P0-21 staged - thumbnail map soft-capped at 200, oldest-evicted; round 1 caught a HIGH (evicted keys left dead in the dedup map, permanent blank thumbnails); fixed and round 2 PASS; last row in P0b, group gate next
- 2026-09-24 P0b group gate - full verify + -Check both pass (one transient suite-ordering hang on an unrelated test, confirmed pre-existing/unrelated by isolated re-run with and without P0b changes); detect_changes advisory (5 processes touched, all within P0-19/20/21 scope, nothing unexpected); committed 8585c2a (10 files); not merged to main yet (P0c/P0d remain in the P0 tier)
- 2026-09-24 P0-15 staged - thumbnail decode sampled via SKCodec instead of full-res, csharp-reviewer PASS, 1 MEDIUM alpha-premultiplication bug caught and fixed
- 2026-09-24 P0-16 staged - fps StateFlow throttled to ~1 Hz, window/rate math still per-frame; kotlin-reviewer PASS, 1 LOW pre-existing clock-jump weakness deferred
- 2026-09-24 P0-17 staged - RD receive loop reuses one buffer instead of per-message MemoryStream, VM applies frames through a latest-wins slot instead of unconditional Post; csharp-reviewer PASS, StreamSerial guard and dispose-ordering traced clean, 1 LOW pre-existing deferred; P0-18 remains in P0c
- 2026-09-24 P0-18 staged - theme apply and sidecar rewrite skipped on content-equal profile loads, fixing startup's double-apply; round 1 FAIL was test-coverage-only (logic confirmed sound), fixed with 6 new tests; round 2 PASS, 1 MEDIUM test-isolation fix applied inline; last row in P0c, group gate next
- 2026-09-24 P0c group gate - full verify + -Check both pass; detect_changes advisory (high risk driven by LoadAsyncCore's breadth touching 8 BuildSavefileAsync-adjacent processes, all within P0-15/16/17/18 scope, nothing unexpected); committed 731df4d (9 files); not merged to main yet (P0d remains in the P0 tier)
- 2026-09-24 P0-12 staged - KeepAliveTimeout + bounded send on both native clients, receive-loop drop-detection fixed to feed the existing reconnect flow; round 1 FAIL surfaced both a missing-tests gap and a real Close-frame self-await deadlock (test-verified fix); round 2 PASS, 2 LOW deferred; ConnectionViewModel.cs:2064 flagged as a follow-up bead (separate PC socket, same class of gap, out of scope here)
- 2026-09-24 P0-13 staged - JNI frame/data queue split, throttled keyframe-on-drop held to first IDR, artwork parse off the JNI thread; round 1 parallel csharp+kotlin reviews: Kotlin PASS, C# FAIL (2 HIGH: keyframe gate not reset across sessions risking RemEx-vj7b's one-time allowance, no concurrency tests); round 2 fixed both (real 8-thread stress test included), PASS, 1 LOW deferred as RemEx-8sf8m; P0-14 remains in P0d
- 2026-09-24 P0-14 staged - OrderedAsyncWorkQueue.Enqueue takes an optional coalesceKey, single consumer merges only adjacent same-key items so buttons/keys and the moves either side are never reordered or dropped; only absolute mouseMove (x+y, no deltas) gets a key; RemexDesktopClient records connect failures and fails fast for 10s on queued sends while StartStreamAsync itself stays ungated; deviated from a shared in-flight connect task since the single consumer already serializes connects; csharp-reviewer round 1 PASS with 1 MEDIUM (coalesce key missed the x/y-plus-delta case) fixed inline plus a test, 1 LOW deferred harmless; last row in P0d, group gate next
- 2026-09-24 P0d group gate - full verify + -Check both pass (6176/6178); detect_changes advisory (HIGH risk driven by StartDesktopStream/SendDesktopPointerBatch connect/pointer processes, all within P0-12/13/14 scope, nothing unexpected); one CHANGELOG entry covering P0-12 through P0-14; committed a0ea840 (17 files); last group in the P0 tier - fast-forwarding main next
- 2026-09-24 P1-1 staged - self-heal resets consecutiveFailures only when the discovered mDNS address actually changed, not on every pinned hit, so a refusing-but-visible PC now backs off toward the intended ~300s ceiling instead of resetting near 40s each cycle; isMulticastReachableHost guard untouched; kotlin-reviewer PASS, 2 LOW deferred
- 2026-09-24 P1-2 staged - client-side leading+trailing (200ms) debounce on desktop_config sends during quality/fps/scale slider drags, caps a drag at 2 host encoder rebuilds instead of one per tick; kotlin-reviewer PASS, 1 MEDIUM (double-send on pause-then-resume, immediate-send check now also gates on time-since-last-send) fixed inline with a regression test, 1 LOW deferred
- 2026-09-24 P1-3 staged - Interlocked pending-apply flag collapses rapid Personalize slider ApplyCustomization calls to one queued dispatcher post reading settings fresh; csharp-reviewer PASS, 1 MEDIUM (test didn't prove the ordering it claimed) fixed with a reentrant-from-CustomizationApplied test, 1 LOW deferred
- 2026-09-24 P1-4 staged - Linux input dispatch moved off the /ws receive loop onto its own consumer thread (mirrors RemoteDesktopHandler's queue+teardown pattern exactly), RunToolWithOutput starts async stdout/stderr reads before WaitForExit so the 2s timeout actually fires and kills a hung xdotool; round 1 FAIL (2 HIGH, missing catch arms + no test) fixed; round 2 FAIL (1 HIGH, new test would pass slow against a revert) fixed by driving HandleAsync via Task.Run with a short assertion window; 1 LOW accepted
- 2026-09-24 P1-5 staged - desktop upload picks a binary v3 /ws/files path (loopback-only) over the legacy Base64/JSON path, RG:403 confirmed (WaitForCommittedAsync drains before file_transfer_complete); csharp-reviewer PASS, 4 MEDIUM + 1 LOW fixed inline (stale no-PC-consumer comments, unlogged dropped-channel exception, cancel-mid-upload + refused-loopback-fallback tests, Dispose/connect race)
- 2026-09-24 P1-6 staged - claim confirmed by measurement (101.6 MB of vendor native PDBs in a Release win-x64 publish); new Release-only MSBuild target strips them from ResolvedFileToPublish + PublishDir; csharp-reviewer PASS, 1 MEDIUM fixed inline (deps.json still listed the stripped PDBs via NativeCopyLocalItems, second target added, verified via a real scratch publish)
- 2026-09-24 P1-7 staged - FFmpeg where/which detection moved from per-instance (2 encoders/connection) to a process-wide cached FFmpegDetector (found path cached forever, not-found retried after 30s mirroring ProbeCache); csharp-reviewer PASS, 1 LOW fixed inline (comment overstated the retry's real-world reach given RemoteDesktopHandler's own permanent negative cache)
- 2026-09-24 scope change - Connor asked to bring P4 (26 rows, previously "Deferred, not in this loop") into the loop's scope; added a P4 batched group table (13 batches) mirroring the P3 batch style, updated Stop rules and the group-gate tier list to include P4, removed the now-redundant Deferred section. P1-8 (Android H.264 decoder busy-poll fix) was in flight via a dispatched implementer when Connor asked to pause the loop; it was killed mid-task but had already written an unstaged, unreviewed, unverified edit to H264StreamDecoder.kt (2ms->200ms poll with an incorrect comment - the output-drain call shares the same loop iteration, so a longer idle poll also delays draining a ready decoded frame, not just idle wakeups). An opus review of the restart plan caught this; the edit was reverted (`git restore`) and P1-8 stays `todo`, to be redone properly with output-drain latency accounted for. Ralph loop was fully stopped (state file deleted; the plugin's stop-hook only checks file existence, not an `active` flag) pending that review and restart with the iteration count reset. Two gitnexus-count-only lines in AGENTS.md/CLAUDE.md were also found staged alongside the perf-row files at this point (unrelated drift) and committed separately (20c3b73d) before restart, per the "unstaged diff = current row" rule.
- 2026-09-24 loop restarted - iteration reset to 1, max_iterations 250, completion_promise PERF-DONE (per opus review of the restart plan). Also fixed per that review: P4 batches B5/B7/B8/B9 reclassified sonnet->opus (RG:1146, RG:396, encoder/capture stream path, ordering risk), "P3 batches"/"for P3, the first todo batch" scope text extended to P3/P4, P4-25's note corrected (claim holds, not worth fixing - not "claim false"), noted `blocked` is an acceptable outcome for P4-1/P4-16 (large redesigns).
- 2026-09-24 P1-8 staged (redo) - round 1 FAILED (1 HIGH: a drain-before-poll reorder was provably useless, decoded frames still sat behind a 20ms poll); round 2 fixed with an `inFlight` counter gating the poll timeout (2ms while a fed AU's output is still pending, 20ms only when genuinely idle) - reviewer's own prescribed lower-risk fallback; round 2 PASS with 1 MEDIUM deferred (inFlight can drift upward and never self-heal if an AU's output silently never renders - falls back to original always-fast-poll behavior, never adds latency) and 2 LOW fixed inline (stale comment, corrected which encoder paths actually guarantee no B-frames). P1-1 through P1-8 staged; P1-9 through P1-32 remain `todo` (P1 is one large ungrouped tier, no sub-gate until all 32 rows are done).
- 2026-09-24 P1-9 staged - stale comment (no shown-before check ever existed) confirmed, demoted P0->P1 since the splash is a deliberate cold-start brand moment; added a process-lifetime `SplashGate` flag so only a warm start (not rotation, not genuine cold start) skips replaying it; kotlin-reviewer PASS, 2 LOW fixed inline (KDoc wrongly named rotation as the trigger; live-read of the mutable global during composition snapshotted via `remember` to avoid a mid-fadeOut recomposition re-triggering navigation). One implementation slip (object declaration split an `@OptIn`/`@Composable` pair) caught by the scoped build, not by review.
- 2026-09-24 P1-10 staged - claim confirmed (SetThreadExecutionState is per-OS-thread; engage/disengage span an await-the-whole-stream gap and land on different pool workers, so the disengage cleared nothing and the hold could outlive the session); replaced with the handle-based Power Request API (PowerCreateRequest/Set/Clear, not thread-bound), ref-counting/locking/interface untouched; verified by hand with `powercfg /requests` across a real cross-thread release. csharp-reviewer PASS, 1 MEDIUM + 2 LOW fixed inline: the cross-thread test let the acquiring thread exit before release (CoreCLR can reuse a dead thread's ID, and Windows drops a thread's execution-state on exit either way, so the test wouldn't have caught the original bug) - fixed by keeping that thread alive on an event until release completes; and a stale "re-locks the session" sentence (the guard never touches lock state) corrected in both REGRESSION-GUARDS.md and a RemoteDesktopHandler.cs comment.
- 2026-09-24 P1-11 staged - claim confirmed (cursor poll ran a fixed 90Hz regardless of activity); added `CursorPollCadence` state machine (90Hz active, drops to 30Hz after ~0.5s quiet, snaps back on any movement/handle/shape-serial change, time-based ~10Hz slow tick force-fireable), `StreamCursorPositionAsync` now shares the capture loop's display-off pause+`pacer.Reset()`-after ordering and calls `WaitForNextTickAsync` with the cadence's per-call interval (no cached-interval assumption, matches `PrecisionPacer`'s actual contract). csharp-reviewer PASS, 0 HIGH/CRITICAL/MEDIUM, 1 LOW deferred (an animating cursor's shape-serial changes count as activity and hold 90Hz for as long as it animates - no worse than before this row, just no saving in that case). New source-scan test in PacerResetOrderingTests.cs pins the pause+Reset ordering inside StreamCursorPositionAsync specifically, manually traced by the reviewer to actually fail if broken.
- 2026-09-24 P1-12 refuted - claim false against current code; the full cursor-shape capture (SyncCursorShapeAsync) is already gated behind `handleChanged || slowTick` in StreamCursorPositionAsync, not run on every 90Hz tick (comment tags this "Phase 4", predates this session). No code change; no review needed.
- 2026-09-24 P1-13 staged - claim confirmed (MJPEG re-encoded+re-sent an identical JPEG every tick); added a WGC-tier raw-frame JPEG cache (skips encode when the raw BGRA buffer is byte-identical to the last one) plus a per-connection MjpegResendGate that decides whether to actually publish (changed buffer, stream-serial change, ~2s forced refresh, or force) - DXGI already cached/replayed internally so only the send-side gate was needed there; GDI fallback left always-encoding (low-priority path). csharp-reviewer round 1 PASS, 1 MEDIUM fixed inline (added 2 tests: a second concurrent client must still see a change the first client's gate already forwarded; a non-live->live transition on the same buffer must be forced out immediately, not held for the 2s refresh - both pass), 2 LOW deferred (timing-assertion slack in the new end-to-end tests; the cache is single-entry so mixed per-client quality/scale settings lose the saving - perf-only, no correctness impact).
- 2026-09-24 P1-14 staged - claim confirmed (ProbeCache key included fps, so an fps-slider change forced a fresh ffmpeg capability probe even for an already-known-good codec/geometry); dropped fps from the key, same rationale already applied to qp (clamped, never decides encoder-open success). csharp-reviewer PASS, 0 findings.
- 2026-09-24 P1-15 staged - claim confirmed on both PC host and Android. Host: added a singleton in-process thumbnail cache keyed on path+mtime+size+maxDim (not path alone - a path-only key would serve a stale thumbnail for a changed file, C15); null (undecodable/transient-failure) results are never cached. Android: requestThumbnail() returns early when `_thumbnails` already holds the entry, since that map survives a folder re-browse while the dedup-only `requestedThumbnails` map is cleared on every one. csharp-reviewer round 1 FAILED (2 HIGH: test used Windows-only System.Drawing instead of the project's existing SkiaSharp; neither test actually proved a cache hit vs. an idempotent re-encode - 1 MEDIUM: a transient decode failure got cached as a permanent "no thumbnail") - fixed (SkiaSharp swap; test now overwrites the file with different-color-but-same-size content and restores the original mtime, so only a real cache hit makes it pass; null results excluded from the cache insert); round 2 PASS, 0 findings. kotlin-reviewer PASS, 2 LOW deferred (Android has no way to detect a host-side file change within an already-cached folder listing - a pre-existing gap, not introduced here; the practical saving is smaller than implied since leaving a folder still discards its thumbnail cache).
- 2026-09-24 P1-16 staged - claim confirmed (viewModelScope outlives the composed screen, so the telemetry-parse collector ran regardless of the dashboard tab's visibility); mirrored TaskManagerViewModel/TaskManagerScreen's existing P0-1 fix exactly (visible flag + LifecycleStartEffect + pager-current-page expression), threaded through AppNavigation's dashboardScreenContent type at every site. kotlin-reviewer PASS, 1 MEDIUM fixed inline (sparkline history froze but kept appending, so a real multi-minute swing while off-screen would render as an instant snap on return - now clears history on the off->on transition), 1 LOW deferred (up to 1s of stale headline values on return - same pre-existing gap as TaskManager, accepted).
- 2026-09-24 P1-17 staged - claim confirmed (two-finger wheel-scroll had no throttle, unlike every other pointer-send path in this file); added the same ~30Hz MOVE_THROTTLE_MS gate around the existing accumulator-based send, accumulator keeps summing every sample regardless of throttle so motion is coalesced, not dropped. kotlin-reviewer round 1 PASS, 2 MEDIUM + 1 LOW - fixed inline (a gesture ending inside the throttle window was silently dropping its tail motion on reset, now flushed first; switched the throttle clock from wall-clock System.currentTimeMillis to SystemClock.uptimeMillis, which can't jump backwards mid-gesture); round 2 PASS, 1 MEDIUM deferred as RemEx-9vjkx (extract this accumulator/throttle logic into a testable pure ScrollCoalescer - review already found a real bug in it once, worth a follow-up, not blocking).
- 2026-09-24 P1-18 staged - claim confirmed (150+ process JSON parse and the derived filter/sort pipeline both ran on Main every ~4s poll); parse moved into withContext(Dispatchers.Default) mirroring DashboardViewModel's already-reviewed telemetry parse, and the combine() dedupe/exclude/filter/sort pipeline got flowOn(Dispatchers.Default). kotlin-reviewer round 1 PASS, 1 MEDIUM fixed inline (the new suspension point let scope cancellation get swallowed by the existing catch-all as a bogus parse-failure log - added a CancellationException rethrow ahead of it); round 2 PASS, 0 findings.
- 2026-09-24 P1-19 staged - claim confirmed (glowAlpha's continuous infinite-pulse .value was read in composition then passed to .background(), so every frame tick recomposed the whole card); the State<Float> is now held instead of dereferenced, and .value is read inside a .drawBehind{} lambda so the pulse only triggers redraw. kotlin-reviewer PASS, 0 findings.
- 2026-09-24 P1-20 staged - claim confirmed (MorphPolygonShape had no equals/hashCode, so cardShape() calls always looked "different" to Compose's change-detection, forcing an outline rebuild on every recomposition - and ProcessInfo's raw cpu/ram doubles gave every Task Manager row a genuinely new, non-equal data-class instance every ~4s poll even when nothing displayed changed); added equals/hashCode (morph by reference - deduped via the existing morphCache, so reference equality is exact; progress by value) and rounded cpu/ram to displayed precision at parse time. kotlin-reviewer round 1 PASS, 1 MEDIUM + 2 LOW - fixed inline (an idle host flooring every process to 0.0 made maxCpu/maxRam a real per-poll NaN source, not just a first-poll one - added a >0.0 guard before the ?: 1.0 fallback; switched round-to-nearest to floor so displayed values are genuinely unchanged from before this row, not just closer; added a plain-JVM equality test against the real cardShape()); round 2 PASS, 0 findings.
- 2026-09-24 P1-21 staged - claim confirmed (status-dot pulse ran truly infinitely while connected, and the chip is composed above every screen for the whole session, so this blocked LTPO refresh-rate drop for the entire connected duration, not just briefly); reduced-motion was already honored - added a pulseActive flag that stops the animation after 3 breathe cycles from a fresh connect, settling to a steady dot afterward and re-arming on reconnect. kotlin-reviewer PASS, 2 LOW (both cosmetic: the pulse re-arms on any recomposition re-entry not only a genuine reconnect - harmless, self-stops either way; a pre-existing doc comment mislabeled a half-cycle - fixed inline).
- 2026-09-24 P1-22 staged - claim confirmed (every routine connect step logged at ERROR, 5-10 lines per attempt, repeated by the offline heartbeat's retries); added a priority param to AndroidLogE (default ERROR, ~40 other call sites unaffected) and retagged routine steps to DEBUG/INFO/WARN, leaving genuine validation failures at ERROR. csharp-reviewer round 1 PASS, 2 MEDIUM - fixed inline (a doc-comment placement bug; more substantively, __android_log_print writes unconditionally regardless of priority so retagging alone didn't reduce write cost - added an __android_log_is_loggable gate, always available at minSdk 34, that actually skips the native write for DEBUG messages nobody enabled); round 2 PASS, 1 new MEDIUM surfaced by the gate now genuinely working (a stale-pin mismatch would log only "Rejecting connection" with the diagnostic SPKI hashes silently dropped at DEBUG) - fixed inline (folded both hashes into the ERROR mismatch line), not re-reviewed (round cap already reached; small, well-understood one-line change).
- 2026-09-24 P1-23 staged - claim confirmed (every desktop app launch shelled out to powershell.exe/firewall-cmd+ufw, uncached, once per launch). Cached to disk keyed on exe path+mtime+port, bounded by a 1h TTL, only ever caching a true verdict so a refused/unknown result always re-queries (no window where a user's own fix-then-Refresh could see a stale "refused"). csharp-reviewer: 3 rounds. Round 1 FAIL (2 HIGH: cache key missing port, caching null verdicts, mtime alone doesn't reliably catch an MSI repair; a test that only passed on Windows) - fixed (port added to key, null never cached, 1h TTL added, test rewritten). Round 2 FAIL (2 HIGH: the rewritten tests still hardcoded platform-specific run counts that fail against Linux's up-to-2-spawn query path; a cached refused verdict could still survive the user's fix-then-Refresh sequence for up to the TTL) - fixed (tests now compare run counts relatively before vs. after, never a fixed constant; only a true verdict is ever cached, so refused/unknown always re-queries with zero staleness) - also fixed 1 LOW (guard against a corrupt/out-of-range or clock-skewed cached timestamp). Round 3 PASS, 1 MEDIUM deferred as RemEx-ny6u4 (a cached true verdict can still outlive a firewall rule removed with no matching mtime change, bounded by the TTL but with no explicit Refresh-bypass), 1 LOW accepted (a Linux test branch exercises the null path rather than a genuine false verdict, but still passes).
- 2026-09-24 P1-24 staged - claim confirmed (/ws sends repetitive telemetry+control JSON with no permessage-deflate negotiated; /ws/desktop and /ws/files carry already-compressed binary and were left untouched). Enabled compression on both host (WebSocketAcceptContext.DangerousEnableCompression) and native client (ClientWebSocketOptions.DangerousDeflateOptions). csharp-reviewer round 1 FAIL, 1 HIGH security finding - the initial implementation defaulted compression context takeover on, and /ws genuinely mixes attacker-reachable content (PingPongHandler's MediaState, settable via any browser tab's OS media-session API) with secrets (ClipboardContent) on one connection - a real CRIME/BREACH-class oracle exploitable by a passive network observer (SPKI pinning only blocks an active MITM) - fixed by disabling context takeover both directions, which resets the deflate window per-message so no message's compressed size depends on another's content; 1 MEDIUM (unverified whether compression actually links/works on the Android NativeAOT target - first use of System.IO.Compression in remex.core), not independently device-tested. Round 2 PASS, 0 findings - reviewer confirmed no message-fragment-mixing path exists, MediaState/ClipboardContent never share a message, and resolved the MEDIUM by inspecting the actual built libRemexCore.so's ELF imports (deflateInit2_/inflate/libz.so genuinely linked) rather than trusting a build-only check; an on-device smoke test remains recommended as non-blocking follow-up.
- 2026-09-24 P1-25 staged - claim confirmed (WriteAsync ran unconditionally on every live customization change, including per-tick slider drags). Added a static dedupe (skip when Seed/Variant/Mode/Contrast unchanged) plus a 400ms debounce (coalesces a genuinely-changing field like contrast to at most one write per window); RG:694 preserved. csharp-reviewer needed 3 rounds, all substantive concurrency-correctness findings. Round 1 FAIL, 3 HIGH - the first design's fast dedupe-skip ran unconditionally (so reverting to the on-disk value mid-debounce got silently dropped, leaving a superseded intermediate value as final) and a value arriving during the actual disk write was lost outright (single write, unconditional clear) - fixed by gating the fast skip on "nothing in flight" and rewriting the debounce as a loop that re-checks pending state after each write and does another pass if something newer arrived; added a revert test, a rapid-burst test, a test-cache-reset hook, and a null-guard against a mid-debounce reset. Round 2 FAIL, 1 HIGH + 1 MEDIUM - the new tests didn't actually exercise the mid-write loop-back branch (round 1's single-write code would still pass them), and one more test class leaked the static cache across sequential test runs via an indirect WriteAsync call - fixed with an injectable write-function test seam gated by a TaskCompletionSource to deterministically hold a write open while a second value arrives (proving the loop-back branch fires), and a cache reset added to the missed test class. Round 3 PASS, 2 LOW (an unbounded test await - fixed inline with a timeout; no-flush-on-app-shutdown - accepted residual risk).
- 2026-09-24 P1-26 staged - claim confirmed on both staging-drawer lists (StagedCards and, per round-1 review, SensorActivationItems too - the latter wrongly assumed "naturally small" but actually holds active sensors plus every staged sensor name). Removed the outer ScrollViewer from both; StagedCards relies on the DockPanel's fill area for its bound, SensorActivationItems gets MaxHeight/ScrollViewer.VerticalScrollBarVisibility moved directly onto the ListBox (a wrapping ScrollViewer.MaxHeight only bounds its own final size, not what it hands the child during measure - the real bound has to live on the ListBox). Both get an explicit VirtualizingStackPanel, matching the TaskManagerView precedent (RemEx-3oy7x). csharp-reviewer round 1 FAIL, 2 HIGH + 1 LOW - the SensorActivationItems gap and a missing regression test (the precedent has one) - fixed with a 5-test source-scan suite covering both lists; round 2 FAIL, 1 HIGH - one new test's null-conditional attribute check would silently pass on a missing MaxHeight instead of catching the regression - fixed inline, not re-reviewed (trivial one-line test fix).
- 2026-09-24 P1-27 staged - claim confirmed on the memory-residency half only. The decode-size half was investigated and refuted: PersonalizeSurfacesTab's WallpaperBlur slider has Minimum=0, and BlurRadiusToEffectConverter returns no effect at radius<=0, so the wallpaper can render fully sharp - the audit's "always blurred" premise is false against current code, and downsizing the decode as suggested would have been a real, user-visible quality regression, not a safe win. Fixed the confirmed half: leaving Wallpaper mode now clears and disposes WallpaperBitmap (deferred via Dispatcher.UIThread.Post, matching the existing superseded-bitmap dispose pattern already used elsewhere in this class, RemEx-8twk0.5) and resets _wallpaperPathLoaded. csharp-reviewer PASS, 2 LOW - fixed inline (the new source-scan test only checked that a dispose happened, not that it was deferred - tightened to match the exact Dispatcher.UIThread.Post shape); one accepted (returning to Wallpaper mode now always re-decodes instead of reusing a cleared bitmap - the correct tradeoff, not a regression).
- 2026-09-24 P1-28 staged - claim confirmed (neither StopStreamAsync's finally nor OnDisconnected released the last decoded, full-display-resolution frame; it stayed resident until the next stream start happened to overwrite it). Added ClearCurrentFrame() to both exit paths, synchronously - matching the file's two other existing call sites, not deferred like the sibling P1-27 wallpaper fix (a different rendering surface with a documented reason for deferral that doesn't apply here). csharp-reviewer PASS, 0 findings - confirmed the synchronous dispose is safe (an existing call site already disposes synchronously at stream rate against the same binding), both new call sites run on the UI thread, and source-scan tests are the right call given this test assembly has no Avalonia.Headless reference to construct a real Bitmap for a behavioral test.
- 2026-09-24 P1-29 staged - claim confirmed (a --minimized logon start unconditionally built MainWindow's whole visual tree - theme apply, wallpaper decode - only skipping Show()). Deferred entirely for a minimized start, relying on BringMainWindowToFront()'s existing lazy ??= construction fallback (RG:1032-1049's already-guarded helper). csharp-reviewer round 1 FAIL, 2 HIGH (regression-guard area) - two other MainWindow consumers gated their whole branch on `MainWindow is { }` BEFORE ever calling that helper, so a minimized start would silently hit their null-owner fallback: the file-consent dialog (exactly the "prompt nobody could see" failure the guard exists to prevent) and the one-time first-run restore-from-snapshot offer (would have been silently and permanently skipped, since the gating flag only fires once per launch) - both fixed by calling/lazily building through the same pattern instead of requiring MainWindow to already exist, with regression tests for both. Round 2 PASS, 1 MEDIUM + 2 LOW - fixed inline (REGRESSION-GUARDS.md's own text described three MainWindow states, now inaccurate since a fourth - never constructed - exists; a stale "always present" comment on the pairing dialog's fallback; a test that pinned call ordering but not the guarding `if` condition itself), not re-reviewed a third time (small, well-understood, non-functional fixes).
- 2026-09-24 P1-30 staged - claim confirmed (the always-present Personalize SideSheetContent bound DataContext directly to the BUILDING CustomizationVm getter, forcing full VM construction - font enumeration, HctColorWheel tone discs - at shell load regardless of whether the sheet is ever opened). Added a non-building CustomizationVmForSheetBinding property that starts null and is only populated via its own change notification once EnsureCustomizationVm actually builds an instance; the XAML now binds that instead. csharp-reviewer PASS, 2 MEDIUM - fixed inline (a closed sheet stayed bound to a disposed VM after a profile import, since the ProfileReplaced handler only notified the new property when the sheet was open - now raised unconditionally; nothing pinned the actual XAML binding target, so reverting just that attribute would defeat the fix with every VM-side test still green - added a source-scan test). This is the exact row RemEx-waqb4's own risk note warned about; its test file gained 4 new cases covering sheet-open, sheet-closed, and already-open-rebuild-on-import.
- 2026-09-24 P1-31 staged - claim confirmed against du-47's own caveat. AttachEmbeddedPairingService already calls StopStandalonePairingPinPolling() as its first line, but that call is dead at startup since it runs before the standalone poll ever starts - confirming the two paths were meant to be mutually exclusive but the startup wiring in App.axaml.cs never enforced it. Checked REGRESSION-GUARDS.md's pairing_pin_response and TransportTrust guards; neither concerns this desktop dual-poll path. Gated the standalone poll's priming refresh + StartStandalonePairingPinPolling behind an embeddedPairingAttached flag; AttachStandalonePairingPinQueryService itself stays unconditional as CanRevealPairingPin fallback wiring for a host with no embedded IPairingService. csharp-reviewer PASS, 0 CRITICAL/HIGH/MEDIUM, 2 LOW accepted (a pre-existing dead fallback poll when no embedded host exists at all, filed as RemEx-f2dwg; source-scan test style matching this file's precedent). Side effect worth noting: before the fix the poll cleared a naturally-expired PIN within 2s, erasing the "expired, get a new one" state OnPairingExpiryTick (RemEx-7ykyn) deliberately keeps once embedded pairing is active - now that state correctly persists.
- 2026-09-24 P1-32 staged - host half REFUTED against current code (TransferSessionManager.ForgetQueued already removes Done/Cancelled on terminal transition, keeps Failed deliberately, RG:546; no host changes made). Android half confirmed: TransferQueueStore.upsert/remove/pruneFinished each re-read the whole JSON from disk on every call despite the caller (FileTransferEngine's _queue StateFlow) already holding the current list, and no automatic terminal-entry pruning existed at all (only a manual "clear finished" UI button). Fixed the redundant reads by having upsert/remove/pruneFinished take the caller's in-memory list; added pruneStale(7 days) run once at engine start rather than on every terminal transition - unlike the host, FileManagerQueuePanel renders a live "Done" row plus its own clear-finished button, so instant removal would make completed transfers vanish before the user saw them. kotlin-reviewer round 1 FAIL, 1 HIGH - moving the list read outside the store's lock left the StateFlow read-modify-write unsynchronized between enqueue (UI thread) and updateState/updateProgress (engine's IO dispatcher, every data frame), so a race could silently drop a queued transfer from both memory and disk with nothing to recover it (the pre-fix code re-read disk every time, which incidentally self-healed this) - fixed with one queueLock synchronizing all 6 mutation sites. Round 2 PASS. 2 LOW from round 1 - fixed inline (pruneStale's KDoc didn't mention it also drops Failed), one accepted (pruning keys on createdAtMs rather than last-state-change timestamp, so a transfer paused/failed over 7 days then resumed to Done gets pruned on the next app start before the user sees it - rare, and fixing it needs a new persisted field for a LOW). Last row in P1; group gate next.
- 2026-09-25 P1 group gate PASS - scripts/verify.ps1 -Scope all: 6291/6293 (2 pre-existing skips, 0 failures), 2 pre-existing translation warnings (not caused by P1 work, not blocking). gitnexus detect_changes advisory (scope staged): 338 symbols/58 processes/75 files, all tied to known P1 rows, no stray changes. Committed 5880fa67 (perf(P1), 76 files), then 29c37fb7 (docs: mark 31 rows committed, P1-12 stays refuted). Fast-forwarded local main to 29c37fb7. P1 tier complete: 31 committed, 1 refuted, 0 blocked.
- 2026-09-25 P2-1 staged - claim confirmed. SwapBaseTheme unconditionally rebuilt a new ResourceInclude on every apply even when the resolved URI hadn't changed - the four preset files are currently byte-for-byte equivalent stubs per the method's own pre-existing remark, so the common case churned an identical dictionary for nothing. Read RG:678 (RemEx-gcqw5) first since this is regression-guard territory; added an early-return skip before the guarded remove-sweep, without touching the sweep itself. csharp-reviewer PASS, 1 MEDIUM + 1 LOW fixed inline (the skip needed dictionaries.Contains(current) too so the sweep's self-healing survives if current is ever dropped or a duplicate appears; a comment overstated what the churn did to ThemeVariant and cited a nonexistent symbol), not re-reviewed. Reviewer surfaced a separate, likely-bigger cost in the same apply path (RequestedThemeVariant can be set to two genuinely different values on one apply for a dark-preset-in-Light-mode or SolarFlare-in-Dark user) - filed as RemEx-ap3pd rather than folded in, since it's a distinct code path from this row's ResourceInclude-churn claim. Also fixed RG:678's stale line anchors.
- 2026-09-25 P2-2 staged - claim confirmed. RefreshSavedPaletteTiles's entire output is a pure function of one caller-computed bool (live light/dark) plus each tile's own Record, which nothing calls this method to mutate, yet it ran unconditionally on every ApplyAndSave - including a slider tick that never touches light/dark mode. Added a VM-lifetime cache of the last-seen bool that skips the whole loop when unchanged. Traced every SavedPalettes mutation (Add self-refreshes before adding; Remove/Dispose-Clear need no repaint) to rule out the cache going stale and masking a real recipe change rather than just skipping a redundant one. csharp-reviewer PASS, 1 LOW fixed inline (a comment elsewhere still called the now-usually-skipped call "a second cheap repaint"), not re-reviewed. New test proves the skip by SurfaceBrush object identity, not colour equality.
- 2026-09-25 P2-3 staged - claim confirmed. Success/Warning use constant seeds at a hardcoded TonalSpot variant, so isDark and contrast are the only inputs that ever vary their output, yet McuScheme.Build ran for both on every single Generate call across 8 call sites, several of which loop once per tile within one apply at the same live (isDark, contrast). Cached both by (isDark, contrast) in ConcurrentDictionary fields; MaterialRoles' init-only properties make sharing cached instances safe. csharp-reviewer PASS, 2 LOW fixed inline (documented the cache's real, small bound instead of adding eviction for an unbounded-in-theory but tiny-in-practice key space; tightened an overstated test comment), not re-reviewed.
- 2026-09-25 P3-B6 staged - build/packaging batch, higher risk than app-logic batches since a mistake here can silently break release-only behavior with no compile error. P3-22 BLOCKED: bundled ML Kit barcode model confirmed ~6.8MB for QR-only scanning (QrScannerScreen.kt:111 scans QR only); an on-demand alternative (play-services-mlkit-barcode-scanning) exists but drops scanning on non-GMS devices and the scanned QR carries spkiHashBase64 (the pairing trust anchor) with no failure listener on process() - an undownloaded model would silently never find a code. Needs Connor's call on the non-GMS tradeoff. P3-23 BLOCKED: Firebase Analytics/Crashlytics deferral would lose crash visibility from exactly the minimal-footprint entry points (QS tile, widget) it exists to catch - the manifest's always-on config is deliberate per its own comment, tied to the Play data-safety declaration; a product decision, not a build tweak. P3-24 BLOCKED: the blanket `{ *; }` keep rules trace to two prior commits (5282f0a5, e9791283) with no recorded crash cause; WorkManager worker classes, Room _Impl classes, Glance action callbacks and ML Kit component registrars are all genuinely reflection-loaded, so narrowing without a device to prove the libraries' own consumer keep-rules suffice risks a silent runtime break. P3-25 FIXED: added `PublishReadyToRunExclude` for Microsoft.Windows.SDK.NET.dll (remex.agent.csproj, Windows TFM only) - the CsWinRT WinRT projection assembly is ~26MB and crossgen more than doubles its installed size (55.3MB measured), for an assembly whose only consumers (WGC capture, SMTC media reader) are never on the cold-start path. Excluding one assembly from R2R changes codegen only (JIT-on-first-use vs precompiled) - the IL still ships, so nothing can break at runtime, only a small first-use latency shift. csharp-reviewer PASS: confirmed `PublishReadyToRunExclude` is a real, documented MSBuild item (verified against installed SDK's CrossGen.targets, and via `dotnet msbuild -getItem` showing it populated on win-x64 and empty on the Linux TFM); traced every call site into the projection (WgcDesktopCapture, WindowsMediaSessionReader, WindowsAppIconResolver) and confirmed none are constructed or touched before an actual capture/media-read starts; confirmed the one capture-start guard (REGRESSION-GUARDS.md:59-68, WarmUpCapture's call-order dependency) is unaffected since JIT-on-first-use doesn't change call order, only its cost. 1 LOW noted (an unrelated older comment reads as though a startup JIT profile exists when this exclusion was justified by code tracing, not profiling - accepted, worth a future wording pass). P3-26 REFUTED: PC host/desktop already runs Workstation+Concurrent GC by default (documented at csproj:59-60, confirmed still true); DATAS only applies to Server GC (not in use), and GCConserveMemory would compact the LOH more aggressively, working against this app's own per-frame large-object capture allocations - the audit's own note said to measure first, and no measurement was taken to justify a change. P3-27 BLOCKED: EventSourceSupport is already false by default under NativeAOT/ILCompiler (no change possible there); OptimizationPreference=Size and StackTraceSupport=false are both genuine tradeoffs (speed on the phone's stream path; readable exception stack traces sent to logcat and back to the app) that need Connor's input, not a default-safe fix.
- 2026-09-25 P3-B5 staged - all 5 rows fixed. P3-17: AppLauncherScreen's "Recent" carousel and full grid are separate call sites onto the same rememberAppIconBitmap, so a recently-launched app's icon (shown in both) was decoded twice - distinct code from P3-12/13's widget/thumbnail caches, this is the in-app launcher screen. Added AppIconBitmapCache (4 MiB android.util.LruCache keyed on the base64 payload), mirroring FileManagerFileItem's ThumbnailBitmapCache pattern (P3-13). P3-18: both MediaMiniPlayer's docked bar and MediaNowPlayingSheet held their once-a-second ticking clock state (progress / nowElapsedMs) directly in the top-level composable's own body, forcing artwork/title/artist/MediaControlSection to recompose every second while playing; extracted the ticking bits into their own leaf composables (MiniPlayerWavyProgress, SeekSection) so only the leaf recomposes - had to define SeekSection ahead of MediaNowPlayingSheet in the file, not after, to keep the existing MediaMiniPlayerGuardTest's Slider-before-MediaControlSection text-ordering assumption true. P3-19: TutorialScreen's pager computed a `val pageOffset` from currentPageOffsetFraction in the page lambda's own composition body before handing it to graphicsLayer; moved the read inside the graphicsLayer lambda itself (draw time, not composition). P3-20: DashboardCoachOverlay's GroupSelectDemo/SelectActionBarDemo fed an Animatable's .value straight into .border()'s color argument, which is evaluated at composition time (unlike graphicsLayer's draw-time lambda) - MiniSelectCard now takes the raw Animatables instead of dereferenced Floats, and each selection ring is its own graphicsLayer{alpha=...}-wrapped layer rather than an inline border color alpha. P3-21: TaskManagerViewModel's 4s background auto-poll unconditionally flipped isRefreshing, animating the Task Manager's pull-to-refresh spinner every cycle even with no user action; refreshProcesses gained a showSpinner flag (default true, so the initial load and any user-initiated refresh are unchanged), and the steady-state poll now calls refreshProcesses(showSpinner = false). New PerfP3B5SourceShapeTest.kt (source-scan, no Robolectric in this module, same precedent as PerfP3B4SourceShapeTest) pins all 5 shapes. scripts equivalent (assembleRelease + testReleaseUnitTest): green, 981 tests. kotlin-reviewer PASS (dispatched separately after staging), 0 HIGH/CRITICAL, 1 MEDIUM deferred (P3-17's cache only saves repeat decodes - the carousel and grid can still both miss-and-decode the same icon in the same frame on first visit; a proper fix needs an in-flight-decode map, not just a completed-result cache) + 1 LOW deferred (P3-21: a ~2s window where a silent/slow host shows a blank list instead of a loading indicator between the spinner's own timeout and the error surfacing - pre-existing gap, not introduced by this row).
- 2026-09-25 P3-B7 staged - build/packaging batch, all 4 rows fixed. P3-29: ui-tooling stays `implementation` (release-only workflow; debugImplementation would drop it from the classpath the Preview pane renders against, per the build.gradle.kts comment), so instead a new release-source-set manifest overlay (app/src/release/AndroidManifest.xml) removes the library's exported androidx.compose.ui.tooling.PreviewActivity via tools:node="remove" - confirmed gone from both the merged manifest and the dex; debug keeps it. P3-30: androidResources.localeFilters = the 9 app locales plus "id" (locales_config spelling of on-disk "in") and "pt" (library strings that only ship values-pt); aapt2 dump: 85 locale configs -> 9, resources.arsc 1,956,720 -> 1,299,936 B, APK 37,098,821 -> 36,441,997 B. P3-31: `Exclude="Assets\windows\**"` on the Assets AvaloniaResource glob - no avares:// consumer exists (only BrandAssetGen/MsixAssetPlan touch those files, from disk); Remex.Desktop.dll 7,034,880 -> 6,233,088 B. P3-32: agent csproj copies only scripts/autostart-remex.ps1 (the one file RemEx.iss runs from {app}\scripts\); msbuild -getItem confirms 1 scripts item (was 22 tracked files + .remember/, ~328 KB). Existing installs keep stale scripts\ files until uninstall (Inno Setup never deletes files it no longer ships). scripts/verify.ps1 -Scope all -NoClean: PASS 6345/6347 (2 skipped), 2 pre-existing fr translation warnings. No Opus review per dispatcher (reviewed per theme).
- 2026-09-25 P3-B8 staged - 6 fixed (P3-28 embed size and P3-54 hidden-stop not done); dotnet verify PASS
- 2026-09-25 P3-B9 staged - 6 fixed; alert deadband moved to SensorViewModel; dotnet verify PASS
- 2026-09-25 P3-B10 staged - 6 fixed; Logs view detach leak also closed; Mica guard kept; verify PASS
- 2026-09-25 P3-B16 staged - PC-side 10 Hz progress coalescing; host wire cadence left unchanged
- 2026-09-25 P3-B11 staged - P3-34/37/38 fixed; P3-33/35 blocked; P3-36 refuted; verify PASS
- 2026-09-25 P3-B12 staged - P3-41/44/45/46 fixed, P3-40 partial, P3-43 refuted; verify PASS
- 2026-09-25 P3-B13 staged - all 4 fixed: icon cache, debug guards, last-seen granularity, console
- 2026-09-25 P3-B14 blocked - resume keyframe delay reverses p7fz; device-only, unverifiable here
- 2026-09-25 P3-B15 blocked - Linux-only JPEG encoder path; no Linux environment to verify
- 2026-09-25 P3 host review fixes - 3 MEDIUM: failed-step-up hold only within 15s of the step-up; icon-cache misses get a 5 min TTL, throws not cached; HWiNFO back-off test on an injected clock; dotnet verify PASS
- 2026-09-25 P4-B1 blocked - frame-buffer ring needs cross-language lifetime tracking; device-only
- 2026-09-25 P4-B2 blocked - org.json needs a String anyway; re-serialize skip too invasive
- 2026-09-25 P4-B4 staged - v2 pacing on queue depth, one-copy frames, off-reader download writes
- 2026-09-25 P4-B7 staged - opt-in processListSlim request flag; additive, no protocol bump
- 2026-09-25 P4-B5 staged - core .so Exec inputs/outputs; unused ASP.NET/VB runtime-pack assets dropped
- 2026-09-25 P4-B6 staged - X11/FreeDesktop/Native Publish=false on win RIDs only
- 2026-09-25 P4-4 review fix staged - queue depth was always 0 (SingleReader Reader.Count throws); now a hand-kept counter in CountedOutboundQueue, one EnqueueOutbound write path, real-channel tests
- 2026-09-25 P4-B3 staged - launchers.json parse cache; save generation keeps VULN-3 removals immediate
- 2026-09-25 P4-B8 staged - GDI scratch bitmaps reused, Linux BGRA passthrough; P4-12/14 blocked
- 2026-09-25 P4-B9 staged - Linux frame wait uses semaphore timeout; no CTS or exception
- 2026-09-25 P4-B10 blocked - multi-viewer encoder/capture/cursor sharing is one redesign
- 2026-09-25 P4-B11 staged - P4-19/21/22/24 fixed, P4-20 refuted (measured), P4-23 blocked
- 2026-09-25 P4-B12 refuted - portal injector poll unreachable; claim holds, no action
- 2026-09-25 P4-B13 refuted - RemEx-4q6l measurement stands; hidden-window gate not worth it
